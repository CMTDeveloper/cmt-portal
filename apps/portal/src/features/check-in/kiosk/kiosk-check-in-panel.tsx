'use client';
import type { Family } from '@cmt/shared-domain/check-in';

interface Props {
  family: Family;
  onDone: () => void;
}

export function KioskCheckInPanel(_props: Props) {
  return null;
}
