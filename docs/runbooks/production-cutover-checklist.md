# Production Cutover Checklist — Firebase (UAT → Prod)

> **Audience:** CMT Developer + AI agents. This is the authoritative runbook for moving the Setu portal from UAT Firebase (`chinmaya-setu-uat`) to production, **without breaking the standalone `chinmaya-family-check-in` app that shares the prod project**. Read the Golden Rules before doing anything.
>
> **Last updated:** 2026-06-07. Keep this current as new collections/scripts/flags land.

---

## 0. Golden rules (read every time)

1. **PROD project `chinmaya-setu-715b8` is SHARED** with the still-live standalone `chinmaya-family-check-in` kiosk app. It has its **own Firestore collections AND its own composite indexes** in 715b8. We must be purely additive.
2. **NEVER `firebase deploy --only firestore:indexes ... --force`** against `chinmaya-setu-715b8`. `--force` deletes indexes not present in *our* `firestore.indexes.json` → it would delete the standalone app's indexes and break the prod kiosk. Always deploy **without** `--force`; the CLI will warn "the following indexes are defined in your project but are not present in your firestore.indexes.json" — **leave them; do not delete.**
3. **Never touch the door-app collections** in 715b8: `family-check-ins`, `guest-families`, and the legacy RTDB `/roster`. The portal reads these **read-only** via the MASTER service account. No writes, ever.
3b. **⛔ NEVER deploy `firestore.rules` to `chinmaya-setu-715b8`.** The repo's rules file is a **deny-all, written for portal-only UAT**. The standalone door app reaches prod through the **CLIENT** SDK (`chinmaya-family-check-in/app/lib/firebase/config.ts:26`), so a deny-all there instantly breaks kiosk check-in, family self check-in, the teacher check-in screen, the teacher report and guest check-in - and a rules deploy **replaces** the ruleset rather than merging, while **neither repo tracks a rules file**, so there is nothing to roll back to. This bites two ways: `--only firestore:rules` **and** a bare `firebase deploy` (since 2026-07-25 `firebase.json` points at `firestore.rules`, so rules ride along with an indexes deploy). Prod needs the live ruleset exported as a baseline first, then targeted denies added on top. Rule 5 below is what keeps this honest - always name the project.
4. **Scripts are UAT-guarded.** Every ops script refuses to run unless `PORTAL_FIREBASE_PROJECT_ID === 'chinmaya-setu-uat'`, *unless* you pass `--allow-prod`. Running against prod is a deliberate, explicit act — you must set the prod project AND pass `--allow-prod`.
5. **No `.firebaserc`** in this repo → **always pass `--project <id>` explicitly** to every `firebase` command. Never rely on a default project.
6. **`NEXT_PUBLIC_*` env vars are sensitive-by-default on Vercel Production.** Add them with `--no-sensitive` or `vercel env pull` reads them back blank and the client bundle silently gets `undefined`. (See §9.)
7. **The two apps run in parallel** until the kiosk cutover (`NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK=true`). Until then, do NOT retire the standalone deployments and do NOT remove their data/indexes.

---

## 1. Prod target — CONFIRMED: `chinmaya-setu-715b8` (shared)

**Decided 2026-06-07 (CMT Developer):** the production Setu Firestore **is `chinmaya-setu-715b8`** — the same project as MASTER and the standalone door app. The portal's Setu collections (families, offerings, levels, enrollments, …) are written **into 715b8 alongside** the door app's collections. They use distinct names (§3), so writes are **additive** — but the index + collection rules in §0 are non-negotiable.

- `PORTAL_FIREBASE_PROJECT_ID` flips from `chinmaya-setu-uat` → `chinmaya-setu-715b8`, becoming the **same** project as `MASTER_FIREBASE_PROJECT_ID`. (Both credential sets target 715b8 — Firestore for PORTAL, Firestore+RTDB for MASTER.)
- Index deploys go to 715b8 **without `--force`** (§5).
- A dedicated separate prod project was considered and **rejected** in favour of the shared model.

> ⚠️ **Safety gate:** the standing "never write `715b8`" directive (2026-05-31) is **consciously lifted only for the deliberate cutover window** described in this runbook. Outside a planned cutover/migration, day-to-day dev + all automated ops still target `chinmaya-setu-uat`. Treat every `--allow-prod` run as a reviewed, intentional act.

---

## 2. The two Firebase projects

| Role | Project ID | Holds | Portal access |
|---|---|---|---|
| **PORTAL_FIREBASE** (current) | `chinmaya-setu-uat` | All portal Setu Firestore collections + Firebase Auth (OTP users). Portal-only. | Read/write. `--force` index deploys safe. |
| **MASTER_FIREBASE** | `chinmaya-setu-715b8` (PROD) | Legacy RTDB `/roster`; door-app Firestore `family-check-ins`, `guest-families`; **its own composite indexes**. Shared with standalone app. | **Read-only** via MASTER service account. |
| **PORTAL_FIREBASE** (after cutover) | `chinmaya-setu-715b8` | Portal Setu collections written **additively** into the shared prod project. | Read/write portal collections only; never `--force` indexes; never touch door collections. |

**Credentials** (service-account, set per environment):
- `PORTAL_FIREBASE_PROJECT_ID` / `PORTAL_FIREBASE_CLIENT_EMAIL` / `PORTAL_FIREBASE_PRIVATE_KEY`
- `MASTER_FIREBASE_PROJECT_ID` / `MASTER_FIREBASE_CLIENT_EMAIL` / `MASTER_FIREBASE_PRIVATE_KEY` / `MASTER_FIREBASE_DATABASE_URL`
- Public client config: `NEXT_PUBLIC_PORTAL_FIREBASE_PROJECT_ID` / `_API_KEY` / `_AUTH_DOMAIN`

> The MASTER service account must have **Firestore read** permission on 715b8 (already confirmed via `pnpm check:door-access`). Re-confirm in prod.

---

## 3. Collection ownership map (in prod 715b8)

**DO NOT TOUCH — owned by the standalone `chinmaya-family-check-in` app:**
- Firestore: `family-check-ins`, `guest-families` (hyphenated names)
- Firestore: **`verification_codes`** — the door app's OTP store, and the ONLY collection in prod whose ruleset allows **unauthenticated `create`**. The portal deliberately does NOT use it (see `setu_verification_codes` below). Never point portal code at this name: `verifyCode()` trusts the document's own fields and the Admin SDK bypasses rules, so a client-forged document there is a pre-auth account takeover.
- RTDB: `/roster` (the legacy student roster — source of truth for migration, read-only)
- Plus the standalone app's own Firestore composite indexes (invisible to our file; protected by the never-`--force` rule).

**PORTAL-OWNED — safe to create/write (additive; distinct names):**
`families` (+ subcollections `members`, `enrollments`, `invites`), `contactKeys`, `offerings`, `donationPeriods`, `levels`, `programs`, `donations`, `classCalendarEntries`, `attendanceEvents`, `attendance`, `check_in_events`, `checkIns`, `guest_check_ins`, `seva_opportunities`, `seva_signups`, `achievements`, `setu_verification_codes` (**renamed from `verification_codes` on 2026-07-26 — the name is a security control, see §14**), `otp_rate_limit`, `weeklySchedules`, `family_notifications`, `counters` (issue #4 — `counters/familyPublicId` + `counters/memberPublicId`, monotonic public-id allocators; self-create on first allocation, no seed), `app_config/disclaimers` (slice 2 — admin-editable disclaimer content; created on the first admin publish, absent = seed DEFAULT version 1, no seed write required), `pledges` (P5 — one doc per monthly-pledge attempt; **status + opaque Stripe handles only, never a bank detail**. Self-creates on the first pledge, no seed, **no composite index** — see §14 2026-07-27. Dormant in prod while `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` is unset).

> **Issue #4 additive fields** (no new collection beyond `counters`): `families.publicFid` (4-digit, from 1001) + `members.publicMid` (5-digit, from 50001) — both nullable; legacy `fid`/`mid` doc IDs are unchanged and remain the join keys.

> **Aug-3 launch batch additive items** (spec `docs/superpowers/specs/2026-07-24-aug-3-launch-batch-design.md`, §14 entries 2026-07-24/25):
> ⚠️ **Adding a family-level field? There are THREE hand-written projections to update, none with a spread:** `features/setu/members/get-family-by-fid.ts` (backs `/family` + the profile gate), `features/setu/search/get-family-for-welcome.ts` (backs `/welcome/family/[fid]`), and the inline literal in `app/api/setu/dashboard/route.ts` (the mobile aggregate - deliberately narrower than `FamilyDoc`, so a `grep ': FamilyDoc = {'` audit MISSES it; that is how it was missed on 2026-07-26). A field added to `FamilyDocSchema` but not to a projection is `undefined` forever on that surface.
>
> - `families.locationNeedsConfirmation` (boolean, nullable+optional) — set by lazy migration when the legacy centre was unknown and `mapLocationDetailed()` fell back to Brampton; drives the centre prompt on `/complete-profile` and is cleared (written as an explicit `false`) by `PATCH /api/setu/family` when the family picks. **Only the literal `true` gates anything**: absent/null = never flagged, `false` = asked and answered. No index; no migration. **Shipped 2026-07-26** — see §14. `location` itself is never emptied while unconfirmed (it holds the defaulted `'Brampton'`), because `FamilyDocSchema.location` is a read-validated `z.string().min(1)`.
> - **`audit_log`** — NEW portal-owned collection, one doc per staff write to another family (`{actorUid, actorMid, actorRole, action, fid, mid|null, before, after, at}`), written inside the SAME transaction as the mutation so an audit gap is structurally impossible. Backs the welcome-team cross-family edit surface. Self-creates on first write: no seed, no migration, **no index** (no query surface at launch). E2E rows carry `_test: true` for the cleanup sweep. **Shipped 2026-07-26.**
> - `guest_check_ins.sessionDate` (string `YYYY-MM-DD`, the Sunday of the walk-in week) — the key every teacher visitors query now filters on. `date` is retained as the actual walk-in day and is NO LONGER a primary read key. Single-field equality, auto-indexed; **no composite index**. Backfill via `backfill:guest-session-date` (§6 step 12, §10) — required in prod before the `date` fallback leg is removed from `readPortalGuestChildren`.

> **Slice 2 additive field** (no new collection): `families.disclaimersAccepted` `{ schoolYear, version, acceptedByMid, acceptedAt }` — optional; absence = **must-accept**, NO backfill/migration.

> **Pending-invite additive field** (2026-07-17, no new collection): `members.inviteStatus: 'pending' | null` — optional/nullable, absence ⇒ a normal active member. Set to `'pending'` on a member created at invite-SEND (visible before accept), cleared to null on accept. NO backfill/migration (materializes only on new invites); read-tolerant everywhere.

> **Pre-cutover audit:** before the first prod write, run a one-off read against 715b8 to confirm **none** of the portal-owned collection names already exist there (they shouldn't — different naming from the door app). If any unexpectedly exists, STOP and investigate a possible collision before writing.

---

## 4. Pre-cutover prerequisites (do these before any data migration)

- [ ] **§1 decision confirmed** (prod target project) and the 2026-05-31 "never write 715b8" directive consciously lifted for the cutover window.
- [ ] **Service accounts** for the chosen prod `PORTAL_FIREBASE` exist with Firestore + Auth admin; `MASTER_FIREBASE` SA has Firestore read on 715b8.
- [ ] **All env vars set on Vercel Production** (see §9 for the full list). Pay attention to the `NEXT_PUBLIC_*` sensitive gotcha.
- [ ] **AWS SES**: prod `AWS_SES_FROM_EMAIL` identity verified in `AWS_SES_REGION`; out of the SES sandbox (or all recipients verified).
- [ ] **AWS SNS**: `AWS_SNS_REGION` has an **Origination Number** for Canadian (+1) SMS; account out of the SNS sandbox; spend limit raised; no stuck opt-outs. Diagnose with `pnpm --filter @cmt/portal exec tsx scripts/debug-sns-config.ts` and set defaults with `scripts/sns-set-defaults.ts`. (Module-cached SNS client needs a **cold redeploy** when region changes.)
- [ ] **Stripe**: live `STRIPE_API_KEY`, prod `STRIPE_CHECKOUT_URL` (Cloud Run proxy), `STRIPE_USE_TEST_CHECKOUT=false`, `WEBHOOK_API_KEY` set.

  ✅ **RESOLVED 2026-07-28 — the live-mode service EXISTS.** Owner supplied
  `https://cmt-stripe-checkout-api-**prod**-195790402763.northamerica-northeast1.run.app`.
  Verified by minting a session through it: returned **`cs_live_…`**, so it holds real Stripe keys. (The earlier dead host,
  `stripe-checkout-api-195790402763…` with no `-prod-`, is simply obsolete — it has no Cloud Run service at all. And
  `…-**test**-…` returns `cs_test_`, so it must NEVER be used for `STRIPE_CHECKOUT_URL` in production: it would look
  correct and collect nothing.)

  **Go-live values — PRODUCTION SCOPE ONLY (Preview must stay on the test host):**
  ```
  STRIPE_CHECKOUT_URL      = https://cmt-stripe-checkout-api-prod-195790402763.northamerica-northeast1.run.app/checkout-link
  STRIPE_USE_TEST_CHECKOUT = false
  ```

  ⛔ **DO NOT point `STRIPE_API_BASE_URL` at the prod host. `/pad/*` IS NOT DEPLOYED THERE.** Measured 2026-07-28 — on the
  prod service `/checkout-link` answers 400 (route exists) but **all four of** `/pad/setup-link`,
  `/pad/monthly-subscription`, `/checkout-session-result` and `/subscription-result` answer **404**. They exist only on the
  test service. So:
  - **One-time donations CAN go live on Aug 3.** They only need `/checkout-link`.
  - **Monthly pledges CANNOT — until `/pad/*` lands on the prod service.** Vaibhav committed 2026-07-28 to deploying it
    before Aug 3. Until the probe below returns **400 (not 404)** for `/pad/setup-link` on the PROD host,
    **`NEXT_PUBLIC_FEATURE_SETU_PLEDGE` must be OFF in production**: with the base URL still on the test host a family
    would authorise a mandate against a TEST price while four surfaces record them as paid (§14 2026-07-27).
  - **The flag and the base URL must move TOGETHER.** Flipping the flag on while `STRIPE_API_BASE_URL` still points at
    the test host is the failure above; pointing the base URL at prod before `/pad/*` exists there is a 404 on every
    pledge. Verify with the probe, then change both, then redeploy.
  - **Gate for enabling the pledge in production (all three, in order):**
    1. `POST <PROD>/pad/setup-link` with an empty body returns **400**, not 404.
    2. `STRIPE_API_BASE_URL` (production scope) → the prod host.
    3. `STRIPE_PLEDGE_PRICE_ID` → the **live** $51/mo Price, confirmed in the Stripe dashboard. Nothing in this repo can
       detect a wrong Price; the amount lives at Stripe.

  **The prod service validates more strictly than the test one** — it rejects a request with no `client_reference_id`
  (`{"error":"Invalid client_reference_id"}`). `api/setu/donations/checkout/route.ts` already sends `client_reference_id:
  donation.did`, and the full portal payload was verified against the prod host successfully. Re-verify with the probe
  below after ANY change to that payload.
  ⛔ **DO NOT flip `STRIPE_USE_TEST_CHECKOUT=false` until the live-mode host is proven to exist.** Measured 2026-07-27: the host in `STRIPE_CHECKOUT_URL`
  (`stripe-checkout-api-195790402763.northamerica-northeast1.run.app`) answers **Google's front-end 404 on every path including `GET /`** — no Cloud Run
  service is deployed there. Only the **test** host (`cmt-stripe-checkout-api-test-…`) is alive. Flipping the flag today would take one-time donations from
  working to broken, on launch day, at the moment families pay. Prove any candidate host BEFORE flipping — an empty body creates nothing, and the two
  answers are unambiguous (**400 = right host, route exists; 404 = wrong host**):
  ```bash
  curl -s -o /dev/null -w "%{http_code}\n" -X POST "<BASE>/checkout-link" \
    -H 'Content-Type: application/json' -H "x-api-key: $STRIPE_API_KEY" -d '{}'
  ```
  Distinguish the two 404s by body, not status: Google's `<h1>Error: Page not found</h1>` means **no service**; Express's `Cannot GET /` means the app is up.
  `STRIPE_API_BASE_URL` (pledge) and `STRIPE_CHECKOUT_URL` (donations) point at the SAME service and must move to live mode **together** — one left behind
  is a half-live payment path.
- [ ] **`CRON_SECRET`** set (Vercel Cron: daily cache-reset, weekly payment reminders).
- [ ] **Session**: `SESSION_COOKIE_EXPIRES_DAYS` ≤ 14 (Firebase hard cap — never exceed).
- [ ] **`NEXT_PUBLIC_PORTAL_BASE_URL`** = the prod domain (`https://cmt-setu.vercel.app` or the custom domain) — used in invite-email links.
- [ ] **Local `.env.local`** for running migration scripts points `PORTAL_FIREBASE_*` at the prod project and `MASTER_FIREBASE_*` at 715b8.

---

## 5. Firestore index deploy (prod) — the careful way

The repo's `firestore.indexes.json` holds the **portal's** indexes only. Deploy them to prod **additively**:

```bash
# From repo root. NEVER --force. Always explicit --project.
firebase deploy --only firestore:indexes --project chinmaya-setu-715b8
```

- Expect a warning listing indexes "defined in your project but not present in your firestore.indexes.json" — those are the **standalone app's** indexes. **Do NOT delete them.** Answer No / leave them.
- Indexes build asynchronously (minutes). A query against a still-building index throws `FAILED_PRECONDITION: index is currently building`. Wait and retry.
- Portal indexes that must exist before the corresponding feature works in prod:
  - `check_in_events (fid ASC, checkedInAt DESC)` — family dashboard (B2).
  - `enrollments (pid ASC, status ASC)` collectionGroup — teacher roster.
  - `enrollments (oid ASC, status ASC)` collectionGroup — **school-year rollover** discovery.
  - `families (searchKeys CONTAINS, location ASC)` — welcome-team search.
  - `members (publicMid ASC)` collectionGroup **field-override** — issue #4 roster/search lookup by the 5-digit Member ID (`collectionGroup('members').where('publicMid','==')`). (`families.where('publicFid','==')` needs no entry — single-field top-level equality is auto-indexed.)
  - `enrollments (location ASC)` collectionGroup **field-override** — the `/admin/locations` remove-guard (`countLocationReferences` runs `collectionGroup('enrollments').where('location','==').count()`). A collectionGroup single-field query is NOT auto-indexed; without this override the "remove a centre" path 500s. (The top-level `families`/`offerings`/`levels` `.where('location','==')` counts in the same guard ARE auto-indexed — only the collectionGroup one needs an entry.)
  - `offerings`, `donations`, `levels`, `attendanceEvents`, `classCalendarEntries`, `invites`, `seva_opportunities` composite indexes (all in `firestore.indexes.json`).

> UAT (`chinmaya-setu-uat`) is portal-only, so `--force` there is safe. **715b8 is never `--force`.**

---

## 6. Data migration sequence (prod)

> Run from `apps/portal`. Every command needs `.env.local` pointed at prod and the explicit **`--allow-prod`** flag (scripts refuse non-UAT otherwise). **Dry-run first**, inspect, then real run. These are idempotent (deterministic doc IDs, `set(merge)`), so re-runs are safe.

> 🛑 **STEP 0 — REFRESH THE RTDB SNAPSHOT FIRST (added 2026-07-24).** `migrate-legacy-families.ts` → `listAllFamilies` → `readRtdb`, and `readRtdb` (`packages/firebase-shared/src/admin/rtdb.ts:45-52`) serves from the **local snapshot and never touches the network** whenever `RTDB_SNAPSHOT_DIR` is set — which `CLAUDE.md` tells you to keep set in `apps/portal/.env.local`. **Migrating against a stale snapshot silently omits every family registered or changed since it was captured, with no error.** Check `.rtdb-snapshot/meta.json` `capturedAt` and refresh before migrating. Cost is negligible — the whole snapshot is ~1.6 MB, i.e. ~$0.0016 at RTDB's ~$1/GB. (The CLAUDE.md "never download RTDB" rule targets repeated dev/test full-node reads, not one deliberate pre-cutover capture.)
> ```bash
> cat apps/portal/.rtdb-snapshot/meta.json          # check capturedAt
> pnpm --filter @cmt/portal snapshot:rtdb           # one live read; rewrites the snapshot
> ```
> Then compare the family count against the previous capture so the drift is a measured number, not an assumption.

Order matters — later steps depend on earlier ones:

1. **Seed the program scaffolding** (offerings + levels + calendar). These define the school-year structure the migration enrolls into.
   ```bash
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-donation-periods.ts --allow-prod
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/migrate-to-programs.ts --allow-prod      # donationPeriods → offerings
   # Levels: the committed LOCATIONS defs ARE the CURRENT (2026-27) CMT sheet — pass the
   # year prod launches at EXPLICITLY (a bare run defaults to --year 2025-26 but now writes
   # 2026-27 curriculum). Then --verify (read-only) proves prod == the sheet, matching UAT.
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-bala-vihar-levels.ts --year 2026-27 --dry-run --allow-prod
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-bala-vihar-levels.ts --year 2026-27 --allow-prod
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-bala-vihar-levels.ts --year 2026-27 --verify   # exit 0 = prod matches the sheet
   # Calendar: committed ENTRIES_BY_YEAR['2026-27'] = the CMT sheet's "Class Dates" tab
   # (Brampton). Pass the launch year + location explicitly; dry-run first. Scarborough's
   # dates aren't in the sheet yet — seed --location Scarborough when Vaibhav sends them.
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-bala-vihar-calendar.ts --year 2026-27 --location Brampton --dry-run --allow-prod
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-bala-vihar-calendar.ts --year 2026-27 --location Brampton --allow-prod
   ```
2. **Migrate legacy families** from the 715b8 RTDB `/roster` into Setu Firestore (families + members + contactKeys). Dry-run first.
   > ⚠️ **Superseded by lazy migration (§14 2026-07-14).** Under the lazy `publicFid` model (Model Y2) the prod cutover does NOT bulk-migrate: a legacy family enters Setu lazily on its first engagement (OTP sign-in / kiosk check-in / teacher add), and `lazyMigrateLegacyFamily` sets its kids' `legacySid` at that point (so step 3 is also moot). This also means steps 4-5 & 8 (bulk enrollment + public-id backfills) are not the prod path - enrollments + `publicFid` are minted lazily at first enrollment. Run this bulk migrate ONLY if you deliberately want the whole legacy roster pre-loaded into prod Setu for the welcome tools at launch (the welcome roster is Setu-only for now); otherwise skip steps 2-5. **Step 8 (the BV enrollment backfill) is a SEPARATE launch decision** — it, not this bulk migrate, is what populates the teacher **Enrolled / Previous-students** rosters at launch, and it is FID-safe (mints no `publicFid`). Don't lump step 8 into "skip the eager path": decide it on its own (see step 8).
   ```bash
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/migrate-legacy-families.ts --dry-run --limit 20 --allow-prod
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/migrate-legacy-families.ts --allow-prod   # ~864 families, ~15 min
   ```

   > 🟡 **DORMANT FAMILIES ARE SKIPPED BY DEFAULT (added 2026-07-26, spec §1.9b).** A family with **no centre AND no level on any roster row** is not migrated. Measured on the 2026-06-10 snapshot: **299 of 867 skipped, 568 migrated**, holding **190 grade-mappable children**. Migrating them would drop those children — with grades years out of date — into Brampton teachers' **"Registered · not enrolled"** lists on launch Sunday, at levels they will never attend (that list is location+grade driven, not enrollment driven).
   >
   > **Nothing is lost.** `lazyMigrateLegacyFamily` still runs on their first OTP sign-in, kiosk check-in, or teacher add, and at that point they are asked for their real centre and the parent re-confirms the grade. Contact coverage of the skipped set was measured: **299 have an email, 238 also a phone, 0 have neither.**
   >
   > - Every examined family lands in `--csv-out`, the skipped ones with `status=dormant`, so the set is auditable rather than a number nobody can check.
   > - **`--include-dormant`** migrates them anyway. You almost certainly do not want it — that flag is what puts the 190 stale-grade children back into teachers' lists.
   > - **Expected run summary at prod cutover:** `Dormant: ~299` (exact number depends on the refreshed snapshot). **This is not breakage.**
   > - ⚠️ **ACCEPTED LIMIT — skipped families are NOT findable by welcome-team at the desk.** `searchFamilies()` queries only `collection('families')` and `/welcome/roster` browses the same, and **neither has a legacy-roster fallback** (unlike the family's own sign-in and the kiosk, which do — `find-family-by-contact.ts:62-71`). So on launch Sunday a skipped family cannot be looked up by name, phone or legacy FID. Expected desk traffic is near zero (all 299 are dormant with no active child), and the first thing any of them does migrates them. **Recorded as a known limit, not an oversight.**
   > - `/welcome/roster`'s migration-status check subtracts these, reporting them as `skippedDormant` rather than `missing` — so it reads **green**, not a permanent amber "299 not yet in portal". A dormant family that has since signed in counts as **migrated**, not skipped.
3. **Backfill legacy student IDs** onto members (links Setu members ↔ roster rows for attendance/door bridging).
   ```bash
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/backfill-legacy-sid.ts --allow-prod
   ```
4. **Backfill current Bala Vihar enrollments** (enrolls currently-registered kids into the 2025-26 offering; writes `pid:oid`; deactivates all-graduated families).
   ```bash
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/backfill-bv-enrollments.ts --dry-run --limit 30 --allow-prod
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/backfill-bv-enrollments.ts --allow-prod
   ```
5. **Backfill public IDs (issue #4)** — assign a 4-digit `publicFid` to every existing family + a 5-digit `publicMid` to every member. Depends only on the family migration (step 2); independent of enrollments. Idempotent (skips already-stamped docs); oldest-first so the oldest family gets 1001. Deploy the `members.publicMid` index (§5) first. Dry-run + CSV first.
   > ⚠️ **`publicFid` half is superseded by lazy minting (§14 2026-07-14).** The family `publicFid` is now minted at a family's FIRST enrollment (`enrollFamily`), NOT backfilled - so `migrate:public-ids`'s publicFid pass and the whole `renumber:public-ids` step are NOT run at prod cutover. (`publicMid` is still minted at member creation; only run this if you bulk-pre-loaded families in step 2 and want their members stamped.)
   ```bash
   pnpm --filter @cmt/portal migrate:public-ids --dry-run --csv-out /tmp/public-ids.csv --allow-prod   # preview the allocation
   pnpm --filter @cmt/portal migrate:public-ids --allow-prod   # publicFid 1001+, publicMid 50001+ (UAT run: 877 families / 2668 members)
   ```
6. **Grant admin / welcome-team** to the right people (so the admin surfaces are reachable).
   > ⚠️ `grant-admin.ts` now requires the target to be a **registered portal user** — they must sign in at least once (OTP) *before* the grant, otherwise it exits with `registered-user-required`. (`grant-welcome-team.ts` still auto-provisions a missing auth user.)
   ```bash
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/grant-admin.ts <email-or-phone> --allow-prod
   pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/grant-welcome-team.ts <email-or-phone> --allow-prod
   ```
7. **(Annual, later) School-year rollover** — only when promoting to 2026-27. See §7.
8. **⭐ LAUNCH DECISION — populate the teacher rosters. (After the launch year is activated + its BV offerings are seeded, before kiosk cutover) Run the current-year BV enrollment backfill.** The teacher attendance screen's **Enrolled** AND **Previous students** lists are *enrollment-driven*: a level shows a student ONLY if their family has an active enrollment for that term's `pid` (`deriveRoster`). Under the pure-lazy cutover (steps 2-5 skipped) no legacy family has one at launch, so **every teacher's roster — including "Previous students" — starts EMPTY**; a returning student appears only once their family engages (kiosk check-in / teacher add-student / portal enroll). To give teachers a working roster on day one, run this backfill: it migrates + enrolls currently-registered legacy families (skips graduated/inactive — null legacy `level`), so their kids surface in **"Previous students"** as unconfirmed carry-forwards (e.g. `legacyFid 477` / Harshita, Grade 2 → Level 2). **It is FID-safe — it does NOT mint a `publicFid`** (verified 2026-07-15: the enrolled family's `publicFid` stays null until the enrollment is *confirmed* via a teacher mark / donation), so it is fully consistent with the lazy-`publicFid` model. Idempotent, live-year (`getSchoolYearConfig().currentYear`); **skips** families that already have an active current-year enrollment (rollover-promoted grades untouched) and cancels any stale prior-year BV enrollment for the families it enrolls.

   > 🛑 **DECIDED 2026-07-24 (CMT Developer): DO NOT RUN THIS AT THE 2026-08-03 CUTOVER.** The ⚠️ note below is the operative guidance; the earlier "Recommended for launch" wording was written before the 2026-07-20 rollover change and has been removed as stale. Teachers see returning students via the attendance screen's **"Not in this class yet"** section instead - its *Registered · not enrolled* group is location+grade driven, NOT enrollment driven, so it populates from the §6 step 2 bulk migration alone and marking a child present enrolls them. Verified 2026-07-24: `features/setu/teacher/grade-eligible.ts:68-123` reads `families.where('location','==')` + a `collectionGroup('members')` scan, both satisfied by `lazy-migrate.ts:177-197`, with **no new Firestore index**.
   >
   > ⚠️ **RECONSIDER under the 2026-07-20 rollover model (grade+level only, no carry-forward).** This backfill does the SAME auto-registration that the rollover change deliberately removed: it enrolls currently-registered legacy families from their roster membership, producing "Registered" carry-forwards — i.e. it would recreate the "500 registrations". If the intent is a clean prod-launch roster (Vaibhav's model), **do NOT run this backfill**: let teacher rosters start empty and fill as families engage (kiosk check-in / teacher add / self/portal enroll), each of which enrolls the family AND mints the FID, and surfaces returning kids in the teacher **"Registered · not enrolled"** (grade-eligible) section. Run the backfill only if you explicitly want last year's roster pre-loaded as unconfirmed carry-forwards on day one (accepting the inflated "Registered" count). Decide this deliberately.
   ```bash
   pnpm --filter @cmt/portal backfill:bv-enrollments --dry-run --limit 30 --allow-prod
   pnpm --filter @cmt/portal backfill:bv-enrollments --allow-prod
   ```

9. **Backfill `guest_check_ins.sessionDate`** (only if prod already holds portal guest check-ins written before the sessionDate change; the field shipped 2026-07-25). Teacher visitors queries key on `sessionDate`; `readPortalGuestChildren` still reads the old `date` as a transitional fallback, so this is not launch-blocking - but the fallback leg cannot be removed until this has run.
   ```bash
   pnpm --filter @cmt/portal backfill:guest-session-date -- --dry-run --allow-prod   # ALWAYS first
   pnpm --filter @cmt/portal backfill:guest-session-date -- --allow-prod
   ```
   Spot-check the dry-run output: a doc whose `date` is already a Sunday must map to **itself**, never to the Sunday a week earlier. Docs with no `date` are skipped and reported (pre-2026-07-24 docs predate the field entirely). Re-runs are idempotent; `--recompute` is the recovery path if a run ever writes wrong values.

> After each step, spot-check with `inspect-setu-family.ts` / `inspect-legacy-roster.ts` / `list-uat-families.ts` (rename mentally to "list families") and `check-uat-migrations.ts`.

---

## 7. School-year rollover (2025-26 → 2026-27) — annual

Two ways: the admin UI at `/admin/school-year` (recommended — guided + preview), or the CLI. CLI:

```bash
# 1. Preview what Step 1 would create (no writes)
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/start-new-year.ts --dry-run --allow-prod
# 2. Create next year's levels + offerings (idempotent; empty teacher slots)
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/start-new-year.ts --allow-prod
# 3. Preview the promotion (no writes) — shows advance/graduate/needs-attention + per-level transitions
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/promote-families.ts --dry-run --allow-prod
# 4. Fix any "needs-grade" kids (names/fids printed), then commit the promotion
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/promote-families.ts --allow-prod
# 5. Re-run the preview → should report ~0 to advance (idempotency proof)
```
Requires the `enrollments (oid, status)` collectionGroup index (deploy via §5 first). Promotion is grade-driven, idempotent, history-preserving (`levelSnapshots`); a family with only missing-grade kids keeps its enrollment active (never stranded).

**Current school year is app-managed (no code bump at rollover).** The active year lives in Firestore at `app_config/school_year` (`{ currentYear: 'YYYY-YY' }`), set by admins via the editor on `/admin/school-year` (GET/PUT `/api/admin/school-year`, admin-only). Prasad period resolution (`getCurrentPrasadPeriods`) and the rollover `from`/`to` defaults read it, so the old per-rollover bump of `CURRENT_PRASAD_PIDS` / `DEFAULT_TO_YEAR` is gone — those constants are now `FALLBACK_PRASAD_PERIODS` / `DEFAULT_FROM_YEAR` fallbacks only. At rollover, update the year once in `/admin/school-year` (no deploy). No new Firestore index — the config read reuses the existing `offerings (programKey, termLabel)` shape. The `e2e/setu/test-accounts.spec.ts` BV/level pins are still hand-bumped at rollover (separate from prasad).

---

## 8. Feature-flag flip sequence (Vercel Production)

Flags live in `apps/portal/src/lib/flags.ts`, read from `NEXT_PUBLIC_FEATURE_*` env vars. **`NEXT_PUBLIC_*` are statically inlined** — a change requires a **rebuild/redeploy** (an env-only update does nothing until a fresh build). Flip on only after the data migration + index deploy + a UAT-equivalent walkthrough:

| Flag env var | Enables | Flip when |
|---|---|---|
| `NEXT_PUBLIC_FEATURE_SETU_AUTH` | OTP sign-in + `/family/*` | After families migrated + SES/SNS prod-verified |
| `NEXT_PUBLIC_FEATURE_SETU_DONATIONS` | Donation/Stripe flow | After Stripe live + offerings seeded |
| `NEXT_PUBLIC_FEATURE_SETU_TEACHER` | Teacher attendance | After BV enrollments backfilled + roster verified |
| `NEXT_PUBLIC_FEATURE_CHECK_IN*` | Legacy check-in surfaces | Per parallel-run plan |
| `NEXT_PUBLIC_FEATURE_SETU_ADULT_CLASS` | The Adult Study Class gate: `/adult-class`, `POST /api/setu/adult-class`, and the fourth `/family` gate | **Only after** an `adult-study-class` PROGRAM doc (status `active`) and an enabled OFFERING exist for every location, with the fee confirmed. With the flag on and no offering the gate self-disables (condition 0), but with an offering at the wrong price every paid Bala Vihar manager is redirected to it on their next `/family` visit. Needs UAT E2E green first. |
| `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` | Monthly pre-authorized-debit giving: the `/family` + `/donate/success` card, `POST /api/pledges/{start,finalize}`, and the daily reconciler cron | **LEAVE UNSET AT LAUNCH.** `/pad/*` on the payment service is Stripe **TEST** mode, so flipping this is not a no-op: the first family through afterwards signs the **first REAL bank mandate**. Before flipping, work the checklist at the foot of `docs/superpowers/plans/2026-07-26-launch-p5-monthly-pledge-v3.md` — above all, **open the Stripe dashboard and confirm the LIVE Price really is $51**. The amount lives at Stripe; nothing in this codebase can detect a wrong one, and it would debit monthly and undetectably. |
| `NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK` | **Door cutover** — retires the standalone kiosk | **Last**, only after parallel-run proven. After this, the standalone app + its 715b8 indexes can finally be retired. |

> Release timing: Setu routes are not announced to families until donations + teacher are both proven (per the 2026-05-22 decision). Legacy `/login` + `/check-in/*` stays the production entry point until then.

---

## 9. Environment variables (full prod set)

Source of truth: `turbo.json` `tasks.build.env` (must list every var or Vercel builds strip it from the sandbox). Set all on **Vercel Production**.

### 9.0 Vercel environment split (established 2026-07-27)

**Until 2026-07-27 there was no split**: Vercel **Preview** and **Production** both pointed at `chinmaya-setu-uat`, and `https://cmt-setu.vercel.app` — the URL everyone calls production — was UAT with a nicer name. Preview also held only 36 of the 62 variables, so preview deploys ran with **every feature flag off and no Stripe config at all**.

**Now:**

| | Vercel **Preview** | Vercel **Production** |
|---|---|---|
| `PORTAL_FIREBASE_*` | `chinmaya-setu-uat` | `chinmaya-setu-uat` **→ `chinmaya-setu-715b8` on Aug 3** |
| `MASTER_FIREBASE_*` | `chinmaya-setu-715b8` (read-only) | `chinmaya-setu-715b8` (read-only) |
| Feature flags | all on (see the parity warning below) | all on |
| Stripe | test host, `STRIPE_USE_TEST_CHECKOUT=true` | test host today; **live on Aug 3 — blocked, see §8** |
| `SETU_*_ALLOWLIST` | **set** (must stay set — a preview deploy must never email a real family) | set today; **cleared on Aug 3** |
| `NEXT_PUBLIC_PORTAL_BASE_URL` | **deliberately unset** — preview URLs are per-deployment, so a fixed value would put the wrong host in preview emails | the custom domain, from Aug 3 |
| `SETU_*_REDIRECT_TO` | **unset** — so preview mail reaches the real (allowlisted) recipient | ⚠️ **SET to `setu-uat@chinmayatoronto.org`** — every outbound email/SMS is funnelled there. **MUST be removed on Aug 3** (§9.1 item 3) |

Preview is otherwise identical to Production (59 of 62 vars; the 3 above are the whole difference).

**Preview domain — assign it to the BRANCH, never to the project.** `cmt-setu-preview.vercel.app` is the intended
human-friendly preview hostname. Adding it with `vercel domains add` produces a STATIC ALIAS pinned to whatever
deployment is current — which on 2026-07-28 was a **production** build, i.e. a hostname named "preview" serving
production. Had the E2E default been pointed at it, a suite that seeds families and rewrites `app_config` would have run
against production. It was removed the same day. Add it in the dashboard with **Git Branch = `develop`**, which is the
only form that (a) serves preview and (b) follows each new push instead of going stale. Verify with `vercel alias ls`
that its source deployment matches the one behind
`cmt-setu-git-develop-chinmaya-mission-torontos-projects.vercel.app` — a 200 alone proves nothing about which build
answered.

**Preview must match Production in VALUE, not just in NAME — and you cannot read the values.**
`vercel env ls` prints `Encrypted` for every one of them, and sensitive vars pull back as empty strings, so a
name-by-name comparison looks complete while proving nothing. On 2026-07-28 a name check passed and four
`NEXT_PUBLIC_FEATURE_CHECK_IN_*` flags were still at `false` on Preview, rendering a genuinely different page.

The only way to compare is **behaviourally**, and only where a flag is observable:

```bash
# a flag-gated page 404s when its flag is off; compare the two hosts
for r in /check-in/staff-sign-in /check-in/guest; do
  for h in https://cmt-setu-preview.vercel.app https://cmt-setu.vercel.app; do
    printf '%s %s -> %s\n' "$h" "$r" "$(curl -s -o /dev/null -w '%{http_code}' "$h$r")"
  done
done
```

⚠️ **This method sees far less than it appears to.** Of the 16 flag-gated pages and 8 flag-gated APIs, exactly
**one** (`/check-in/staff-sign-in`) is reachable without a session; on every other route the auth redirect (307)
or the 401 fires *before* the flag gate, so both hosts answer identically no matter what their flags say. A clean
run of the loop above is NOT evidence of parity for anything but that one route. The rest is only settled by
running the authenticated E2E suite against Preview — which is the point of running it there.

That one observable route did catch a real defect: on 2026-07-28 `NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK` was `true`
on Production (set 2026-07-11, per the §14 entry) but `false` on Preview, so middleware redirected the kiosk to
`/check-in/staff-sign-in` and Preview answered **404** — a dead end for a sevak at the door. Corrected on Preview
the same day. Note this is the *Vercel Production env*, which is UAT-backed; the separate 715b8 kiosk flip is
still a §8 cutover decision.

### 9.1 The Aug 3 Production delta — the ONLY things that change

The cutover is a **config flip, not a data migration**: prod Setu fills lazily from the legacy RTDB roster on first engagement (§6), and **no UAT→prod copy exists or is needed**, so the `_test:true` E2E fixtures in UAT can never reach production.

1. `PORTAL_FIREBASE_PROJECT_ID` / `_CLIENT_EMAIL` / `_PRIVATE_KEY` → the **715b8** service account. Also the three `NEXT_PUBLIC_PORTAL_FIREBASE_*` client values.
2. **Clear `SETU_EMAIL_ALLOWLIST` and `SETU_PHONE_ALLOWLIST`.** ⚠️ **Empty means NO filter — clearing them is part of what turns sign-in on for real families.** They currently hold 3 emails / 2 phones, so *today a real family would never receive their OTP*. Its polarity is the opposite of what "allowlist" suggests.
3. ⚠️ **REMOVE `SETU_EMAIL_REDIRECT_TO` and `SETU_PHONE_REDIRECT_TO` from Production.** **Step 2 ALONE DOES NOTHING WITHOUT THIS.** Production currently has `SETU_EMAIL_REDIRECT_TO=setu-uat@chinmayatoronto.org`, and in `resolve-sender.ts` the redirect branch is checked FIRST and `return`s **before the allowlist is ever consulted** (`sendEmail` line 77 vs line 89). So while it is set, **every** OTP, invite and reminder goes to that one shared inbox no matter what the allowlists say — a real family would receive nothing, and clearing the allowlists would not change that by one message.
   Verified behaviourally on 2026-07-28: prod logs `EMAIL REDIRECT (test): dineshdm7@gmail.com → setu-uat@chinmayatoronto.org`, preview logs `real SES send → dineshdm7@gmail.com`. **This is also why "prod email works" is misleading** — prod mail is not reaching the addressee, it is reaching the shared testing inbox.
   Confirm afterwards from the runtime log: a post-cutover send MUST print `real SES send → <the family's own address>`, never `EMAIL REDIRECT`.
4. **Stripe → live mode. THREE settings, and they must move TOGETHER.** As of 2026-07-28 Production is entirely in TEST mode on **both** payment paths, which are switched independently:
   - **One-time donation** → `STRIPE_CHECKOUT_URL` / `STRIPE_CHECKOUT_URL_TEST`, selected by **`STRIPE_USE_TEST_CHECKOUT`**, currently **`"true"`** (`lib/stripe-config.ts:9`).
   - **Pledge** → **`STRIPE_API_BASE_URL`** directly, currently the **`…-test-…`** Cloud Run host. It does **NOT** consult `STRIPE_USE_TEST_CHECKOUT` — that switch is one-time-only, so flipping it does nothing for the pledge.
   - **`STRIPE_PLEDGE_PRICE_ID`** = `price_1TyHlzI3slorhh9mRHmBKAzi` (the LIVE price, supplied by Vaibhav and staged 2026-07-28).

   ⚠️ **A Stripe Price belongs to a MODE.** The live price above sent to the `-test-` service is rejected as "No such price". It is inert today only because `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` is `false`. **If the flag is flipped while `STRIPE_API_BASE_URL` is still the test host, every pledge fails at step 4 — AFTER the family has authorised the mandate**, i.e. a live bank authorisation with no subscription behind it (the orphan-mandate case the reconciler exists for). Move the price and the base URL in the same pass.

   ⚠️ **A Price ID carries NO mode marker.** `price_…` looks identical test or live — unlike sessions, where `cs_test_`/`cs_live_` gives it away. Nothing in this codebase can detect a wrong one, and the portal reaches Stripe only through Vaibhav's proxy, so there is no API check either. **Only the Stripe dashboard can confirm the live Price reads $51.00 CAD / month recurring**, and a wrong one debits families monthly and undetectably. Weak supporting signal available without the dashboard: Stripe IDs embed an account segment — every TEST object here carries `RNUSAfwnFq` (`price_1TxTuw…`, `sub_1TyFTY…`, `seti_1TyCz2…`) while the new price carries `I3slorhh9m`, i.e. a different account. That distinguishes "a live price" from "another test price pasted by mistake"; it says nothing about the amount.

   Also keep **`PLEDGE_MONTHLY_AMOUNT_CAD`** equal to the Price's amount — it is only what the portal DISPLAYS, so a mismatch shows $51 while the bank takes something else.

   The live one-time service **does** exist and was verified 2026-07-27 (`…-prod-…` mints `cs_live_`). Still **BLOCKED for the pledge**: `/pad/*` is not deployed on that host — Vaibhav expected to deploy 2026-07-29 (§8).
5. **`setu.chinmayatoronto.org` → this project.** ⚠️ That hostname ALREADY EXISTS and currently points at a DIFFERENT
   deployment — `cmt-setu-coming-soon-ptoueydno.vercel.app`, the holding page (confirmed via `vercel alias ls`,
   2026-07-28). So this is a RE-POINT, not a fresh add: attach it to `cmt-setu` with **Git Branch = `main`**, and expect
   the coming-soon site to stop serving at that address the moment you do. Then set `NEXT_PUBLIC_PORTAL_BASE_URL` to
   `https://setu.chinmayatoronto.org` and REDEPLOY (it is `NEXT_PUBLIC_*`, inlined at build time), or invite emails and
   the Stripe return URLs keep quoting the old host.
6. ~~Decide `NEXT_PUBLIC_FEATURE_SETU_PLEDGE`~~ **DECIDED 2026-07-28: pinned to `false` on Production**, and re-stored as **non-sensitive** so its value is readable from now on. It stays off until BOTH Vaibhav items close: `/pad/*` live on the prod Stripe host (§8) and the LIVE $51/mo Price confirmed. To turn it on later you must also **redeploy** (item 7).
7. **Redeploy.** `NEXT_PUBLIC_*` is inlined at BUILD time; an env change alone does nothing.

> **Vercel CLI ≥ 58 is required.** 54.6.0 refuses every preview-scoped `env add` with `action_required: git_branch_required`, even when given its own suggested command verbatim, and it reports sensitive values as empty strings rather than saying so. 58 writes preview fine and prints `[SENSITIVE]` explicitly.

> ⚠️ **SENSITIVE VARS CANNOT BE READ BACK — BY ANYONE.** `SETU_EMAIL_REDIRECT_TO`, `SETU_PHONE_REDIRECT_TO` and (until 2026-07-28) `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` are stored as Vercel **Sensitive** environment variables. `vercel env pull` returns the literal string `[SENSITIVE]`, there is no reveal flag, and the dashboard cannot show them either — they are write-only by design.
> **Consequence for items 2-3:** you can never *confirm by reading* that the redirect vars are gone or that an allowlist is empty. The only proof is behavioural — remove them, send one real OTP, and read the runtime log for `real SES send → <the family's own address>`. Do not treat a clean-looking `env ls` as verification.
> **When pinning a value you will later need to audit, add it with `--no-sensitive`** so it stays readable. A feature flag is not a secret; `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` was re-stored that way on 2026-07-28 for exactly this reason.

**Firebase (server SA):** `PORTAL_FIREBASE_PROJECT_ID`, `PORTAL_FIREBASE_CLIENT_EMAIL`, `PORTAL_FIREBASE_PRIVATE_KEY`, `MASTER_FIREBASE_PROJECT_ID`, `MASTER_FIREBASE_CLIENT_EMAIL`, `MASTER_FIREBASE_PRIVATE_KEY`, `MASTER_FIREBASE_DATABASE_URL`
**Firebase (public client):** `NEXT_PUBLIC_PORTAL_FIREBASE_PROJECT_ID`, `NEXT_PUBLIC_PORTAL_FIREBASE_API_KEY`, `NEXT_PUBLIC_PORTAL_FIREBASE_AUTH_DOMAIN`
**Auth/session:** `TEACHER_PASSPHRASE`, `SESSION_COOKIE_EXPIRES_DAYS` (≤14)
**AWS:** `AWS_SES_REGION`, `AWS_SNS_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_SES_FROM_EMAIL`
**Stripe:** `STRIPE_API_KEY`, `STRIPE_CHECKOUT_URL`, `STRIPE_CHECKOUT_URL_TEST`, `STRIPE_USE_TEST_CHECKOUT` (=`false` in prod), `WEBHOOK_API_KEY`
**Stripe PAD / monthly pledge (ALL OPTIONAL — irrelevant while `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` is unset):** `STRIPE_API_BASE_URL` (the Cloud Run host; the existing `STRIPE_CHECKOUT_URL` is that base + `/checkout-link` and is unchanged), `STRIPE_PLEDGE_PRICE_ID`, `PLEDGE_MONTHLY_AMOUNT_CAD` (default 51). The client **fails closed** at call time if the base URL or price id is missing, so leaving them unset cannot half-enable anything. ⚠️ `PLEDGE_MONTHLY_AMOUNT_CAD` is only what the portal **displays**; what is actually debited lives on the Stripe Price behind `STRIPE_PLEDGE_PRICE_ID`. They must be set together and must agree — see §8.
**Setu allowlists / misc:** `SETU_EMAIL_ALLOWLIST`, `SETU_PHONE_ALLOWLIST` (split per channel — clear/empty to allow all in prod, or keep gated during soft-launch), `EVENT_REGISTRATION_RATE_LIMIT_PER_MIN`, `CRON_SECRET`, `NEXT_PUBLIC_PORTAL_BASE_URL`
**SES-managed templates (ALL OPTIONAL — leave UNSET at launch):** `SES_TEMPLATE_PAYMENT_REMINDER`, `SES_TEMPLATE_DONATION_THANK_YOU`, `SES_TEMPLATE_SETU_INVITE`, `SES_TEMPLATE_SETU_JOIN_REQUEST`, `SES_TEMPLATE_PLEDGE_ACTIVATED`, `SES_CONFIGURATION_SET`. Unset means the portal renders that email in code, which is exactly today's behaviour — so setting none of these is the safe launch state. Set one only after a real UAT send of that template has been read by a human: a template that exists but fails to render is accepted by SES and delivered to **nobody**, silently. Templates and configuration sets are region-scoped — create them in `AWS_SES_REGION` (`ca-central-1`) or they behave as if absent. Full variable contract: `docs/runbooks/ses-email-templates.md`.
**Feature flags:** the `NEXT_PUBLIC_FEATURE_*` from §8, plus **`NEXT_PUBLIC_FEATURE_SMS_OTP` — leave UNSET.** SNS is still sandboxed with no Canadian origination number, so SMS sign-in is refused with a typed 400 at every surface. Flip it on only after AWS clears the sandbox AND an origination number is registered; it restores SMS sign-in everywhere at once.

**Vercel CLI gotchas:**
```bash
# NEXT_PUBLIC_* are sensitive-by-default on Production → add with --no-sensitive or they read back blank.
vercel env add NEXT_PUBLIC_FEATURE_SETU_TEACHER production --value "true" --force --yes --no-sensitive
vercel env pull .env.vercel.check   # verify values are present (not masked)
```
Prefer `git push` (empty commit) to trigger a rebuild that picks up changed `NEXT_PUBLIC_*`. **Do NOT** run `vercel deploy --prod` from a stale/wrong-cwd link (creates an orphan project). Vercel project is **cmt-setu** (`prj_hU9MepeAVmjdfCcsao3tWduvdrXC`).

---

## 10. CLI command reference (every script + purpose)

> All are `pnpm --filter @cmt/portal <alias>` which expands to `tsx --env-file=.env.local scripts/<file>.ts`. For PROD, append `--allow-prod` and ensure `.env.local` targets the prod project. ⚠️ = writes data.

| Alias | Purpose | Prod note |
|---|---|---|
| `check:door-access` | Verify MASTER SA can read 715b8 door collections (read-only probe). | Run first — confirms read bridge. |
| `check:migrations` | Audit which migrations have run (counts per collection). | Read-only sanity check. |
| `list:uat-families` | List migrated families (id/name/location). | Read-only; works for prod too. |
| `seed:donation-periods` ⚠️ | Seed legacy `donationPeriods` (pricing tiers per location/year). | `--allow-prod`. Idempotent. |
| `migrate:programs` ⚠️ | Copy `donationPeriods` → `offerings` (the model the app reads). | `--allow-prod`. |
| `seed:bala-vihar-levels` ⚠️ | Seed `levels` (grade bands, curriculum) per location/year — LOCATIONS defs = CMT's current (2026-27) sheet, the single source of truth. `--year YYYY-YY` (default 2025-26), `--location Brampton\|Scarborough`, `--delete-ids a,b`, `--reset-teachers` (default: **preserves** existing `teacherRefs`), **`--verify`** (read-only DB-vs-defs reconciliation; exit 0 = match; safe on prod w/o `--allow-prod`). | `--allow-prod` to write. `--dry-run`/`--verify` first. Idempotent. |
| `seed:bala-vihar-calendar` ⚠️ | Seed `classCalendarEntries` + `weeklySchedules` (Sunday class/no-class days) per year/location — `ENTRIES_BY_YEAR` (2025-26, 2026-27) = the committed CMT sheet transcription. `--year YYYY-YY` (default 2026-27), `--location Brampton\|Scarborough\|all` (default Brampton), `--admin-mtg` (opt-in "Teacher's Admin Meeting" specialEvent, default omitted), `--dry-run`. Weekly schedule transcribed for Brampton only. | `--allow-prod` to write. `--dry-run` first. Idempotent. |
| `migrate:legacy-families` ⚠️ | RTDB `/roster` (715b8) → Setu families/members/contactKeys. | `--dry-run` first. ~864 families. |
| `renumber:public-ids` ⚠️ | Re-number EVERY family publicFid into a uniform band starting at **5001** (above all real legacy ids), so publicFids are collision-free with legacy ids AND visually distinct. Skips any legacy id (belt-and-suspenders). Advances the `familyPublicId` counter past the band. Idempotent (fid-ordered fixpoint; re-run finds 0 to change). Member ids (publicMid, 50001+) untouched. | `--dry-run` default (preview); `--apply` to write; **refuses non-UAT unless `--allow-prod`.** |
| `migrate:calendar-ids` ⚠️ | One-off: normalize calendar entry doc IDs. | Only if needed. |
| `backfill:legacy-sid` ⚠️ | Add `legacySid` to members (links to roster rows). | After family migration. |
| `backfill:bv-enrollments` ⚠️ | Enroll currently-registered BV kids into the **live** school year's offering (`pid:oid`), reading `getSchoolYearConfig().currentYear` (no longer hardcoded 2025-26). **Skips** families that already have an active current-year enrollment (protects rollover-promoted grades), **cancels** any stale prior-year BV enrollment for the families it enrolls, and prints an enrolled-but-gradeless report. Idempotent. | `--dry-run` first. Run after the launch year is activated + its offerings seeded (see §6 step 8). |
| `backfill:portal-access` ⚠️ | Gate roster-migrated non-manager adults: set `members.portalAccess:'pending'` on non-primary **adults** of **migrated** (`legacyFid`) families. Registration-added members, personas, managers, children skipped. Idempotent. | `--dry-run` first. UAT-default (hard-refuses non-UAT without `--allow-prod`); for 715b8 run after family migration — behavior change, locks those members out until manager approval. |
| `migrate:public-ids` ⚠️ | Issue #4 — assign 4-digit `publicFid` to every family + 5-digit `publicMid` to every member, from `counters/familyPublicId`/`counters/memberPublicId`. Idempotent (skips already-stamped docs); oldest-first → 1001 / 50001. `--dry-run` / `--limit N` / `--fid X` / `--csv-out path`. | UAT-default (refuses non-UAT without `--allow-prod`); for 715b8 run after family migration. Deploy the `members.publicMid` index (§5) first. UAT run 2026-06-25: 877 families / 2668 members. |
| `school-year:start` ⚠️ | Clone levels+offerings to next year (rollover Step 1). | `--dry-run` first. §7. |
| `school-year:promote` ⚠️ | Advance grades + re-level + close/create enrollments (Step 2). | `--dry-run`, fix needs-grade, then commit. §7. |
| `grant:admin` ⚠️ | Grant admin role (custom claims) to a contact. | Needs re-login to take effect. |
| `grant:welcome-team` ⚠️ | Grant welcome-team role to a contact. | Needs re-login. |
| `seed:admin` ⚠️ | Seed an initial admin. | First admin bootstrap. |
| `seed:e2e-family` ⚠️ | Seed a `_test:true` E2E fixture family. Plain run forces the **Registered** BV ground state (active 2026-27 enrollment, prior-year cancelled, `_test` donations for that eid deleted); `-- --confirm-bv` additionally writes one `_test` completed donation for the 2026-27 BV eid (flips the fixture to **Enrolled** — used by `e2e/setu/enrollment-state.spec.ts` Phase 2); `-- --disclaimers <accepted\|pending>` sets the fixture's `families.disclaimersAccepted` (default `accepted` — un-gated once `NEXT_PUBLIC_FEATURE_SETU_DISCLAIMERS` is on; slice 2). Also provisions (unconditionally, additively) a **dedicated `_test` teacher-attendance fixture** — an isolated BV level `e2e-att-level` (bespoke `pid=e2e-att-period`, hidden from the live-year `/admin/levels` list) + a separate `_test` family `CMT-E2E-ATT` with 2 grade-matched children enrolled under that pid — so `e2e/setu/teacher/attendance-binary.spec.ts` has a deterministic 2-row roster without touching the shared family (slice 3). Each run also clears any `attendanceEvents` for `e2e-att-level` so the ground state is "unmarked". | **UAT/test only — never prod.** |
| `seed:test-accounts` ⚠️ | Seed the 8 role-persona test accounts (parents both locations, family-member, 2 location teachers, universal teacher, sevak, admin) for manual testing + role E2E. See `docs/runbooks/test-accounts.md`. | **UAT only — hard-refuses prod (no `--allow-prod`).** Needs `TEST_ACCOUNTS_PASSWORD`. |
| `seed:kiosk-account` ⚠️ | Seed the single generic **kiosk** account (email/password) carrying the least-privilege `kiosk` role that authorizes the door-tablet check-in endpoint. Create-or-update the password Auth user + write the `kiosk` capability via `addCapability`. Idempotent. | **UAT only - hard-refuses prod (no `--allow-prod`).** Needs `KIOSK_ACCOUNT_EMAIL` + `KIOSK_ACCOUNT_PASSWORD` in `.env.local` (min 8 chars, letter+digit). |
| `attendance:report` | Print an attendance report. | Read-only. |
| `refresh:check-ins` ⚠️ | Refresh the portal's door check-in cache. | Per cron/runbook. |
| `wipe:uat-leaks` / `wipe:test-leaks` ⚠️ | Delete `_test:true` leaked fixtures. | **UAT/test only — NEVER prod.** |
| `debug-sns-config.ts` | Dump SNS account state (sandbox/origination/spend/opt-out) per region. | Read-only diagnosis. |
| `debug-phone-lookup.ts` / `inspect-setu-family.ts` / `inspect-legacy-roster.ts` | Read-only inspection helpers. | Safe anywhere. |
| `inspect:brampton-level` | Read-only: reproduce `deriveRoster` for Brampton BV 2025-26 — per-level projected roster sizes + enrolled-grade histogram. Use to validate the teacher view after a backfill/rollover. | UAT-only (refuses other projects). |
| `snapshot:rtdb` | Capture legacy 715b8 RTDB `/roster` + `/families` into gitignored `apps/portal/.rtdb-snapshot/` (one deliberate full download), then set `RTDB_SNAPSHOT_DIR=.rtdb-snapshot` in `.env.local` so ALL local reads resolve from the snapshot — zero RTDB download cost. | Read-only. Re-run to refresh; PII — never commit. |
| `backfill:birth-months` ⚠️ | Populate `members.birthMonth` (1–12) from legacy roster `dob_m` via `legacySid` (snapshot-fed, idempotent). Feeds the prasad assigner. | `--dry-run` first; `--allow-prod` at cutover, after family migration. |
| `strip:eager-publicfids` ⚠️ | Clear the eager pre-lazy-model `families.publicFid` from families with no active enrollment, so `/welcome/roster` shows a new Family ID only for families that have actually enrolled (matching the lazy `publicFid` model). Default scope = no active enrollment (any year); `--never-enrolled-only` narrows to families with no enrollment doc at all. Always skips `_test:true` fixtures. Idempotent. | `--dry-run` default; `--apply` to write; **refuses non-UAT unless `--allow-prod`** — and prod is already lazy, so a prod run should never be needed. UAT-only cleanup of the eager renumber residue. |
| `clear:promotion-carryforwards` ⚠️ | Delete the old rollover's auto carry-forward enrollments (`enrolledVia: 'promotion'`, `status: 'active'`) — the "Registered" inflation. Preserves the 4 on `_test` E2E fixtures; leaves families/members/real enrollments untouched. Index-free (unfiltered `collectionGroup('enrollments')` read, in-memory filter). Idempotent (a re-run finds none). | **Dry-run default; `--commit` to delete. Refuses unless UAT.** UAT-only — the new rollover (§14 2026-07-20) no longer creates these, and prod never had them. |
| `normalize:legacy-grades` ⚠️ | Repair stale non-canonical `members.schoolGrade` (blobs like `"Pre L1 (Gr JK-SK)"`, `"Grade 4"`, `"J"`) left by pre-fix `legacy-parser` migration runs — they surfaced as junk chips in the `/welcome/roster` Grade filter. Re-derives the correct grade authoritatively from the legacy `/roster` snapshot (by `legacySid`, or by matching the stale blob to the row's `level` when the old run lost `legacySid` — only when all same-blob kids agree), else folds `"Grade 4"→"4"` via `normalizeGrade`; genuinely ambiguous rows left untouched + reported. Snapshot-fed (never live RTDB). Idempotent. | `--dry-run` default; `--apply` to write; **refuses non-UAT unless `--allow-prod`.** Prod materializes families lazily via the current parser (never produces the blobs), so a prod run is only needed if a deliberate bulk `migrate-legacy-families` was ever run there. |
| `backfill:guest-session-date` | Stamp `sessionDate` (the Sunday of the walk-in week) onto existing `guest_check_ins` docs, derived from their `date`. `date` is never modified - it is the only record of the day the guest actually walked in. Docs with no/unparseable `date` are skipped and reported, never guessed. Index-free (one unfiltered collection read, batched writes). | `--dry-run` **always first**; `--limit N` for a bounded pass; `--recompute` overwrites an existing `sessionDate` (the recovery path - a plain re-run is skip-if-present and will NOT repair a bad value); **refuses non-UAT unless `--allow-prod`**. |
| `seed:adult-class-fixtures` ⚠️ | Seed/reset the UAT E2E fixtures for the Adult Study Class gate: the `adult-study-class` **program** doc, one enabled **offering** (`adult-study-class-brampton-2026-27`, $101), and **six `_test` families — one per row of spec §2.3** (1, 2, 3, 5, 6, 7), each with its own email so the 5-per-15-min sign-in limiter (keyed on the normalized contact) is not shared. BV rows get an active enrollment **and** the completed donation condition 3 requires. Deletes any prior adult-class enrollment so the gate can fire again on a re-run. | **Refuses unless UAT.** Test fixture only — never prod. Prod gets its program + offering from the admin UI. |
| `seed:centre-confirmation-family` | Seed/reset the UAT E2E fixture for the unknown-centre prompt: a family COMPLETE in every other respect (3 members + home address) with only `locationNeedsConfirmation:true` outstanding — the exact shape that used to loop between `/complete-profile` and `/family`. Idempotent; re-asserts the flag so the spec is repeatable. | **Refuses unless UAT.** Test fixture only — never prod. |
| `sns-set-defaults.ts` ⚠️ | Set SNS SMS default attributes. | AWS account change. |

**Non-pnpm commands:**
| Command | Purpose |
|---|---|
| `firebase deploy --only firestore:indexes --project chinmaya-setu-715b8` | Deploy portal indexes to PROD — **NEVER `--force`** (§5). |
| `firebase deploy --only firestore:indexes --project chinmaya-setu-uat` | Deploy to UAT (`--force` safe). |
| `firebase firestore:indexes --project <id>` | List current indexes on a project. |
| `git push` | Triggers Vercel build + the pre-push gate (typecheck/lint/test/build). |
| `vercel env add <NAME> production --value "…" --no-sensitive --force --yes` | Set a prod env var (see §9 gotchas). |

---

## 11. Verification (mock-free — do not skip)

After migration + index deploy + flag flip, walk the **actual user paths** in prod (green tests ≠ shipped working):
- [ ] Sign-in: open `/sign-in`, OTP to a real allowlisted contact, land on `/family`.
- [ ] Family dashboard shows the migrated family + BV enrollment + attendance (test the N=2 case: a family with two kids).
- [ ] Teacher: assign a teacher (re-login), open `/teacher`, a level shows the correct roster count (compare to the standalone check-in app's count for that level).
- [ ] Donation: run a real (small) Stripe live checkout end-to-end; confirm the webhook records the donation.
- [ ] Welcome-team search returns a known family.
- [ ] Rollover (when run): preview matches expectations; commit; re-open a promoted child → new grade/level + journey strip; teacher roster for the new year shows promoted kids; re-run preview → ~0.
- [ ] Confirm the **standalone kiosk app still works** in prod (its collections + indexes untouched).

---

## 12. Rollback / safety

- **Feature flags** are the fastest rollback: set `NEXT_PUBLIC_FEATURE_SETU_* = false` and redeploy → the Setu surfaces disappear; legacy `/login` + `/check-in/*` remain. Data stays intact.
- **Data**: migrations are idempotent and additive; they don't delete door-app data. There is no destructive migration. If a portal collection is wrong, fix-forward with a corrected re-run (deterministic IDs overwrite).
- **Indexes**: if a wrong index was added, remove it from `firestore.indexes.json` and redeploy **without `--force`** (this won't touch the standalone's indexes). Never `--force`.
- **Auth**: bad role grants are reversible (`grant:*` writes custom claims; revoke by re-running with the inverse, or via the admin welcome-team UI).

---

## 13. Quick "first prod cutover" sequence (copy/paste skeleton)

```bash
# 0. Confirm §1; set .env.local to prod (PORTAL=715b8 or dedicated; MASTER=715b8).
pnpm --filter @cmt/portal check:door-access                      # read bridge OK?
firebase deploy --only firestore:indexes --project chinmaya-setu-715b8   # NO --force
# 1. scaffolding
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-donation-periods.ts --allow-prod
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/migrate-to-programs.ts --allow-prod
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-bala-vihar-levels.ts --allow-prod
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/seed-bala-vihar-calendar.ts --allow-prod
# 2. families
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/migrate-legacy-families.ts --dry-run --limit 20 --allow-prod
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/migrate-legacy-families.ts --allow-prod
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/backfill-legacy-sid.ts --allow-prod
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/backfill-bv-enrollments.ts --dry-run --limit 30 --allow-prod
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/backfill-bv-enrollments.ts --allow-prod
# 3. admins
pnpm --filter @cmt/portal exec tsx --env-file=.env.local scripts/grant-admin.ts <you> --allow-prod
# 4. set Vercel prod env vars (§9), flip flags (§8), redeploy, verify (§11).
```

---

## 14. Change log (UAT) — keep this current

> **Rule:** any important UAT DB change (new collection/field, new index + deploy, new ops/migration/seed/backfill script or run, schema change, new env var/flag, corrective write) gets a dated entry here **and** an update to the relevant section above, in the same change. Prod replays this log additively (never `--force`, never touch the door-app collections).

- **2026-07-28h** - 🔴 **A monthly PAD mandate could be started by a family with nothing to fund - and one that had was left with no way to enroll.** Commits `f85a1d2`, `7735bf2`. Reported by Vaibhav with screenshots; **both states existed in UAT before anyone went looking.** **NO index / collection / schema change. ONE corrective UAT write - see below.**
  **What was reachable.** `MonthlyDonationOption`'s button starts a mandate and does **not** enrol, and nothing on the enroll page or at the route asked whether there was a Bala Vihar contribution to spread. So `/family/enroll/bala-vihar` showed **"Add a child to enroll" directly above "Give $51 monthly"**, and `/api/pledges/start` checked only manager + email + family-exists. `DonationChoice` had it right all along by enrolling FIRST; the bare card beside it did not.
  **The second half, and the worse one.** A family with a `started` pledge and no enrollment got the pending reassurance card, which carries no CTA - and the page stands its sticky footer down whenever that component renders. **The screen offered no way to enroll at all.** They were committing $51/month to a program they could not join.
  **Fixed at the chokepoint.** `POST /api/pledges/start` now returns **409 `enrollment-required`** without an active Bala Vihar enrollment. In the route rather than per screen because three surfaces reach it and one always gets missed - the shape of the `pledge-covers-enrollment` hole in **2026-07-28f**. Once a mandate exists the portal cannot undo it: there is no cancel endpoint, and `cancelPledgeRecord` is bookkeeping only. The enroll page gates the standalone card on `alreadyEnrolled` (the gate `/family/donate` has always had and this page never applied), and `DonationChoice` keeps an **enroll-only** control in the `pending`/`giving` states - suppressing PAYMENT while a mandate confirms is the double-charge fix and stays; suppressing free enrollment was collateral.
  **Also (`7735bf2`), found by Codex review on `/donate/success`:** `resolvePledgeSlot` gated on `?pledge=` being non-empty - **any** value - and `finalizePledge` deliberately no-ops on a pid it cannot match, so a manager with no pledge got a live "Give $63 monthly" button under *"Thank you for setting up a monthly gift"*. `pledge-card.tsx` opens by forbidding exactly that. The slot now requires a pledge that EXISTS and is `started`/`active`, and `canStart={false}` matches both `/family` call sites. **Not an exotic URL:** a real return lands on that address, so it is in the family's history.
  **✍️ CORRECTIVE UAT WRITE, 2026-07-29T00:31Z.** Two stranded pledges cancelled at CMT Developer's instruction, via `cancelPledgeRecord` (so each carries a `pledge.cancel` audit row, actor `script:cancel-stranded-pledges-2026-07-28`): `CMT-Z8UXTJIO` (family9@gmail.com, 1 child, 0 enrollments) pid `rmds5sWM0OwRcBrC6d7K`, and `CMT-2VZFPAPC` (Family10, **0 children**) pid `q8dcSkvZnvPRyDb9R3g9`. Both `started` → `cancelled`, verified by re-read. **Bookkeeping only - it stops no money.** Both were TEST-mode mandates against the `-test-` host so there was none to stop; the Stripe objects still exist there. **PROD-CUTOVER TODO: none** (UAT-only data), but note the precedent - a real stranded mandate would need a **manual Stripe cancellation** on top of this.
  **Why the suite was green throughout.** Three `/donate/success` tests rendered `?pledge=PLG-7` against `getFamilyPledge -> null`, a state that **cannot occur** (`startPledge` writes the doc before minting the hosted page). They were asserting the bug with an impossible fixture. Reseeded to what a real return produces - **rule 7: the fixture has to look like a real account.** 3605 unit tests, none skipped.
  **Open follow-up:** `started || active` is now written inline in **three** places (checkout route, `/family/donate`, `resolvePledgeSlot`) while the helper beside them, `isPledgeGiving`, is `active`-ONLY - which is how a `started` mandate sailed past the donate page for weeks. Extracting `isPledgeInPlay` was deliberately deferred out of launch week: it touches the double-charge guard and fixes nothing today.

- **2026-07-28g** - **LIVE pledge Price staged on Production; adult-class gate ignored pledging families; donation-choice extended + cosmetics.** **NO DB / index / collection / migration change.**
  **Env change (Production only).** `STRIPE_PLEDGE_PRICE_ID` = **`price_1TyHlzI3slorhh9mRHmBKAzi`** (the LIVE price from Vaibhav), re-stored **non-sensitive** so it stays auditable. Verified by pulling it back. **Inert today** - `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` is `false`, and `STRIPE_API_BASE_URL` is still the `-test-` host, so nothing can use it. Vaibhav deploys `/pad/*` to the prod host **2026-07-29**; the price and the base URL must then move in the SAME pass - see §9.1 item 4 for why a live price against a test host fails AFTER the family authorises the mandate.
  **Verification available without the Stripe dashboard, and its limit.** Stripe IDs embed an account segment: every TEST object here carries `RNUSAfwnFq` (`price_1TxTuw…`, `sub_1TyFTY…`, `seti_1TyCz2…`) and the new price carries `I3slorhh9m`. That distinguishes "a live price" from "another test price pasted by mistake" - the realistic error - but says **nothing about the amount**. $51.00 CAD/month recurring still needs Vaibhav's eyes on the dashboard.
  **Discovered while checking:** Production is in TEST mode on **both** payment paths, and they are switched independently - `STRIPE_USE_TEST_CHECKOUT="true"` governs only the ONE-TIME path; the pledge reads `STRIPE_API_BASE_URL` directly and ignores that switch entirely. §9.1 item 4 rewritten accordingly.
  **🔴 Code fix - the adult class never asked a family who paid by PLEDGE, and charged them.** Reported by CMT Developer. `isBalaViharPaid` recognised a donation record, a legacy row or a teacher-managed offering; a pledge is none of those, so condition 3 never passed. The same predicate decides the adult-class **fee waiver**, so a pledging family was also billed for a class that should be free to them. Leg (d) added, keyed on `active` (matching `isPledgeGiving()` and therefore every other payment surface). **Accepted consequence:** a pledging family is asked once the mandate clears and the `/family` gate catches them, not on the success page. `MUST_CONSIDER_PLEDGE` in `pledge-isolation.test.ts` exists to catch exactly this shape and missed it because the list is hand-enumerated - `needs-selection.ts` is on it now, `load-gate-data.ts` on the read allowlist. Making `hasActivePledge` **required** rather than optional-with-default is what surfaced the two fee-deciding call sites.
  **Also:** the donation choice now covers families who have **not enrolled yet** (it enrols them itself; CTA reads "Enroll and continue"), and per Vaibhav "one-time payment" → "one-time donation" with the RECOMMENDED badge removed.

- **2026-07-28f** - **"Choose your donation" radio group on the enroll page, and a 🔴 DOUBLE-CHARGE hole closed server-side.** Commits `f12eea3`, `440535b`, `fa9105a`. Design by Vaibhav; copy is his, as approved by CMT Developer. **NO DB / index / collection / migration change.**
  **The feature.** The enroll page rendered "Continue to donation" AND a separate "Give $51 monthly" card - two primary buttons for one decision. Replaced by one radio group with one CTA (`donation-choice.tsx`), covering three states the design does not draw: non-manager (cannot commit the family to a debit), pledge pending (no payment control at all), already giving (shows the plan, never a second ask). Scoped to the already-enrolled state; the standalone card stays for not-yet-enrolled families so the monthly plan does not become unreachable again.
  **🔴 The hole, found by a Codex review in a file the feature never touched.** `/family/donate?eid=` renders the FULL one-time payment form and had **no pledge awareness at all** - it read the pledge only to set `alreadyPledging` = `isPledgeGiving()`, which is **`active` alone**. A family whose mandate was still confirming (`started`) saw the complete form and could pay **$500 on top of $51/month**, and the portal can reverse neither. Not a stale-bookmark case: the route is returned as `donateUrl` by the enrollments API and is the fallback from `EnrollCta`, `CompleteDonationButton` and the new component's own $0 branch.
  **Fixed at the layer that cannot be routed around.** `POST /api/setu/donations/checkout` now returns **409 `pledge-covers-enrollment`** for a Bala Vihar enrollment charge while a pledge is `started` **or** `active`. Three SCREENS suppress this control (dashboard, enroll page, donate page) and one was missed for weeks - per-surface client suppression decays the moment someone adds a fourth. Scoped so nobody is stranded: a GENERAL gift stays open (a pledging family may give extra), non-BV enrollments are untouched, and `failed`/`cancelled` reopen the one-time route. The donate page gained the matching UI state so a family reads an explanation rather than meeting a 409. The checkout route was added to the `pledge-isolation` allowlist - the only entry there that REFUSES money rather than reporting it received.
  **Verified:** 3560 unit tests (none skipped), plus deployed-preview screenshots at 390px and 1440px - 0 horizontal overflow, CTA reachable on the phone.
  **⚠️ A bug the unit tests structurally could not catch.** The page keeps BOTH layout trees in the DOM, so every radio existed twice; a shared group `name` let the desktop instance steal `checked`, and the **phone's radio rendered EMPTY** beside a card still tinted as selected. Fifteen tests passed because each renders ONE instance. Fixed with `useId()` (`440535b`) and pinned by three N=2 tests. **Screenshot the real page; a component test cannot see a duplicated tree.**
  **Open follow-ups (UX only, no money at risk):** the not-yet-enrolled state still shows two competing CTAs; no `role="radiogroup"` semantics; a viewport resize across the `md` breakpoint silently resets the selection to "Full donation".

- **2026-07-28e** - 🔴 **NO PLEDGE COULD EVER HAVE ACTIVATED: the portal read `status`, the payment service sends `state`.** Fixed in `7d2e118`; **verified end to end on UAT, the first time this flow has ever completed.**
  **The defect.** `getCheckoutSessionResult` (step 3) read `data['status']`. The service answers `{"state":"success","reason":"PAD mandate setup completed","stripe":{"status":"complete","setup_intent_status":"succeeded"}}`. `toResult` maps anything unrecognised to `pending` - the correct safe direction, since guessing `success` would mark a family as giving with no confirmed subscription - so the mismatch was **silent, not loud**: step 3 reported `pending` for a mandate Stripe had already marked `succeeded`, `advancePledge` never proceeded to step 4, and the daily cron would have re-polled that pledge forever. It presented for hours as a sandbox ACSS mandate that would not settle, and was escalated to Vaibhav as such. **Nothing was ever wrong with the sandbox or with Flinks.**
  **How it hid.** The spec's contract table wrote steps 3 and 5 as returning a bare `success | failed | pending` and **never named the field carrying it**, so the client guessed. Every unit test then mocked `{status: …}` - our own invention - so 172 green tests asserted the guess back at us. Vaibhav's own sequence diagram said *"if **state** is `success`"* all along. **A contract table must name the field, not just the vocabulary.**
  **The fix.** `toResult` takes the whole body and reads `body['state'] ?? body['status']`, tolerant of both because the two endpoints were never proven to agree with each other and a second wrong guess costs another silent stall. **Top level only** - `stripe.status` is the Checkout *Session's* lifecycle and reads `complete` even for a FAILED mandate, so reading it would turn a failure into a success; a test pins that. New tests use the live payload verbatim. Spec contract table corrected.
  **UAT data change (test fixtures only).** Drove `POST /api/cron/reconcile-pledges` manually against deployed preview: `{scanned:2, activated:1, processing:1}`. Pledge `Qir0cgYfF2QiPEuAQhCZ` (E2E family `CMT-2V836RQ0`) went `started` → **`active`**, minting TEST subscription `sub_1TyFTYRNUSAfwnFqmDXx6VCz`, `activatedAt` set, `needsStripeVerification` unset. A pledge-activation email fired (redirected to `setu-uat@` on preview). **Side benefit: that one run VERIFIED steps 4 and 5 live** - `subscriptionId` really is that field's name, and step 5's outcome field parses. Both had been guesses. **NO schema / index / collection / migration change.**
  **Still true after this:** `/pad/*` 404s on the PROD Stripe host, so this is all TEST-mode only, and `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` stays `false` in production (§14 2026-07-28d).
  ⚠️ **The bigger remaining risk is NOT this bug.** Once a pledge is `active` the portal never checks it again - `reconcile-pledges.ts:78-81` scans only `status=='started'`, `advancePledge` returns early on `active`, and there is **no inbound webhook route anywhere in the repo**. The provider contract's step 8 says his backend treats `invoice.paid` / `invoice.payment_failed` as final truth - a channel we have no access to. **Concrete consequence: if a family's 3rd monthly collection fails (NSF, revoked mandate, closed account), Stripe knows, Vaibhav's backend knows, and the portal goes on showing that family as giving - and as having satisfied their Bala Vihar enrollment donation - forever.** Latent while the flag is off. This is spec open item O9. Independently confirmed by a Codex adversarial review of `7d2e118`, which rated it above the bug just fixed.

- **2026-07-28d** - **Production pledge flag PINNED off; discovered that the launch-critical env vars cannot be read back at all.**
  **Env change (Production only):** `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` re-added as **`false`, non-sensitive**, `vercel env add … --value false --no-sensitive --force`. Verified by pulling it back: `NEXT_PUBLIC_FEATURE_SETU_PLEDGE="false"`. This closes §9.1 item 6, which had stood as an undecided owner question. **NO code change, NO DB change, NO index, NO migration.**
  **Why pin rather than inspect:** `NEXT_PUBLIC_FEATURE_SETU_PLEDGE`, `NEXT_PUBLIC_FEATURE_SETU_ADULT_CLASS`, `SETU_EMAIL_REDIRECT_TO` and `SETU_PHONE_REDIRECT_TO` were all stored as Vercel **Sensitive** vars, which are **write-only — `vercel env pull` yields the literal `[SENSITIVE]`, there is no reveal flag, and the dashboard cannot show them either.** Their values were therefore unknowable, including by the owner. Overwriting is the only way to reach a known state. See the new warning block under §9.1.
  **Consequence for the Aug 3 mail switch (§9.1 items 2-3): removal of the redirect vars can never be confirmed by reading them.** The only proof is behavioural - remove, send one real OTP, and read the runtime log for `real SES send → <the family's own address>`. A clean-looking `env ls` is not verification.
  **⚠️ The prior value was almost certainly `true`.** The §14 entry of 2026-07-27 records `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` as **ON in production**, proven behaviourally at the time (`/api/pledges/start` → 503 = flag on, vs 404 = flag off). **That could not be re-confirmed on 2026-07-28** — both prod and preview now answer that probe with **401**, because the middleware auth gate fires before the flag check, so the discriminator only works with a session. Treat the 2026-07-27 record as the last known state.
  **Why that matters for this merge:** the 9 commits merged the same day are precisely the ones that make the pledge *reachable* (enroll page + dashboard CTA). Merging them onto a production build with the flag ON would have exposed real families to a flow whose `/pad/*` endpoints **404 on the prod Stripe host** (BLOCKER 1 below). The pin removes that coupling.
  **Sequencing note:** `NEXT_PUBLIC_*` is inlined at BUILD time, so the pin was done **before** merging `develop` → `main`; the merge's production rebuild is what bakes it in. Pinning after the merge would have required a second rebuild. **The pin has no effect until that build completes** — confirm afterwards with a signed-in probe of `/api/pledges/start` expecting **404**.
  **`NEXT_PUBLIC_FEATURE_SETU_ADULT_CLASS` on Production remains sensitive and therefore unknown** - deliberately left alone (owner decision 2026-07-28), but it is the last launch-visible flag in an unverifiable state. Pin it the same way before launch.
  **Doc fix, same pass:** `docs/superpowers/plans/2026-07-25-EXECUTION-ORDER.md` (the "start here" doc) instructed readers to run the E2E suite with `PLAYWRIGHT_BASE_URL=https://cmt-setu.vercel.app` and claimed a bare `pnpm test:e2e` hits localhost. **Both halves were wrong and the first is dangerous**: that host is Production, the suite is not read-only (it seeds families, enrols children, starts pledges and rewrites `app_config`), and from the cutover it serves real families. Corrected to bare `pnpm test:e2e` → Preview, per `e2e/_helpers.ts:25`.

- **2026-07-28c** - **SENT TO VAIBHAV: the 6 open payment-service items (2 blockers, 2 questions, 2 FYIs).** Asked by CMT Developer 2026-07-28. Record here so the asks survive the conversation; tick them off as he answers.
  **🔴 BLOCKER 1 - `/pad/*` is not deployed on the PROD host.** Re-verified 2026-07-28 16:5x UTC: on `…-prod-…` all four of `/pad/setup-link`, `/pad/monthly-subscription`, `/checkout-session-result`, `/subscription-result` return **404**, while `/checkout-link` returns 401 (exists, wants the key). On `…-test-…` all five return 401. Reproduce with a POST of `{}` to each. Until these stop 404ing, `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` cannot be turned on in production. **One-time donations are NOT blocked** - that path is verified live (`cs_live_`).
  **🔴 BLOCKER 2 - confirm the LIVE $51/mo Price.** The debited amount lives on the Stripe Price, not in this repo, so nothing here can detect a wrong one (`pledge-amount.ts` says so explicitly). Need the live Price id for `STRIPE_PLEDGE_PRICE_ID` plus dashboard confirmation that it is **$51.00 CAD / month**.
  **🟠 QUESTION 3 (NEW, time-sensitive) - can a sandbox ACSS mandate be forced to succeed?** Ours has sat at `pending` for hours: session `cs_test_c1XZqEumEB2z89VHmJQSoITnjfrxgYaRgy6nbHeIt8ejDJFHsMXKeKkNYH`, customer `cus_Uy9HdjKIEIR0v9`, pledge `Qir0cgYfF2QiPEuAQhCZ` (fid `CMT-2V836RQ0`). The portal polls correctly - `lastCheckedAt` advances on every reconciler run and `lastError` stays empty - so this is provider-side. **If sandbox mandates never settle, the activation path (advance-pledge steps 4-5) and the "Paid" dashboard state cannot be observed end-to-end before launch; they would ship unit-tested only.** That limits what can honestly be claimed as tested on Aug 3.
  **🟠 QUESTION 4 - does `/pad/monthly-subscription` dedupe on the `idempotencyKey` BODY field?** A custom field on the proxy, not a native Stripe `Idempotency-Key` header, so nothing in this repo can test it. If ignored, two racing step-4 calls each mint a REAL subscription; the portal stores one `subscriptionId`, so the duplicate is permanently invisible - a family debited twice monthly with nothing here showing it. Still the ONE place correctness rests entirely on an external contract (also logged as (H) in the 2026-07-27 entry).
  **ℹ️ FYI 5 - there is no cancel endpoint**, so the portal cannot stop a debit; `cancelPledgeRecord` is bookkeeping only and says so in its own docstring. Stopping a monthly gift is a MANUAL cancellation in Stripe by the temple. Asked who owns that and how quickly, and whether a cancel endpoint is possible later - its absence is a live operational trap the moment families can pledge.
  **ℹ️ FYI 6 - there is no webhook**, so after the first payment the portal cannot see whether monthly debits are collected; a family who cancels at their bank keeps reading as paid. Accepted for launch (see the 2026-07-27 entry); flagged in case webhooks become feasible.
  **No code, DB, index, env or flag change from this entry** - it is a record of open external dependencies.
- **2026-07-28b** - 🔴 **LAUNCH BLOCKER FOUND: production redirects ALL outbound email to one inbox, and it overrides the allowlist.**
  Reported as "preview sign-in sends no code, prod works". Both claims were true and both were misleading. Runtime logs settle it: production takes the `EMAIL REDIRECT (test): <family> -> setu-uat@chinmayatoronto.org` branch, preview takes `real SES send -> <family>`. **Prod email was never reaching the addressee** - it reaches the shared testing inbox, which is where it was being read. Preview was working correctly all along and delivering to the real address.
  **Why this is a blocker, not a curiosity:** in `resolve-sender.ts` the redirect is checked FIRST and `return`s before the allowlist (`sendEmail` line 77 vs 89). While `SETU_EMAIL_REDIRECT_TO` is set, **clearing the allowlists accomplishes nothing** - every OTP, invite and reminder still goes to `setu-uat@`. The runbook previously called the allowlists "the single switch that opens the portal to the public"; that was wrong, and both §9.1 and the §13 summary are corrected. Removing the two `SETU_*_REDIRECT_TO` vars is now §9.1 item 3.
  **SES itself is healthy** (checked via SDK, since none of this appears in logs): production access, sending enabled, 50k/day quota, `chinmayatoronto.org` + `bvregistration@` DKIM `Success`. So "no email arrived" was never an SES fault.
  **Env change (Preview only):** set `SETU_EMAIL_REDIRECT_TO=setu-uat@chinmayatoronto.org` at CMT Developer's request, so preview now matches production and all preview mail lands in the shared inbox. Preview still has **no** `SETU_PHONE_REDIRECT_TO` (production does) - preview SMS therefore still goes to real allowlisted phones, which the phone allowlist alone is holding back. **NO code change, NO DB change, NO index, NO migration.**
  **Prod-cutover TODO: remove BOTH `SETU_EMAIL_REDIRECT_TO` and `SETU_PHONE_REDIRECT_TO` from Production on Aug 3, then confirm from the runtime log that a send prints `real SES send -> <the family's own address>` and NOT `EMAIL REDIRECT`.**
- **2026-07-28** - **E2E sweep against the `develop` preview. No product defect found; four operational facts worth carrying to prod.**
  **DB effect (UAT only, all transient):** cancelled 3 leftover `pledges` documents on the E2E family `CMT-FSWEDU2X` through the real `POST /api/admin/pledges/[pid]/cancel` (test residue that was blocking the pledge spec - the spec now cleans up after itself). Read-only inspections of `pledges` / `donations` / `check_in_events` / `enrollments` for that one family. **NO new collection, field, index, migration, script or schema change.**
  **(A) ⚠️ TEACHER ASSIGNMENTS DO NOT SURVIVE A SCHOOL-YEAR ROLLOVER.** `POST /api/admin/school-year/copy-teachers` is a deliberate, admin-triggered step. UAT is on 2026-27 with that step never run, so both teacher personas return an EMPTY level list - operationally, **every teacher opens an empty roster and no child appears anywhere**. This is by design, but it is the kind of design that ruins a first Sunday. **Prod-cutover TODO: after any rollover, run copy-teachers before teachers next log in, and spot-check one teacher's roster.**
  **(B) `NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK` was `false` on Preview** while Production has had it `true` since 2026-07-11. Middleware still redirected `/check-in` to `/check-in/staff-sign-in`, which 404'd - a dead end at the door. Fixed on Preview; see the parity note in §9.0 for why name-parity checks cannot catch this class of thing.
  **(C) The pledge start path is VERIFIED WORKING on the test payment host.** `POST /api/pledges/start` returns 201 with a real `cs_test_` Stripe URL, and the pledge spec now runs 6/6 - its first ever green run. This does NOT relax the §8 gate: `/pad/*` is still absent from the PROD host, and the live $51 Price is still unconfirmed.
  **(D) Open question, deliberately left failing:** `kiosk-auto-enroll` expects a family's `publicFid` to exist BEFORE their first enrollment, but publicFids mint lazily AT first enrollment and that fixture must have no active enrollment. What the door should show a never-enrolled family is a UX decision; the assertion was annotated rather than relaxed so the decision is not buried.
- **2026-07-27** - ⚠️ **`NEXT_PUBLIC_FEATURE_SETU_PLEDGE` IS NO LONGER A COSMETIC FLAG** (`a7b208a`). Vaibhav established that the monthly plan is not a separate "support the mission" ask but the **second way to pay the Bala Vihar enrollment donation** - $500 once, or $51/month continuing until manually stopped. Approved by CMT Developer 2026-07-27 in the "mark pledge = satisfied" shape.
  **What flipping the flag ON now does:** a family with an `active` pledge reads as **donation complete / 100% / Enrolled** on their dashboard with **zero** completed one-time donations, gets no donation nudge, and counts as **paid** on the teacher roster, the welcome roster chip + engagement column, and the enrollment report. Before this change the flag only revealed a card. **It now changes who the portal says has paid.**
  **P5's "a pledge gates NOTHING" invariant is deliberately reversed**, and `pledge-isolation.test.ts` became an allowlist of the five files permitted to read pledge state - anything new still fails the build. The "no bank detail ever crosses this boundary" half is unchanged.
  **🔴 Accounting caveat, unresolved by design:** with no Stripe webhook the portal cannot observe that any monthly payment **after the first** was collected, so a family who cancels at their bank keeps reading as satisfied and **the enrollment report over-counts monthly givers**. Do not treat the report as a collections figure while any pledge is live. Revisit when invoice polling exists.
  **No schema, collection, index or env-var change.** `pledges` stays a top-level collection; the new staff read is one `where('status','==','active')` single-field query (no composite index). `PledgeDoc` gains no `eid` - the plan is continuous, so an active pledge satisfies the CURRENT year's enrollment across a rollover.
  **Prod-cutover TODO:** the §9.1 pledge-flag decision is now materially bigger than it was. Leaving the flag ON at launch means the amount debited is whatever the Stripe Price says - and with `STRIPE_USE_TEST_CHECKOUT=true` that is a TEST price, so families would be marked as paid against money that never moves.

- **2026-07-27** - ✅ **VERCEL PREVIEW AND PRODUCTION ARE NOW SEPARATE ENVIRONMENTS** (CMT Developer: *"let's separate vercel preview and production env, on August 3rd we will use vercel prod env for actual production where we will point our custom domain to it"*). See the new §9.0 / §9.1.
  **What was actually true before this:** both environments pointed at `chinmaya-setu-uat`, so "production" was UAT with a nicer URL - and **Preview held only 36 of Production's 62 variables**, missing every `NEXT_PUBLIC_FEATURE_*` flag and all Stripe config, so preview deploys silently ran with the whole Setu feature set OFF. Preview is now 59/62; the 3 remaining differences are deliberate and listed in §9.0.
  **Preview also still had the DEAD Stripe host** in `STRIPE_API_BASE_URL` - the earlier fix that day reached Production only, because the CLI silently refused the preview write (below). Corrected.
  **⚠️ Vercel CLI 54.6.0 cannot write preview-scoped env vars at all** - it returns `action_required: git_branch_required` even when given its own suggested "all Preview branches" command verbatim, and it reports sensitive values as empty strings instead of saying they are hidden. Upgraded to 58.0.0, which does both correctly (`[SENSITIVE]`). **Anything below CLI 58 will appear to work and change nothing.**
  **The Aug 3 flip is a CONFIG change, not a data migration** - §6's lazy model means prod Setu fills from the legacy RTDB roster on first engagement, and no UAT→prod copy exists, so UAT's `_test:true` fixtures cannot reach prod. The exact delta is §9.1. **The item most likely to be missed: the outbound-mail gate, which is TWO settings, not one.** (a) `SETU_EMAIL_ALLOWLIST` / `SETU_PHONE_ALLOWLIST` hold 3 emails / 2 phones (empty = no filter), and (b) **`SETU_EMAIL_REDIRECT_TO` is set to `setu-uat@chinmayatoronto.org`, which overrides the allowlist entirely** - the redirect branch returns before the allowlist is read. Clearing the allowlists WITHOUT removing the redirect changes nothing: every OTP still lands in the shared testing inbox and **no family receives anything**. Both must be done. See §9.1 items 2-3.

- **2026-07-27** - ✅ **ADULT-CLASS: a teaching adult is now SHOWN, greyed out and labelled, instead of hidden** (`6bc2640`). Reverses the presentation half of §6.6 at CMT Developer's request after a real two-adult family (one a teacher) saw a single name and read it as their family record being wrong. **The selection rule is unchanged** - `selectableAdults()` is still the sole authority, the write route still rejects a teaching mid with `mid-not-selectable`, the checkbox is `disabled` so it is neither checkable nor submitted, and an all-teaching household still yields an empty selectable set so the gate still never fires. A unit test asserts `selectableAdults` and the new `teachingAdults` **partition** the eligible adults (disjoint + covering), so nobody can be shown twice or silently vanish again. The E2E's checkbox helper now scopes to `:not([disabled])` - without that the greyed row inflates every "how many adults may this family pick" count in the file. Spec §2.3 / §4.4 / §6 updated in the same commit.

- **2026-07-27** - 🔴 **THE LIVE-MODE STRIPE CHECKOUT SERVICE DOES NOT EXIST, AND PRODUCTION IS ISSUING `cs_test_` SESSIONS.** Found while diagnosing the pledge's 503.
  **What was measured, not assumed.** `POST /api/setu/donations/checkout` on `cmt-setu.vercel.app` returns **200** with a real `https://checkout.stripe.com/c/pay/**cs_test_**…` URL — so one-time donations WORK, but in **Stripe test mode** (`STRIPE_USE_TEST_CHECKOUT=true` in prod). Meanwhile the live-mode host named in `STRIPE_CHECKOUT_URL` returns Google's front-end 404 on **every** path including `GET /`; only the test host is alive, and it serves all of `/checkout-link`, `/pad/setup-link`, `/pad/monthly-subscription`, `/checkout-session-result`, `/subscription-result` (400 on an empty body = route exists). **Consequence: the go-live step "set `STRIPE_USE_TEST_CHECKOUT=false`" would break donations rather than arm them.** See the ⛔ note in §8. **Owner action (Vaibhav): deploy or name a live-mode service URL.**
  **`STRIPE_API_BASE_URL` set** on Production + Preview to the verified test host `https://cmt-stripe-checkout-api-test-195790402763.northamerica-northeast1.run.app` (no trailing slash, no `/checkout-link`), and deliberately stored **`--no-sensitive`** so it can be read back — see the pull caveat below. The pledge 503 was this var pointing at the dead host.
  **⚠️ `vercel env pull` SILENTLY RETURNS EMPTY STRINGS for sensitive vars.** The pull showed `STRIPE_API_BASE_URL=""`, `NEXT_PUBLIC_FEATURE_SETU_PLEDGE=""` and `NEXT_PUBLIC_FEATURE_SETU_DONATIONS=""` — all three are in fact set. Proven behaviourally: the donations route 404s when its flag is off and returned 200, and `/api/pledges/start` returns 503 (flag ON) rather than 404 (flag off), on a build deployed AFTER those vars were written. **Never conclude "unset" from a pulled blank; probe the running app.** This supersedes the "verify via env pull" half of the `vercel env add --value` note.
  **Also recorded:** `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` is currently **ON in production**, which `lib/flags.ts` says it must not be at launch (a family through that flow authorises a mandate against a TEST price). Either flip it off before Aug 3 or move the price to live — it is a decision, not an oversight, and it is not yet made.
  **Method note:** the dead host was caught only because the probe included a **known-good control** (`/checkout-link`, used by the live donation flow). Without it the 404 read as "the vendor never deployed `/pad/*`", which was wrong and was nearly reported as such. Always probe a third party with a path you know works.
  **A SECOND cause was hiding behind the first, and it was ours.** With the base URL fixed the pledge error moved `404` → `400`, and replaying the portal's exact payload against the service returned `{"error":"Invalid branding_settings.display_name"}`. `createPadSetupLink` sent `branding_settings: {}` while the one-time donation route had always sent `{ display_name: 'Chinmaya Mission Toronto' }` - so **every pledge would have failed before the family ever reached Stripe**, flag on or off. Fixed to send `ORG_NAME`, with a test. The reason it took a live probe to find: `post()` threw `"/pad/setup-link failed with 400"` and **discarded the response body**, so the pledge doc's `lastError` recorded a status and nothing else while the service had named the exact field. `PadClientError` now carries the provider's own text (truncated to 300 chars). Vaibhav's `/pad/*` was live and correct throughout - all four endpoints plus both result endpoints answer on the test host.

- **2026-07-27** - ✅ **ADULT-CLASS E2E: RUN FOR THE FIRST TIME, DIAGNOSED, AND NOW 12/12 GREEN.** The spec had been authored-but-unrun since 2026-07-26. On its first execution against the deployed app it failed at row 1's first assertion; **both causes turned out to be FIXTURE bugs. The adult-class gate itself was correct throughout and no product code was changed.**
  **Cause 1 - the fixtures had never accepted the disclaimers.** `AdultClassGate` defers to `earlierGatesPending`, which is `profileGatePending` OR the disclaimer read. With `NEXT_PUBLIC_FEATURE_SETU_DISCLAIMERS` now ON and `app_config/disclaimers` at version 16, the six fixture families had `disclaimersAccepted: null`, so `DisclaimerGate` redirected them to `/acknowledgements` and the adult-class gate **correctly** never fired. The seed's own comment shows the author reasoned about the PROFILE half of that deferral and stopped there. `seed:adult-class-fixtures` now records acceptance, reading the version and school year **live** (a pinned version would rot the fixture the next time an admin publishes new content and re-gates everyone).
  **Cause 2 - the spec could not type into a disabled input.** The fixture seeds `NO_ALLERGIES`, which ticks "No known allergies" and DISABLES the food-allergies field; the spec called `fill` on it and retried until timeout. It now unticks the box first when it finds the field disabled.
  **Why this was expensive to find, and the lesson:** the gate is deliberately FAIL-SOFT, so a deferral is indistinguishable from a fault - no error, no redirect, no log. **And under PPR a gate `redirect()` streams AFTER the shell, so `curl` sees HTTP 200 plus dashboard HTML even when the browser is being redirected.** That made an early manual probe read as "the gate is dead" when the family was in fact being sent to `/acknowledgements`. Trust Playwright's final URL over a curl body for anything gate-related.
  **A runbook correction:** the earlier claim that the fixtures were "verified against the real `needsAdultClassSelection` predicate - 6/6 correct" was weaker than it read. That verification script hardcoded `isManager: true` and `currentOffering`, and never exercised the gate-deferral chain at all - which is precisely where the bug was.
  **Prod-cutover impact: none, and the flag is safe to leave on.** No schema, index, migration or product-code change. `NEXT_PUBLIC_FEATURE_SETU_ADULT_CLASS` is verified working end to end, including the §6.3 case (an unrelated member edit leaving a partial selection untouched) that the spec singles out as the one unit tests and a single-pass walkthrough both miss.
- **2026-07-27** - **P5 monthly pledge Tasks 5-6: the family card, and the reconciler that makes the feature correct.** Commits `8dd6407` (card) and this one (reconciler). Ships **DARK** - `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` is unset, so none of this is reachable in prod.
  **(A) NO new Firestore index, and that is a decision, not an omission.** The P5 plan's Task 6 Step 5 (a stale report over pledges stuck in `started`) reads as a `status ==` plus a `startedAt` range, which WOULD need a `pledges(status, startedAt)` composite - and `firestore.indexes.json` has zero `pledges` entries. It is computed **in memory from the reconciler's own scan** instead: every row the report names is a row the run already loaded, so the second query would buy nothing but an index to deploy and a `FAILED_PRECONDITION` to discover in prod. `started` is a transient state resolved within a day, so the scan is small by construction. The family card's read (`getFamilyPledge`) is likewise a bare `where('fid','==')`, and `startPledge`'s duplicate check filters status in memory for the same reason. **Every pledge query in the feature is a single-field equality. Prod-cutover TODO: no index deploy from this entry.**
  **(B) NEW CRON `/api/cron/reconcile-pledges`** (daily 15:00 UTC, in `vercel.ts`, an hour after the prasad sweep so the two do not contend). Guarded exactly like the existing crons: timing-safe `CRON_SECRET` bearer, `GET` **and** `POST` exported (an earlier cron silently 405'd every scheduled run by exporting POST only), and listed in `PUBLIC_ROUTES` - a cron route missing from that list is 401'd by middleware before its own secret check runs, and the job never fires. That invariant used to be a comment; it is now a test (`src/__tests__/cron-routes-are-public.test.ts` asserts every path in `vercel.ts` `crons` is public). Also gated on `flags.setuPledge`: dark returns `{success:true, disabled:true}` and calls no provider, since `/pad/*` is Stripe TEST mode. **Needs no new env var** - `CRON_SECRET` already exists.
  **(C) What the cron exists for.** Step 4 (create the subscription) is a SECOND server call after the family returns from the Stripe-hosted mandate page. If the browser dies in between, the mandate is live at Stripe, no subscription exists, no money moves, and the family believes they are giving monthly. Nothing else notices. The reconciler retries step 4 with the SAME `setupSessionId` and the same derived idempotency key. Errors are isolated per row: the error is recorded on that row, its status is left alone (a bad afternoon at Stripe must not mark a queue of good pledges dead), and the run continues.
  **(D) Activation goes through ONE transactional claim.** `activatePledgeAndNotify` is the only writer of `status:'active'`, and only the caller that wins the claim emails the family - the returning browser and the cron both see the same `started → active` window, and a plain read-then-write in two places would double-mail a family about their money. The steps 3→4→5 state machine was likewise extracted to a single `advance-pledge.ts` shared by both callers; two copies of a state machine that decides whether money is moving would eventually disagree.
  **(E) Task 7 - admin cancel is BOOKKEEPING ONLY, and the screen says so.** New `POST /api/admin/pledges/[pid]/cancel` (admin-only; **no new `canAccessRoute` clause** - the existing `/api/admin/` admin-only catch-all covers it, and the handler re-checks `isAdmin` helper-based so extraRoles count) plus a new admin screen `/admin/pledges`, tiled under **Reports** and hidden while the flag is dark. 🔴 **The temple stops the actual debit MANUALLY in Stripe** (Vaibhav 2026-07-26) - there is no cancel endpoint on the payment service and the portal cannot stop a debit. `cancel-pledge.ts` does not even import the PAD client; the absence is the guarantee, and a test asserts no provider call. The warning copy is a named export asserted **through the accessibility tree**, in the banner AND repeated in the confirmation toast: left implicit, staff click Cancel, believe the money stopped, and the family keeps being charged. New **`audit_log` action `pledge.cancel`**, written in the SAME transaction as the status change (carrying `actorExtraRoles`, and the prior status in `before`). `failed` is refused rather than relabelled - it is the provider's verdict, `cancelled` is the temple's decision, and overwriting one destroys the record of which happened. `/admin/pledges` reads with `orderBy('startedAt','desc').limit(200)` - single-field, auto-indexed both directions, **still no composite index** - and joins family names with one `getAll`, never a per-family fan-out.
  **(F) Task 8 - VERIFICATION STATUS, stated plainly.**
  ✅ **Verified against deployed UAT (mock-free):** `GET /api/cron/reconcile-pledges` with the real `CRON_SECRET` returns **200 `{success:true, disabled:true, scanned:0, ...}`**. That single call proves four things: the route deployed; it IS in `PUBLIC_ROUTES` (a path NOT in the list gets 401 from middleware before the handler - confirmed by contrast, `/api/cron/no-such-cron` with the same valid bearer returns 401); the ship-dark gate holds (no provider call); and **GET** works, which is how Vercel Cron actually triggers. Without a bearer: 401. `POST /api/pledges/start` unauthenticated: 401.
  ✅ **Verified by standing tests, not a one-time grep** (`features/setu/pledges/__tests__/pledge-isolation.test.ts`, each guard mutation-checked to confirm it can fail): the `pledges` collection is read ONLY inside `features/setu/pledges/`; no enrollment/roster/teacher/donations/attendance/reports/check-in module references pledge state; no bank-detail identifier appears in pledge **code** (comments stripped first, since two files describe the absence in prose); no CSV builder mentions a pledge.
  ⛔ **NOT verified, and blocked on owner action - the pledge has never been run end to end against a real payment service.** The E2E exists at `e2e/setu/pledge.spec.ts` and **has never been executed**; it collects under Playwright and nothing more. It needs, in order: `NEXT_PUBLIC_FEATURE_SETU_PLEDGE=true` on **UAT only** plus a rebuild (the flag is `NEXT_PUBLIC_*`, statically inlined - an env-only change does nothing); `STRIPE_API_BASE_URL` + `STRIPE_PLEDGE_PRICE_ID` set on UAT; and `/pad/*` confirmed live on the payment service. Authorising the mandate is a MANUAL step inside the spec - driving Stripe's hosted form would couple the suite to a third party's DOM. **Also unverified: that the Stripe TEST price is actually $51** - that needs the Stripe dashboard, and nothing in this codebase can detect a wrong one.
  **(G) POST-REVIEW FIX - a compare-and-swap hole on the FAILURE transitions.** An independent review of the committed diff found that the plan's design guarded the ACTIVATION transition (`claimPledgeActivation`) and left all three `status:'failed'` writes as bare `ref.update` calls, with `advancePledge` returning a hardcoded `'active'` regardless of whether the claim won. Two callers race by design here (the returning browser and the cron), so a late `failed` answer could overwrite a committed activation. **That is not a display bug:** Stripe keeps debiting monthly, the family's card shows the ask, and because `failed` deliberately does NOT block a new pledge (`start-pledge.ts` LIVE_STATUSES - so one bad attempt cannot lock a family out) the family is then offered a **SECOND monthly mandate on the same bank account**. Fixed by generalizing the claim into `claimPledgeTransition(db, pid, to, extra)`, which settles BOTH terminal statuses and returns `{won, status}` so callers report what is true rather than what they guessed. Reproduced deterministically before fixing; 10 mutations on the fix, all killed. **No schema, index, env or flag change.**
  **(G2) SECOND POST-REVIEW FIX - step 4 is now guarded by a fresh read, not the caller's snapshot.** (G) made every status WRITE compare-and-swapped, but a claim cannot un-charge anybody. `advancePledge`'s early-return guards read the CALLER'S SNAPSHOT, taken before a step-3 provider round trip of possibly hundreds of ms - so an admin cancel landing in that window sailed straight past them and **created a REAL recurring bank debit for a pledge the temple had just cancelled**, which the portal would then correctly show as `cancelled` while Stripe billed the family monthly. Step 4 now re-reads the document and bails unless the pledge is still `started`. This narrows the window to a single Firestore read; it **cannot close it** without a distributed lock, which is why (H) below matters. The `subscriptionId` write immediately after remains **deliberately unguarded** - if a subscription did get created against a no-longer-`started` pledge, recording its id is the only thing that makes that live debit findable by a human, and guarding it would make an untracked recurring charge invisible.
  **(G3) THIRD POST-REVIEW FIX - the orphan is now SELF-FLAGGING and actually visible.** (G2) narrowed the step-4 window but could not close it, and the reviewer then checked whether the surviving `subscriptionId` was findable by a human. **It was not** - `list-pledges-for-admin.ts` deliberately does not project the provider handles, the stale report only scans `status == 'started'`, and the reconciler stops selecting a row the moment it leaves `started`. So the justification for keeping that write ("it is the only thing that makes the debit findable") was aspirational. Now: the transaction that records the handle also sets **`needsStripeVerification: true`** plus an explanatory `lastError` when the pledge is no longer `started`; the reconciler reports those ids (`unverified`, logged and counted in the cron response); and `/admin/pledges` shows a **"Verify in Stripe"** chip on the row. **Deviation from the reviewer's suggestion, deliberately:** it proposed querying `status IN ('cancelled','failed') AND subscriptionId != null`. That is the NORMAL steady state - `cancel-pledge.ts` does not clear the handle and a step-5 failure keeps it - so it would report almost every settled pledge, and a monitor that always fires is one nobody reads. Keying on the flag reports only the genuinely anomalous event. New field on `PledgeDocSchema` (bare `.optional()`, no default). Query is a **single-field equality - still no composite index**, and it is fail-soft so monitoring can never cost the reconciliation.
  **(G4) FOURTH POST-REVIEW FIX - the flag now compares SUBSCRIPTION IDs, not status, and gained coverage doing it.** (G3)'s flag fired whenever the status had left `started`, which over-fired on the ordinary CONVERGENT race: cron and browser both pass the step-4 guard while the pledge really is `started`, both call step 4, **the idempotency key holds and hands both the SAME id**, and whichever lands second finds a pledge the other already carried to `active`. Nothing is wrong - one subscription, one email - yet it raised "Verify in Stripe" on a healthy pledge, under exactly the overlap this feature is designed around. A monitor firing on healthy rows is the trap the flag exists to avoid. It now compares the id being written against the one already stored, which is **narrower AND wider**: the false alarm goes, and it catches a case status could never see - **two DIFFERENT ids for one pledge, i.e. the provider's idempotency did not hold and the family has two live debits**, which can happen while the status is still `started`. In that case the FIRST id is kept (it may already be activated and tracked) and both ids are carried in `lastError`, so neither pointer to a live debit is lost. **This is the first in-portal detection of the (H) risk below** - it does not prevent it, but it is no longer invisible. Also: a subscription created when the document has vanished now logs loudly rather than silently, since with no row the log is the only evidence the debit exists.
  **(G5) FIFTH POST-REVIEW CHANGE - clarity, not correctness: `verifiedSubscriptionId`.** In the divergent-id case (G4), the document keeps the FIRST subscription id while the one this caller actually confirmed live may be the second - so `subscriptionId`, which is singular and reads as "the" subscription, can name one nobody verified. Both ids were already in `lastError`, so nothing was lost; the risk was that **a human summoned by our own verification flag could cancel the wrong subscription in Stripe while the other kept billing the family.** The activation claim now also records `verifiedSubscriptionId` - the one step 5 confirmed. Normally identical to `subscriptionId`; read them together before cancelling anything. **⚠️ WHOEVER INVESTIGATES A "Verify in Stripe" ROW: read `subscriptionId`, `verifiedSubscriptionId` AND `lastError` together - the flag means two subscriptions may exist and only one of them is named in the primary field.** New optional schema field; no index, no migration, no behaviour change on the ordinary path.
  **(H) ⚠️ ADDED TO THE PRE-FLIP CHECKLIST, owner Vaibhav: confirm the payment service deduplicates on the `idempotencyKey` BODY FIELD** of `/pad/monthly-subscription`. It is a custom field on CMT's Cloud Run proxy, **not** a native Stripe `Idempotency-Key` header, and nothing in this repo can test it. If the proxy ignores it, two racing step-4 calls each mint a REAL subscription and - since the portal stores one `subscriptionId`, last write wins - the duplicate is permanently invisible to the reconciler. A family debited twice a month with nothing in the portal showing it. This is the ONE place correctness rests entirely on an external contract.
  **(I) Prod-cutover TODO: none beyond deploying the code, while the flag stays unset.** §8 now carries the pledge flag row and §9 the three optional env vars. Before that flag is EVER flipped, work the checklist at the foot of the P5 v3 plan - the first family through afterwards signs the first real bank mandate, and **only the Stripe dashboard can confirm the LIVE Price is $51.**
- **2026-07-26** - **P2 Task 9: the guest→teacher and `/welcome/visitors` deployed-UAT E2E.** Two new specs, `e2e/setu/teacher/guest-to-teacher.spec.ts` and `e2e/setu/admin/welcome-visitors.spec.ts`. 7/7 green against `https://cmt-setu.vercel.app`.
  **DB effect: transient only.** Between them they create 6 `guest_check_ins` docs (4 through the real `POST /api/check-in/guests`, 2 written directly so they can carry a deliberately midweek `date` the server clock would never produce) and **delete every one in `afterAll`**, by the ids they captured. Verified empty afterwards. **NO new collection, NO new field, NO index, NO migration, NO env var, NO flag.** Nothing else is written: the specs never click **Confirm** on a door guest, which would create a pending family + auto-enrollment. `guest_check_ins` is a **portal-owned** collection (§3) - the door app's own guest store is `guest-families`, which these specs only ever read.
  **Prod-cutover TODO: none from this entry.** But two standing facts it pinned are worth carrying:
  (a) both specs depend on `NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK=true` on the target deploy (it gates the page *and* the route); it is currently **true on UAT** and is a §8 flip decision for prod.
  (b) `readPortalGuestChildren` still queries **both** `sessionDate ==` and the transitional `date ==` leg. When that second leg is dropped after the prod backfill (see the 2026-07-25 guest date-key entry below), re-run `--project=setu guest-to-teacher`: its midweek test is the one that proves the `sessionDate` leg alone is sufficient.
- **2026-07-26** - **P4 Adult Study Class: roster payment gains a fourth state, plus the UAT E2E fixtures.** Commits `8ac2256`, `bdea8ef` (payment) and this one (E2E).
  **(A) `RosterPayment` is now four states, not three** (`packages/shared-domain/src/setu/roster.ts`). `paymentFromAmounts` returned `'unknown'` for any zero expected total, collapsing three different situations into one; Task 10's adult-class waiver makes the middle one routine. Owner decision 2026-07-26: **`paid` means money actually arrived**, so a family who never owed anything is never labelled paid. New **`'not-applicable'`** (chip "N/A") = no fee applies (free program or waiver); **`'unknown'`** now means specifically *we cannot tell* — the offering doc is missing so nothing ever priced the enrollment, or the offering is **off-portal** (`teacher-managed` cash, or `legacy` whose payment lives in the prod RTDB roster). Guarded as `!== 'portal'` so a fourth `PAYMENT_SOURCES` entry inherits the cautious verdict. `paymentFromAmounts` was **deleted** rather than widened — taking a pre-summed total is what made the two kinds of zero indistinguishable, and all four callers had their own copy of the price-resolution fallback; `classifyRosterPayment` now takes the raw pieces. **NO schema-doc change, NO new index, NO migration, NO DB op** — `RosterPersonCsvRowSchema.payment` is a bare `z.string()` and the verdict is derived at read time. **Prod-cutover TODO: none beyond deploying the code.** Visible change at deploy: `/welcome/roster`'s Payment filter gains an **N/A** option and the summary strip an N/A tally. Measured against UAT before shipping — **898 families, 7 would move, all E2E fixtures, zero real families** (every real Bala Vihar offering carries tiers, so none reaches a $0 total). The default roster view (Paid + Enrolled) is **unchanged**, since free/waived families read N/A, not Paid.
  **(B) Two of the four callers were already dead code** — `deriveFamilyPayment` and `deriveFamilyRosterSignals` have no production caller (their module `list-families.ts` no longer exists). Both were kept and rewired to the shared classifier rather than deleted; **deleting them is an open follow-up.**
  **(C) New UAT fixture seed `seed:adult-class-fixtures`** (§10) — the `adult-study-class` program + a $101 Brampton offering + **six `_test` families, one per §2.3 matrix row**. Run against UAT 2026-07-26: program and offering created, families `CMT-PJZXDPPN` (row 1), `CMT-O0PZLXF8` (2), `CMT-QYGNV23Y` (3), `CMT-U3AI9ROI` (5), `CMT-TO7QCQVR` (6), `CMT-TJKC2G2X` (7). Each fixture was then **verified against the real `needsAdultClassSelection` predicate reading real UAT data — 6/6 correct**, including the isolation §2.3 demands: row 3 has a paid Bala Vihar and 0 selectable adults, row 6 has 2 selectable adults and no Bala Vihar, so row 7's two independent failures are each proven separately. **Prod-cutover TODO: none — these are `_test:true` fixtures. Prod gets its program + offering from the admin UI**, and `NEXT_PUBLIC_FEATURE_SETU_ADULT_CLASS` must not be flipped before both exist (§8).
  **(C2) ⚠️ FLIPPING THE FLAG DIVERTS THE SHARED E2E FAMILY TOO.** The gate runs on every `/family/*` render, so any fixture family meeting its five conditions bounces to `/adult-class` in specs that have nothing to do with this feature. Verified in UAT 2026-07-26: the shared `CMT-FSWEDU2X` is Brampton, one non-teaching adult, one child, active Bala Vihar - and has **zero completed donations**, which is the ONLY reason condition 3 fails and it stays un-gated today. `e2e/setu/enrollment-state.spec.ts` Phase 2 re-seeds it with `-- --confirm-bv`, writing exactly that donation; combined with the Brampton adult-class offering seeded above, it then satisfies all five. Correct product behaviour, but it presents as unrelated dashboard specs failing on a URL mismatch. Before flipping the flag: give the shared family an adult-class enrollment (satisfies condition 4), or re-run `seed:e2e-family` without `--confirm-bv` after any run that used it.
  **(D) The E2E itself is OWNER-GATED and has NOT been run** (`e2e/setu/adult-class.spec.ts`), exactly as `kiosk-auto-enroll.spec.ts` is: it needs `NEXT_PUBLIC_FEATURE_SETU_ADULT_CLASS=true` in the **UAT** Vercel env plus a redeploy (the flag is `NEXT_PUBLIC_*`, so it is statically inlined and an env-only change does nothing). Index audit done and **no new index is needed** — the offerings queries are served by the existing `(programKey, enabled, startDate)` and `(enabled, location, programKey, startDate)`, and the enrollment read by the existing `enrollments (programKey, status)` collection group.
- **2026-07-26** - **P5 monthly pledge RESCOPED: the portal will collect NO bank details. NO code, schema, collection, index, env var or flag change - a spec/plan revision only.** Vaibhav, 2026-07-26: *"For pledge, let's NOT collect bank details"* … *"I am updating stripe endpoints to now accept one-time credit card (current) and adding monthly PAD"* … *"so in our setu app, we won't store anything PCI - all done directly in Stripe."* This landed **before any pledge code was written** (verified: zero pledge implementation in the repo), so it cost a documentation revision and nothing else. **DELETED from the design:** the `pledge_secrets` collection, AES-256-GCM + `PLEDGE_ENCRYPTION_KEY` (and its key-custody blocker), the accounting hand-off (named recipient / encrypted channel / retention limit - which the plan called *the highest residual risk in the feature*), bank-field shape validation, the 90-day purge sweep, and the export/decrypt/transmit script. **Two of the three unassigned launch blockers close with them.** **REPLACED BY:** the family is redirected to a **Stripe-hosted** page to authorise the PAD, exactly as the one-time Bala Vihar donation already works - the portal never talks to Stripe directly, it forwards to CMT's Cloud Run proxy (`STRIPE_CHECKOUT_URL`, `x-api-key`, shared with `chinmaya-event-registration`) and receives a hosted URL. Same architecture, different mode; **no new payment plumbing.** The portal stores **status only** (`started` | `active` | `cancelled` + an opaque `providerRef`). **⚠️ NEW BLOCKER (spec O9), owner Vaibhav:** with no Stripe webhook in the portal today (`mark-donation-status.ts:11` states settlement is client-trusted), it is unresolved how the portal learns a PAD mandate was **actually** established. Self-attestation is tolerable for a one-time gift and NOT for a recurring mandate, where the portal would assert an ongoing debit arrangement that may not exist. Preferred fix is a status/verify endpoint on Vaibhav's service - cheapest to add while he is editing those endpoints. **Prod-cutover TODO: none yet** - P5 remains fully decoupled (it gates nothing) and is now BLOCKED on the integration contract rather than on crypto. **Env vars unchanged:** `SES_TEMPLATE_PLEDGE_ACTIVATED` in §9 is still correct and still optional; `PLEDGE_ENCRYPTION_KEY` was never added to §9 and now never will be. Docs revised: the spec in place (single file, no stale copy), the P5 v2 plan with a per-task fate banner on all 13 tasks, `EXECUTION-ORDER.md` Phase 3, the launch INDEX, and both P5 review artifacts.
- **2026-07-26** - **P6 shipped: dormant-family migration skip + the unknown-centre prompt (both cutover-blocking).** Spec §1.9a-c. Commits `cd2ca2c`, `25b8431`, `05ef885`, `634d158`, `f775828`, `f65e8c7`.
  **(A) Dormant skip.** `migrate-legacy-families.ts` now skips families with **no centre and no level on any roster row**. Verified by dry-run against the real snapshot: **867 examined → 299 skipped, 568 migrated**; all 867 land in `--csv-out` (the skips as `status=dormant`). This keeps **190 grade-mappable children with years-stale grades** out of Brampton teachers' "Registered · not enrolled" lists on launch Sunday. They are not lost — `lazyMigrateLegacyFamily` still runs on their first sign-in/kiosk/teacher-add. `--include-dormant` restores the old behaviour. **Prod-cutover TODO: expect `Dormant: ~299` in the run summary and do NOT read it as breakage** (§6 step 2). **Accepted limit:** skipped families are not findable by welcome-team at the desk — `searchFamilies` and `/welcome/roster` have no legacy fallback. Recorded, not fixed.
  **(B) Migration-status had to change with it.** `getMigrationStatus` diffed EVERY legacy fid against Setu, so after the skip it would have reported `missing: 299` **permanently** — an amber warning on a launch-week screen with no way to tell a deliberate skip from a broken migration. It now excludes dormant families from the expected set and reports `skippedDormant` separately (new **required** field on `MigrationStatusResponse`); the roster strip states it in muted text ("skipped on purpose"), not as a warning. A dormant family that has since signed in counts as **migrated**, not skipped. Dormancy is **recomputed from the roster** in both places via one shared `listDormantLegacyFids()` rather than the migration persisting its skip set — a persisted set goes stale/partial after any `--limit`/`--fid` run, and it is nearly free (one TTL-cached `/roster` read `listAllFamilies()` already makes).
  **(C) Unknown-centre prompt.** `families.locationNeedsConfirmation` now flows end to end: schema → **BOTH** hand-written field maps (`get-family-by-fid.ts` for `/family` + the gate, `get-family-for-welcome.ts` for `/welcome/family/[fid]`) → both `/family` gates → `/complete-profile` selector → `PATCH /api/setu/family` (which validates `location` against `getLocationOptions()` and clears the flag). The schema change **alone would have been dead code** — those maps have no spread, so a field missing from them is `undefined` forever. Only the literal `true` gates anything. **No index, no migration, no backfill.** Mobile contract entries added for both the response field and the PATCH request field.
  **(D) ⚠️ MEASURED SNAPSHOT DRIFT — the §6 step-0 refresh is not theoretical.** The deployed-UAT `migration-status` check (which reads **live** RTDB, no snapshot at runtime) returned `legacyTotal: 867, skippedDormant: 2` on 2026-07-26, i.e. the **live prod roster now holds 869 families against the snapshot's 867** — two new families since the 2026-06-10 capture, both dormant. Small, but it confirms the roster is still moving. **Refresh the snapshot immediately before the prod migration and diff the count**, or those families are silently omitted.
  **(F) Post-ship review found FIVE gaps that neither the tests nor the UAT walkthrough caught (fixed `ad0ab53`).** Recorded because four of them are *integration seams* - the kind that only appear when you look at what the new code now touches, not at the new code itself.
  1. **`PATCH /api/welcome/families/[fid]` (staff edit) did not clear the flag.** Spec §1.9c names welcome-team as the remedy for an ACTIVE family whose legacy centre is wrong, and P1's staff-edit shipped the same day - so the two features were in contact without agreeing. Staff would fix a centre at the desk and the family would still be asked at next sign-in, able to silently override the correction. **Demonstrated live against the then-deployed build before the fix landed** (`location` written, flag left `true`).
  2. **That same route did not validate `location`** against `getLocationOptions()`, so the check added to the family's own route was bypassable one route over. Also **demonstrated live**: a staff PATCH of `{"location":"Atlantis"}` returned `200` pre-fix, `400 unknown-location` post-fix.
  3. **`GET /api/setu/dashboard` builds a THIRD hand-written family projection** (5 fields, narrower than `FamilyDoc`) and did not carry the flag - so the mobile could not implement the prompt, while the changelog claimed it could. The plan's audit method (`grep ': FamilyDoc = {'`) structurally could not see it, because this one is an inline literal inside `NextResponse.json`. **If you add a family field, there are THREE projections to update, not two.**
  4. **Double RTDB download.** `getMigrationStatus` ran `listAllFamilies()` and `listDormantLegacyFids()` in one `Promise.all`; both read `/roster`, and `readRtdb`'s TTL cache stores only the resolved value - it does **not** dedupe in-flight requests. A cold migration-status request downloaded the whole roster twice (~3.2 MB). Now sequenced so the second is a cache hit.
  5. **`--fid <dormant-fid>` matched nothing** - the skip ran before the `--fid` filter, so asking for a dormant family by id reported "0 matched" and read as "no such family". An explicit `--fid` now overrides the skip.
  **(E) Verification.** `pnpm test` green (3037 portal + 764 shared-domain). The three dangerous predicates were **mutation-tested**, not assumed: forcing `centreReady` true fails the block-Save test; forcing it to `centre !== ''` fails **six pre-existing** tests (the "locks out the whole launch population" signature); forcing `centreDone` true fails four (the permanent loop). Deployed-UAT E2E **5/5** (`e2e/setu/registration/centre-confirmation.spec.ts`, new fixture `seed:centre-confirmation-family` → `CMT-VFTO4X7X`), including an API assertion that the picked centre persisted and the flag cleared. `GET /api/welcome/families/migration-status` probed live in UAT and returns `skippedDormant`.
- **2026-07-26** - **SECURITY: the portal's OTP store moved off the client-writable `verification_codes` collection -> `setu_verification_codes`. Prod ruleset committed as a baseline. NO index, NO migration; a rename, not a data move.** The live 715b8 ruleset (provided by CMT Developer, now captured verbatim at `firestore.prod.rules.baseline`) allows **`create` on `verification_codes/{any id}` with NO `request.auth` condition**, and its `hasAll(['code','expires','type'])` check does not forbid extra keys. The portal's doc id is `sha256(normalizeContact(...))` and `normalizeContact('email', v)` is just `v.trim().toLowerCase()` (`contact/normalize.ts:1-4`), so the id is **computable by anyone who knows the address** - no secret. `verifyCode()` (`firestore/verification-codes.ts`) then trusts the document's own `code`/`type`/`expiresAt`, and the Admin SDK bypasses rules entirely. **Exploit:** write `{code, expires: <Timestamp>, type, expiresAt: <future ms>}` from the public client SDK - `expires` satisfies the rule, `expiresAt` satisfies the reader - then present that code to `POST /api/setu/auth/verify-code` and receive a session **as the victim**. `create` only lands when no doc exists, which is the normal state for anyone not mid-sign-in, and `verifyCode` deletes the doc on success or lockout so the window reopens. That is **pre-auth account takeover for any known contact**. **Fix:** the collection NAME is the control. Everything outside `family-check-ins/**`, `guest-families/**` and `verification_codes/*` is denied to the client SDK by that ruleset, so moving to `setu_verification_codes` puts the store out of client reach **without touching prod rules or the door app**. `CODES_COLLECTION` is a named constant carrying the reasoning inline, and a test asserts `storeVerificationCode` **never** calls `.collection('verification_codes')` - the name is load-bearing, so it gets an assertion rather than a comment. **Operational note:** codes issued in the ~10 minutes before deploy stop working; affected users re-request. No backfill needed (the 10-min TTL makes the old collection self-empty). **The door app's own exposure is deliberately NOT addressed** - CMT Developer's decision 2026-07-26: it is retired after Aug 3. **CORRECTION to the 2026-07-25 entry:** it recorded that "`pledges` is client-writable in prod" and treated that as a P5 launch blocker. That was wrong - Firestore allow rules are ADDITIVE, so the `match /{document=**} { if false }` block grants nothing and everything unnamed is already denied. `pledges`/`pledge_secrets` match no rule there. **P5 Task 1 Steps 4-5 need no new prod deny and are no longer blocked on the ruleset export.** **Prod-cutover TODO: none** - the rename ships with the code and prod holds no portal OTP documents yet.
- **2026-07-26** - **Aug-3 launch Phase 1 Track B + Phase 3: staff cross-family edit, SES-managed email templates, and SMS sign-in refused. SEVEN new env vars; NO new collection, index, field, script or migration.** Plans: `docs/superpowers/plans/2026-07-25-launch-p1-roles-and-cross-family-edit-v2.md` Tasks 7-10 and `...-p3-communications-v2.md` Tasks 1-5. **(1) Staff cross-family edit.** Three new routes under `/api/welcome/families/[fid]` (family `PATCH`; members `POST`; member `PATCH`/`DELETE`), welcome-team + admin only, coordinator explicitly denied (family READ without family EDIT). **No `canAccessRoute` change** - the `/api/welcome/families/` prefix clause already covers them; test cases added for the new sub-paths. Every write goes through a new shared core (`features/setu/members/write-member.ts`) that the family self-serve routes now also call, so a staff-created member cannot be shaped differently from a family-created one - in particular it carries the per-type required-field matrix, without which staff could create a Child with no grade and no birth month and immediately trap that family on its own `/complete-profile` gate. Staff writes append an `audit_log` row inside the same transaction; family self-serve writes append none. **A family rename additively extends `searchKeys`** (welcome-team search is `array-contains` on it, so a rename that skipped this would leave the family unsearchable by the name every screen then shows); additive rather than replacing, because that array is deduped across the family name AND every member name, so deleting the old value can drop a key a member name also justified. `welcome-team` can now edit a child's grade on the member detail page (previously admin-only, and it was a missing capability rather than a 403 - the control was gated on `admin &&`, so a volunteer never saw it). **(2) SES-managed email templates, dark by default.** New `sendManagedEmail` prefers an SES template when its `SES_TEMPLATE_*` var is set and renders in code otherwise, so the migration is one template at a time with no flag day. **NEW ENV (all optional, all currently unset - leaving them unset is the current production behaviour, unchanged):** `SES_TEMPLATE_PAYMENT_REMINDER`, `SES_TEMPLATE_DONATION_THANK_YOU`, `SES_TEMPLATE_SETU_INVITE`, `SES_TEMPLATE_SETU_JOIN_REQUEST`, `SES_TEMPLATE_PLEDGE_ACTIVATED`, `SES_CONFIGURATION_SET`. All six added to `turbo.json` build env and `lib/env.ts`. **Variable contract + authoring checklist: `docs/runbooks/ses-email-templates.md` (new).** **⚠️ Two traps recorded there, both load-bearing:** a template that EXISTS but fails to render is **accepted by SES, returns a MessageId, and delivers nothing** - the application cannot see it, which is why `SES_CONFIGURATION_SET` (a `RENDERING_FAILURE` destination, created **in `AWS_SES_REGION`**) plus a real per-template UAT send are both required before pointing prod at any template; and SES templates AND configuration sets are **region-scoped**, so one created outside `ca-central-1` behaves exactly like one that does not exist. OTP is excluded from this path at compile time and at runtime - an SES-side edit ships with no deploy and no review, and for a sign-in code a render failure locks every family out at once. **(3) SMS sign-in now refuses honestly.** **NEW ENV `NEXT_PUBLIC_FEATURE_SMS_OTP`** (default OFF; added to `turbo.json`). SNS is still sandboxed with no Canadian origination number - measured 2026-07-24 - so every OTP SMS was accepted by AWS, billed, and delivered to nobody, leaving families on a code screen forever. Seven surfaces now return a typed `400 sms-signin-unsupported` instead: `api/setu/auth/{send,verify}-code`, `api/auth/family/{send,verify}-code` (the LEGACY `/login`, still the production entry point for existing BV families), `api/setu/contacts/{send,verify}-code`, plus the sign-in page, the legacy login form and the add-a-phone affordance. Each API refusal sits before the family lookup so a phone answers identically whether or not it is registered. The register screen no longer hands a phone match to `/sign-in?type=phone` - that handoff pre-selects the Phone tab and suppresses the saved password preference to force an OTP proof, so a family the portal had just recognised would land on the one screen guaranteed to refuse them. **Independently of that flag, `lib/aws/sns.ts` now refuses any non-`+1` publish outright** (logs the prefix only), which covers all seven `sendSMS` callers including the two prasad senders and the join-request notification that publish to unreachable international numbers on every run today. **Prod-cutover TODO: set nothing.** Every new var is optional and its unset state is the current behaviour; `NEXT_PUBLIC_FEATURE_SMS_OTP` should stay unset until SNS clears the sandbox AND a Canadian origination number is registered, at which point flipping it restores SMS sign-in everywhere at once. **MOBILE_API_CHANGELOG entry added** (`6d1fd0d`) - the mobile app calls all four `/api/setu/*` routes and must map the new error code to real copy. **(4) UAT ops:** re-ran `seed:test-accounts`, which now also seeds `setu-test-parent-volunteer@chinmayatoronto.org` - a family-manager with `welcome-team` mid-keyed in `roleAssignments`, i.e. `role=family-manager` + `extraRoles:[welcome-team]`. That is the shape PRODUCTION sees most often (welcome-team volunteers are almost always parents) and NOTHING exercised it end-to-end before: `parent-brampton` looks identical but carries `coordinator`, which is deliberately DENIED family edit, and the standalone `sevak` account has `welcome-team` as its PRIMARY role, so any code reading only the primary role passes for it. Verified by `e2e/setu/admin/staff-family-edit.spec.ts` (6/6 vs deployed UAT). **Prod-cutover TODO: none** - test personas are UAT-only.
- **2026-07-26** - **Aug-3 launch Phase 1: the `coordinator` role + the `audit_log` collection. NEW portal-owned collection `audit_log`; NEW test personas; NO new index.** Plan: `docs/superpowers/plans/2026-07-25-launch-p1-roles-and-cross-family-edit-v2.md` (Track A complete, Track B in progress). **(1) `coordinator` role** shipped through all THREE authorization gates - `canAccessRoute`, the in-handler checks, and the page/layout shells. It grants Programs + Level management + Offerings + the read-only family roster, and is explicitly DENIED reports, seva, prasad, family-search, the class calendar, `/admin` and `/admin/users`. Coordinator and welcome-team are **siblings with disjoint grants**, not a hierarchy. Grantable through `/admin/users` like any other role. **(2) `RESURRECTABLE_SEVAK_CAPS` is now DERIVED from `GRANTABLE_ROLES`** (`revoke-sessions.ts`) - it was a literal `['admin','welcome-team']`, and once a family-less coordinator could hold a session, a coordinator claim surviving member deletion would re-mint as a standalone session holding every family's roster PII. Any future grantable role is covered automatically. **(3) NEW collection `audit_log`** - one doc per staff write to another family (`{actorUid, actorMid, actorRole, action, fid, mid, before, after, at}`, plus `_test:true` on E2E rows), written INSIDE the caller's transaction so an audit gap is structurally impossible. No index needed at launch (no query surface yet); if admin ever filters it by fid or actor, that is a single-field equality Firestore auto-indexes. **(4) MIDDLEWARE BUG FIXED, pre-existing and NOT coordinator-specific:** `deny()` set `?from=<denied path>` for both denial reasons, and the auth-entry branch honoured it, so a **signed-in** user hitting a page their role cannot access looped until `ERR_TOO_MANY_REDIRECTS`. This affected every role with a `dashboardForRole` entry - welcome-team already looped on any admin-only page in prod. `from` is now set only for `no-session`. **(5) UAT ops:** re-ran `seed:test-accounts`, which now seeds a standalone `setu-test-coordinator@chinmayatoronto.org` AND writes `roleAssignments/{managerMid}` for `parent-brampton` so that persona signs in as `family-manager` + `extraRoles:['coordinator']` - the realistic staff-is-also-a-parent shape, and the only end-to-end exercise of the strip-list path. **Prod-cutover TODO: none for the role itself** (additive; nobody holds it until an admin grants it). `audit_log` self-creates on first staff edit - no seed, no migration.
- **2026-07-25** - **UAT now has a deny-all `firestore.rules`. ⛔ PROD 715b8 DELIBERATELY NOT DEPLOYED - a deny-all there takes the door app down. NO index/collection/field/script change.** Plan: `docs/superpowers/plans/2026-07-25-launch-p5-monthly-pledge-v2.md` Task 1, **Steps 1-3 only**. Until now there was **no `firestore.rules` file anywhere in the repo** and `firebase.json` declared only `indexes`, so both projects' entire client-facing surface was governed by whatever was sitting in the Firebase console, which nobody had checked - while the public Firebase API key ships in every bundle and every family holds an authenticated session. **UAT is portal-only and verified safe:** `getClientFirestore`/`getClientAuth`/`getClientDatabase` (`packages/firebase-shared/src/client.ts:38,42,46`) have **zero** call sites in this monorepo outside their own definitions, and the only `from 'firebase/...'` imports anywhere are that same file's lines 1-4. Deployed with `firebase deploy --only firestore:rules --project chinmaya-setu-uat`. **Verified in both directions, not assumed:** the Admin SDK still reads `families`/`levels`/`offerings`/`guest_check_ins`/`donations` and still writes (probe doc created + deleted), while a **client**-SDK read of `families` using the public config now fails with `Missing or insufficient permissions` - so the deny is genuinely in force, not merely uploaded. **⚠️ NEW FOOTGUN introduced by this change:** `firebase.json` now points at `firestore.rules`, so a bare `firebase deploy --project chinmaya-setu-715b8` (no `--only`) would push the deny-all to **prod** and break kiosk check-in, family self check-in, the teacher check-in screen, the teacher report and guest check-in all at once - the standalone `chinmaya-family-check-in` app talks to prod through the **CLIENT** SDK (`app/lib/firebase/config.ts:26`), and neither repo tracks a rules file so there is **no rollback artifact**. A 30-line warning banner at the top of `firestore.rules` states this; there is no `.firebaserc`, so every deploy must name `--project` explicitly. **Prod path (Task 1 Steps 4-5, still BLOCKED and unstarted):** export the live 715b8 ruleset and commit it as a baseline, add targeted `pledges`/`pledge_secrets` denies **on top** of it, diff, and deploy only with the door app owner confirming its access surface is unchanged. **Consequence to carry:** until that lands, `pledges` is client-writable in prod, so the `NEXT_PUBLIC_FEATURE_SETU_PLEDGE` flip is blocked on the ruleset export as well as the O4 key backup.
- **2026-07-25** - **Two live production defects fixed (Aug-3 launch batch, Phase 0). New additive field `guest_check_ins.sessionDate` + new script `backfill:guest-session-date`. NO new index, NO new collection.** Plan: `docs/superpowers/plans/2026-07-25-launch-p2-teacher-visitors-roster-v2.md` Tasks 1-2. **(1) Teacher roster never confirmed a completed donation.** `donations` is a **top-level** collection with an `fid` **field** (`create-donation.ts:28`), so `roster-confirmation.ts:77`'s `d.ref.parent.parent?.id` resolved `undefined` for every doc and the next line `continue`d past **all** of them - the donation signal in `isEnrollmentConfirmed` was dead on the teacher screen since it shipped. Fixed to prefer the field with the parent-path fallback, matching `enrollment-report.ts:178` / `report-dataset.ts:111` / `build-csv-rows.ts:78`. **The test suite passed the whole time because its fixture built donation docs with a populated parent path and no `fid` field - the exact inverse of the real shape**; flipping the fixture to reality made the pre-existing test fail too, which is the evidence the two were one bug. **(2) Midweek guest check-ins were invisible to teachers.** The writer stamped `date` = the actual Toronto calendar day; every teacher surface defaults its `?date=` to `mostRecentSunday()`. New pure helper **`sessionDateFor(ymd)` in `@cmt/shared-domain`** (NOT in `features/setu/calendar` - `features/check-in` must not import from `features/setu`, CLAUDE.md discipline 1, and mobile can consume it); `mostRecentSunday` now delegates to it so the Sunday math has exactly **one** implementation. The guest writer dual-writes `date` + `sessionDate` (clock read **once**, so the two cannot straddle a Toronto midnight); `readPortalGuestChildren` queries `sessionDate` **and** `date` for one release, de-duplicating by doc id, so no pre-backfill guest vanishes between deploy and backfill; `visitors.ts` normalizes the **user-supplied** `?date=` through `sessionDateFor` - without that the fix would merely move the invisibility from Sunday to midweek. Both legs are single-field equalities on a top-level collection: **auto-indexed, `firestore.indexes.json` unchanged**. **UAT ops run:** the backfill was exercised end-to-end against `chinmaya-setu-uat` with two seeded probe docs (Wed `2026-09-09` → `2026-09-06`; Sun `2026-09-06` → **itself**, confirming the noon-UTC anchor defeats the week-early drift), then re-run to prove idempotence, then deliberately corrupted to prove a plain re-run does **not** repair a bad value while `--recompute` does. **Probes deleted; UAT left with the same single pre-existing doc it started with.** Note that doc has **no `date` field at all** - `date` was only introduced 2026-07-24 in `b1395e0`, so in practice the transitional `date` read leg protects an empty window today and should be dropped once prod has run the backfill. **No `MOBILE_API_CHANGELOG` entry:** `sessionDate` is internal to the guest store, and neither `/api/setu/**` request/response shape changed.
- **2026-07-25** - **Skipped-family safety net: unknown-centre families stay findable and get ASKED for their centre. New additive family field `locationNeedsConfirmation` (ships with the launch batch; no migration).** Follow-up to the dormant-skip decision in the entry below - CMT Developer's requirement: a family skipped by the dormant filter must still be found at sign-in/registration, and since we already know its centre is unknown, it must be **prompted** rather than silently defaulted. **Findability verified (no change needed):** `find-family-by-contact.ts:62-71` falls back to the legacy roster when the `contactKeys` lookup misses, and a legacy hit runs `lazyMigrateLegacyFamily` (`build-session-claims.ts:174`; kiosk `resolve-kiosk-family.ts:70`). Contact coverage of the 299 dormant families measured: **299 have an email, 238 also a phone, 0 have neither** — all reachable on the email channel that is prod-ready. **Gap found + fixed:** `/register/family` already collects a required *Primary location* (`:59`, `:337`, `:508-519`, fed by `/api/setu/locations`), but the **sign-in** path does not — `legacy-parser.ts:224` `mapLocation(first.center)` silently returns `'Brampton'`, so a returning dormant family would be assigned a centre nobody chose. **Fix (4 additive pieces, no data migration):** (1) `FamilyDocSchema` gains **`locationNeedsConfirmation: z.boolean().nullable().optional()`** — nullable+optional, so it widens the read schema and every existing doc still validates; `location` itself stays a read-validated `z.string().min(1)` because grade-eligible, roster filters, level matching and search all assume a string. (2) `mapLocation` reports when it fell back and `lazy-migrate` persists the flag `true`; bulk-migrated families with a real centre never get it. (3) `ProfileCompletionGate` (`app/family/layout.tsx:55`) checks it alongside `isFamilyAddressComplete`, diverting the manager to `/complete-profile`. (4) `complete-profile-form.tsx` shows a centre selector when the flag is set — consistent with the family-level fields it already handles (`familyAddress`, `ProvinceSelect`), and `/api/setu/locations` is already family-readable (`can-access-route.ts:199-205`). On save it sets `location` and clears the flag. **§3 collection-ownership map updated** with the new field. **No new index** (the flag is never queried at launch; if welcome-team later filters on it, that is a single-field equality which Firestore auto-indexes for a top-level collection). **MOBILE_API_CHANGELOG entry required** when it lands — the family shape returned by `/api/setu/family` and `/api/setu/dashboard` gains a field, and the mobile completion flow should mirror the prompt.
- **2026-07-25** - **CORRECTION to the 2026-07-24 centre finding + §6 step 2 now SKIPS DORMANT FAMILIES. Documentation/decision only; no DB change yet (the script filter ships with the launch batch).** **(a) Correction:** the 2026-07-24 entry claimed "124 of the no-centre families have an active child" and that their kids would be missing from Scarborough rosters. **Both were wrong** — the measuring script treated the literal string `"NULL"` as a level value. Re-measured against the same snapshot: of the 299 all-NULL-centre families, **0 have an active child**; **0 families are misassigned** (there is no case where the parser's `first.center` is NULL while another row of that family carries a real centre, so `mapLocation(first.center)` never mis-picks for an active family); and **124 have any child row / 119 have a child whose legacy grade maps / 190 children total**, join dates 2020-2023, `classyear` NULL, all `Unpaid`. **The real symptom is the inverse of the original claim:** Scarborough loses nothing; instead ~190 **dormant children with stale grades** would land in **Brampton** teachers' "Registered · not enrolled" lists, since that list is location+grade driven and NOT enrollment driven (a child last seen in Grade 2 in 2023 is now Grade 5, so they surface in the wrong level and never show up). **(b) DECIDED (CMT Developer):** migration carries the child's grade for level placement, and everything else is repaired by the parent at first sign-in — which **already works**: `REQUIRED_CHILD = ['schoolGrade','birthMonthYear']` (`member-required-fields.ts:42`) and `lazy-migrate.ts:185` writes `birthMonthYear: null`, so **every migrated child is incomplete by construction** and the `/family` gate (`app/family/layout.tsx:55,76`) routes the manager to `/complete-profile`, where the stale grade is re-confirmed. **(c) GAP accepted:** the completion gate checks the member matrix + `isFamilyAddressComplete` but **never asks for the CMT centre** (home address ≠ centre), and `FamilyDocSchema.location` is a read-validated `z.string().min(1)` so it **cannot be written null/empty** without breaking every later read of that doc. An active family with a wrong legacy centre is fixed by welcome-team editing, which ships in the same launch batch. **(d) ACTION for §6 step 2:** `migrate-legacy-families.ts` gets a **dormant filter** — skip any family where every row has NULL/empty `center` **and** every row has NULL/empty `level`. Skipped families are NOT lost: `lazyMigrateLegacyFamily` still runs on their first OTP sign-in / kiosk check-in / teacher add. The skipped set must be counted in the run summary and written to `--csv-out` so it is auditable, never silent. Consistent with the 2026-07-20 principle of not pre-creating records.
- **2026-07-24** - **Aug 3 launch decisions + two cutover traps found. NO DB CHANGE — documentation and measurement only.** Spec: `docs/superpowers/specs/2026-07-24-aug-3-launch-batch-design.md`. **(1) §6 step 8 self-contradiction RESOLVED (CMT Developer):** the stale "**Recommended for launch**" sentence has been **deleted**; the 2026-07-20 ⚠️ note is now the operative guidance. **Do NOT run `backfill:bv-enrollments` at the 2026-08-03 cutover.** Teachers see returning students through the attendance screen's **"Not in this class yet"** section, whose *Registered · not enrolled* group is **location + grade driven, not enrollment driven** — verified 2026-07-24 that `features/setu/teacher/grade-eligible.ts:68-123` reads `families.where('location','==')` plus a `collectionGroup('members')` scan, both satisfied by what `lazy-migrate.ts:177-197` writes, with **no new Firestore index**. So the §6 step 2 bulk migration alone populates it, and marking a child present enrolls them. **(2) §6 step 2 bulk migration WILL run at prod cutover** (CMT Developer) — this is what makes the welcome roster and the grade-eligible list non-empty on day one. **(3) NEW TRAP — added §6 "STEP 0":** `migrate-legacy-families.ts` → `listAllFamilies` → `readRtdb`, and `readRtdb` (`packages/firebase-shared/src/admin/rtdb.ts:45-52`) serves the **local snapshot and never touches the network** whenever `RTDB_SNAPSHOT_DIR` is set — which `CLAUDE.md` instructs. The current local snapshot is `capturedAt 2026-06-10T11:39:05Z`, so migrating as-is would silently omit ~7.7 weeks of summer registrations **with no error**. Refresh via `pnpm --filter @cmt/portal snapshot:rtdb` first; the snapshot is ~1.6 MB ≈ **$0.0016**, so the CLAUDE.md cost rule (aimed at repeated dev/test reads) does not argue against one deliberate pre-cutover capture. **(4) DATA-QUALITY finding — SEE THE 2026-07-25 ENTRY BELOW, WHICH CORRECTS THE NUMBERS IN THIS PARAGRAPH.** `legacy-parser.ts:112-116` `mapLocation()` falls back to **`'Brampton'`** for any unrecognised centre. Measured in the 2026-06-10 snapshot the legacy `center` field is `Brampton` 1,411 rows / **`"NULL"` 574** / `Scarborough` 548 / `"ALL"` 10 — i.e. **299 of 867 families (34%) have no usable centre**, all migrating as Brampton. ~~and 124 of those have an active child … absent from Scarborough's~~ — **that claim was a measuring error, corrected 2026-07-25.** **(5) MEASURED external prerequisite state (read-only SDK diagnostics):** **SES is prod-ready** (ca-central-1, sending enabled, 50,000/day, 14/s, out of sandbox; `chinmayatoronto.org` + `bvregistration@chinmayatoronto.org` verified). **SNS is NOT** — still **in the sandbox**, `MonthlySpendLimit $1`, **zero origination numbers**, one verified destination. Sandbox exit + a Canadian origination number are AWS/carrier reviews measured in business days-to-weeks and **cannot complete by Aug 3**. Mitigation: the legacy roster is **100% email-reachable** (867 families: 767 email+phone, 100 email-only, **zero phone-only**), so launch sign-in is **email-only** and nobody is locked out; the `+1`/NANP SMS gate ships but stays dormant behind a flag until SNS clears. **No new collection, index, field, script, or migration in this entry.**

- **2026-07-20** - **Kiosk staff login: `/check-in` locked behind a shared `sevak` username+password (welcome/sevak team).** The door kiosk (`/check-in` + `/check-in/guest` + `/check-in/lookup`) was PUBLIC (200 to anyone in UAT). It is now gated to the least-privilege `kiosk` role (admin inherits) behind a friendly shared login, reusing the existing kiosk account + `password-sign-in` cookie machinery - no bespoke auth backend. **Code (7 tasks, commits `1693271`→`039bd64` + cleanup `09d5df5`):** (1) new shared helper `features/setu/auth/mint-password-session.ts` (the rate-limit → `firebaseSignInWithPassword` → `buildSessionClaimsForContact` → `__session` cookie core), with `password-sign-in` refactored to delegate to it (externally byte-for-byte unchanged; its 16 tests still green); (2) new **public** endpoint `POST /api/setu/auth/kiosk-sign-in` - body `{username,password}`, maps `username === (process.env.KIOSK_USERNAME ?? 'sevak')` → `process.env.KIOSK_ACCOUNT_EMAIL`, mints the kiosk `__session`, honors `?from` (safe-path only), 401 `invalid-credentials` on any wrong field (no field-leak), 429 passthrough, 403 if the resolved session is not `isKiosk`; added to `PUBLIC_ROUTES`; (3) new **public** page `/check-in/staff-sign-in` (`features/check-in/kiosk/staff-sign-in-form.tsx`, CspRoot + SetuLogo, username[sevak]+password, hard-nav on success, `?error=session-expired|unauthorized` banner); (4) **access-control change** - removed `/check-in`, `/check-in/guest`, `/check-in/lookup` + the 4 legacy APIs (`/api/check-in/{families/:id, families/:id/check-in, lookup, guests}`) from `PUBLIC_ROUTES` and gated them in `canAccessRoute` to `isKiosk || isAdmin` (the `/api/check-in/setu/*` rule was already gated); (5) middleware `deny()` routes an unauth/wrong-role hit on the three kiosk leaf pages → `/check-in/staff-sign-in?from=&error=session-expired|unauthorized` (NOT `/sign-in`, NOT legacy `/login`), + `dashboardForRole('kiosk') → '/check-in'`; (6) mid-use expiry: `features/check-in/kiosk/kiosk-auth.ts` `handleKioskAuthExpiry(res)` hard-navs to the staff login on any kiosk-API 401 (idle tablet past the 14-day cap gets the clear prompt on the next tap), wired into all 4 kiosk fetch call-sites. `/check-in/{admin,teacher,family}` are SEPARATE role logins and were left untouched. **New env `KIOSK_USERNAME`** (default `'sevak'`; added to local `.env.local`; the endpoint falls back to `'sevak'` when unset, so it is OPTIONAL in Vercel). **UAT op:** re-ran `seed:kiosk-account` (idempotent) to re-affirm the `kiosk-tablet@chinmayatoronto.org` account (role `kiosk`, uid `85f395a5…`) and align its password with local `.env.local` for the deployed E2E. **NO new Firestore index / collection / field / migration / schema.** **NO `MOBILE_API_CHANGELOG` entry** - web-only: the gated `/api/check-in/*` are not consumed by the mobile family app, and `kiosk-sign-in` + the staff page are web-only; `password-sign-in`'s request/response shape is unchanged by the refactor. **⚠️ Prod-cutover TODO (at kiosk cutover, on top of the existing kiosk TODO in the 2026-07-11 entry below - seed the kiosk account in prod + set `NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK=true`):** the staff-login endpoint reads **`KIOSK_ACCOUNT_EMAIL` at request time**, so it MUST be set on the prod `cmt-setu` Vercel env or the login returns 500 `server-misconfigured` (it does NOT need `KIOSK_ACCOUNT_PASSWORD` at runtime - that is only used by the seed to set the account's Firebase password; the sevak types the password). Optionally set `KIOSK_USERNAME` in prod Vercel to override the `'sevak'` default. The gating + endpoint + page ship with the code. Until kiosk cutover, prod stays flag-off (`/check-in` 404s via `flags.checkInKiosk`), so the gating has no prod effect yet.
- **2026-07-20** - **UAT: cleared the 492 promotion carry-forward enrollments (targeted, not a family wipe).** After the rollover model change (below), the *existing* auto carry-forwards still sat in UAT. Per CMT Developer, cleared them WITHOUT deleting any family: new `clear:promotion-carryforwards` script (dry-run default, UAT-guarded, index-free) deletes only active enrollments with `enrolledVia: 'promotion'`, **preserving the 4 on `_test` E2E fixtures** (CMT-E2E-PSIB/PSOLO/ATT + CMT-FSWEDU2X — the "Previous students" fixtures the Playwright suite needs). Ran `--commit` on UAT: **492 promotion enrollments deleted**, families/members/real enrollments untouched. Active BV enrollments dropped 529 → 37 (21 welcome-team + 8 family-initiated + 4 first-attendance + 4 preserved E2E). **Side-effect (not addressed here): 490 families now have a `publicFid` but no active enrollment** — those FIDs were minted in the same eager backfill/rollover era; run `strip:eager-publicfids` (§10, existing) if a fully FID-clean prod-shape roster is wanted (unenrolled → no FID, consistent with lazy minting). **No schema/index/collection change; UAT-only data op (no prod replay — prod never had the eager carry-forwards).**
- **2026-07-20** - **Annual rollover is now grade + level ONLY — it no longer creates carry-forward enrollments (Vaibhav's model; the root-cause fix for the "500 registrations").** Root cause of the inflated roster: the school-year rollover (`promote-families.ts`) discovered every family with an active prior-year enrollment and, besides bumping grades, **wrote a brand-new active enrollment for the new year** (`enrolledVia: 'promotion'`). That auto-created carry-forward is exactly what showed as "Registered" (UAT: 492 of 520 active BV enrollments were `enrolledVia: promotion`). Per CMT Developer + Vaibhav (2026-07-20): the rollover's job is **only** to move kids up a grade and remap them to the new year's level; a family must **re-enroll fresh each year** through an actual action (manual enroll / teacher first-attendance / self check-in), which is also the single point where the Family ID is minted (`enrollFamily` → `ensurePublicFid`). **Code change:** `promote-families.ts` now writes only the grade updates + the source-enrollment cancellation; the `tgtRef` (new-year enrollment) write is removed. Discovery scope is unchanged (only families with an active prior enrollment — so it still never touches never-enrolled families). Level remap is unchanged: the bumped grade maps to the new-year level via the admin level grade-bands at read time (e.g. Grade 1 → Grade 2 → Level 2). Idempotency now rests on source-cancellation (a re-run finds no active source). Tests updated (`promote-families.test.ts`) to assert NO new-year enrollment is created + Vaibhav's Grade 1 → Level 2 example. Promote-step UI copy clarified ("does not enroll anyone for next year"). **No schema/index/collection/flag change; no API-shape change (the admin rollover route + report shape are unchanged) → no MOBILE_API_CHANGELOG entry.** Behavioral note for mobile: after a rollover, a previously-enrolled family reads as **not-enrolled** (no active enrollment) until they re-enroll — `bvEngagement` was already nullable, so no shape change. **⚠️ Prod-cutover consequence — reconsider §6 step 8:** the `backfill:bv-enrollments` script does the SAME auto-registration this change removes (it enrolls currently-registered legacy families from their roster membership). Running it at launch would re-inflate the roster with "Registered" carry-forwards — inconsistent with this model. Under Vaibhav's model the launch decision should lean toward **NOT** running the backfill (rosters start empty; families re-enroll + get a FID as they engage; returning kids surface in the teacher "Registered · not enrolled" / grade-eligible section once they're in Setu). See the updated §6 step 8 caveat.
- **2026-07-19** - **Disclaimers/acknowledgements: real Bala Vihar content + `intro`/`acknowledgement` fields + single "I Acknowledge" flow.** Replaced the 4 draft sections with the actual "CMT Bala Vihar Acknowledgements" content: an intro preamble (Hari Om! + the pledge link), **5** value sections with bulleted bodies (Care for Our Sacred Spaces · Respect & Responsibility · Seva & Community Values · Registration & Parent Responsibility · Bala Vihar Supervision), and a single acknowledgement statement. The family screen changed from a checkbox-per-section to ONE **"I Acknowledge"** at the bottom. **Schema (additive, read-tolerant):** `DisclaimersConfigSchema` gains `intro` + `acknowledgement` (both `z.string().optional().default('')`) — no migration; a config written before these fields still parses (they default to `''`). The admin editor now edits the intro, add/remove sections, and the acknowledgement. **UAT content write:** published to `app_config/disclaimers` via the real `setDisclaimersConfig` publish path (**version 5 → 6**, `updatedBy: script:docx-import`); the version bump makes all families re-accept on next visit. **Mobile:** `GET /api/setu/disclaimers` response gains `intro` + `acknowledgement` (both always present, `''` = hide) — MOBILE_API_CHANGELOG entry added; the accept endpoint is unchanged. **No new index/collection/flag.** **Prod-cutover TODO:** when disclaimers go live in prod, publish the same content there (admin `/admin/disclaimers` → edit → Publish, or the seed default now carries it) — the code seed `DEFAULT_DISCLAIMERS_CONFIG` already has the full content, so an absent prod doc renders it without any write.
- **2026-07-19** - **CRITICAL data-loss bug fixed: member-id (`mid`) allocation collided on numbering gaps and silently overwrote an existing member. Plus a one-off UAT recovery of the member it clobbered.** Vaibhav re-added his wife via the invite flow and his child **Harshita vanished**. Root cause (reproduced against real UAT `CMT-AI55HB3E`): both the invite-send route (`api/setu/invite/send`) and the add-member route (`api/setu/members`) allocated the new `mid` as **`${fid}-${zeroPad(memberCount + 1)}`** — from the member *count*, not the highest existing suffix. Once the numbering has a gap (his wife's `-02` had been deleted), `count+1` resolved to `-04`, which already belonged to Harshita, and the routes write with **`txn.set`** → her doc was **silently overwritten** by the pending-wife payload (then accept finalized it as the wife; the fresh high publicMid 52710 is the fingerprint). **Fix (code-only):** new pure helper `nextMemberMid(fid, existingMids)` (`features/setu/ids/member-mid.ts`) = **max(existing numeric suffix) + 1** with a defensive skip-if-taken guard; both routes now call it (local `zeroPad` removed from each). Registration (`register-family.ts`) was already safe (sequential build on an empty family). Unit tests for the helper (incl. the exact `-01,-03,-04 → -05` gap case) + a route-level regression in `invite/send` reproducing Vaibhav's scenario. **`mid` format is unchanged** (`${fid}-NN`, opaque to mobile) so **no API-shape change and no `MOBILE_API_CHANGELOG` entry**; no new index/collection/flag/schema. **One-off UAT corrective write:** restored **Harshita Rana** to `CMT-AI55HB3E` as a NEW non-colliding `mid` `-05` (Child, grade 2 — matching sibling Parth's legacy-5→6 promotion — birthMonth 9, legacySid 2546, fresh publicMid 52716), sourced from the legacy RTDB snapshot (fid 477, sid 2546); nothing else touched, she was NOT added to any enrollment (the enrollment already listed only Parth). The direct write does not bust the `family-${fid}` `use cache`, so she surfaces on `/family/members` on the next server revalidate (~5 min) or the next real family mutation. **Prod-cutover TODO: none beyond deploying the code fix** — the Harshita recovery is UAT-only test data (no prod replay). Latent exposure elsewhere: any family that had a member deleted and then a member added/invited BEFORE this fix could have suffered the same overwrite; in prod the invite/add flows are pre-launch so exposure is minimal, but a corruption scan can be run if needed.
- **2026-07-17** - **Teacher "Registered · not enrolled" list: default cap of 20 + name search (resolves the deferred busy-location follow-up below).** Vaibhav flagged the list as unbounded; the fix is UI-only. Measured the real pool per Brampton BV level (read-only UAT probe): it is **dozens, not hundreds** (Level 2 = 54, others 5-38) and is **already ~all returning/legacy families** (87-100% carry a `legacySid`) — the pool *is* "last year's students who haven't re-enrolled". There is **no prior-year signal to filter on**: last year's BV offerings (`bv-brampton-2025-26`, `bv-scarborough-2025-26`) have **zero enrollments** in UAT (Setu is pre-launch — all 611 Brampton / 245 Scarborough enrolled mids are under this year's `bv-*-2026-27` pid), and `legacySid` doesn't discriminate (nearly everyone has it). So instead of a last-year filter (empty data) we made the ~50-row list scannable: `NotInClassSection` (`features/setu/teacher/components/not-in-class-section.tsx`) now shows the heading count `Registered · not enrolled (N)`, renders the first **20** (server already sorts by first name), and — only when `N > 20` — shows a client-side **name search** over the full pool (the server already returns it, so search is free) plus a `Showing 20 of N · search to add others` footer. Search covers the whole pool (finds anyone beyond the cap) and marking present still enrolls via `grade-eligible` POST (never the guest workaround). **Purely client-side: NO API/response-shape change (no `MOBILE_API_CHANGELOG` entry), no new route, no Firestore index, no schema/flag/DB op.** Deferred future hook (documented, NOT built — YAGNI, empty until Setu completes a full year): once a prior-year Setu enrollment exists, prioritize genuinely-returning students to the top of the capped view via the existing `(pid,status)` index. Deployed-UAT E2E: added a **read-only** case to `e2e/setu/teacher/not-in-class.spec.ts` that asserts the cap+search on real Brampton "Level 2" data (reads the pool size off the live API response; skips gracefully if it ever drops ≤20; never marks anyone present). **Prod-cutover TODO: none beyond deploying the code.**
- **2026-07-17** - **Pending co-manager invite (member created at send) + teacher "Registered · not enrolled" inline group (Vaibhav feedback).** Two features, **code-only, NO new Firestore index / collection / migration.** **(A) Pending invite:** `POST /api/setu/invite/send` now REQUIRES `firstName`/`lastName` (were optional) and creates the co-manager member immediately with a NEW optional member field **`members.inviteStatus: 'pending' | null`** (absent/null ⇒ normal active member) — visible on `/family/members` with an "Invite pending" badge before they accept. The member is NOT added to `family.managers` and has no `contactKey` until accept (`POST /api/setu/invite/accept` now LINKS the pending member — sets uid, clears inviteStatus, adds to managers, writes contactKey — instead of creating a duplicate; legacy no-`memberMid` invites still create). New manager-only **`POST /api/setu/invite/cancel`** (body `{ mid }`) deletes the pending member + invite together; `canAccessRoute` routes `/api/setu/invite/cancel` to the manager catch-all (excluded from the any-signed-in-user invite rule). **`inviteStatus` needs NO migration** — it is nullable/optional and read-tolerant; existing members are unaffected (absent ⇒ active), and `membersRequiringCompletion` skips pending members so no gate change for existing families. **(B) Teacher "Registered · not enrolled":** the attendance screen's new inline "Not in this class yet" section merges the Previous-students list (moved off the now-redirecting `/previous` page) with a new group of registered-but-unenrolled children whose grade/age matches the level, lazily loaded via new teacher-only **`GET/POST /api/setu/teacher/grade-eligible`**. The read (`getGradeEligibleUnenrolled`) uses ONLY existing indexes — families-by-location (auto single-field), one **index-free** unfiltered `collectionGroup('members').get()` filtered in memory (donations-read pattern), and the existing `enrollments(pid,status)` collection-group index. Marking present routes through `markGuest` (existing first-attendance auto-enroll). Mobile contract entries in `MOBILE_API_CHANGELOG.md` (`ccc9490` invite flow + the new grade-eligible route). Deployed-UAT E2E: `e2e/setu/teacher/not-in-class.spec.ts` (replaces `previous-students.spec.ts`) + `e2e/setu/pending-invite.spec.ts` (seeds a pending member+invite directly, no email side-effect, self-cleans). Reused the existing `seed:e2e-family` §6f fixture (attendanceEvents reset per run; no new fixture). **Prod-cutover TODO: none beyond deploying the code** — no schema-collection/index/flag/DB op; `inviteStatus` materializes only on new invites. Known follow-up (deferred, Vaibhav-acked): the registered-not-enrolled list at a busy location (e.g. Brampton) is unbounded — add a search filter / soft cap later.
- **2026-07-15** - **Roster "Enrolled" filter → 3-state issue-#23 engagement (Enrolled / Registered / Not enrolled).** Vaibhav asked why 522 families showed as "Enrolled" for 2026-27 in UAT: the old filter meant "has an active enrollment", which counts rollover carry-forwards. Real breakdown of the 517 active 2026-27 BV enrollments — **15 confirmed** (3 family-initiated + 12 teacher-marked), **502 unconfirmed carry-forwards** (`enrolledVia: promotion` from the 2026-06-07 school-year rollover). Aligned the welcome roster with the family dashboard + teacher roster: `RosterReportRow` gains `bvEngagement: 'confirmed'|'registered'|null` (computed in `report-dataset.ts` via the same `isEnrollmentConfirmed` rule — deliberate enrolledVia, an attendance mark scoped by pid, or an eid-matched completed donation; legacyPaid N/A as all active BV offerings are portal-sourced). The filter is now `engagement` (enrolled/registered/not-enrolled), the summary shows the split, and each card carries an Enrolled/Registered badge. **Code-only: a new bulk `attendanceEvents where status in [present,late]` read (single-field, AUTO-indexed — no composite index), no DB write, no schema-collection/flag change.** This is a UAT-visible artifact (whole legacy roster eagerly migrated + rolled over); in prod-lazy far fewer families would carry forward. Tests + e2e updated.
- **2026-07-15** - **Donations report REMOVED from the reports hub (CMT decision — no collective financial info there).** `/welcome/reports` no longer has the Donations card, and the whole donations *report* path is gone: `REPORT_KINDS` is now `['enrollment','attendance']`; the `DonationsReport` type/schema, `donations-report.ts` (`buildDonationsReport`), and `donationsReportToCsv` were deleted; `/api/welcome/reports/donations` now returns **400** (removed kind); the `canAccessRoute` admin-only rule for it was dropped (all report kinds are welcome-team now). The family **donation feature** (checkout, `donations` collection, payment chips, per-family Paid/Outstanding on the roster) is UNCHANGED — only the aggregate financial *report* was removed. **Code-only: no schema-collection/index/flag/DB change** (`/api/welcome/*`, so no mobile changelog). Tests updated (shared-domain reports + can-access-route, route, reports-hub, report-csv, e2e reports.spec). **Prod-cutover TODO: none beyond deploying the code.**
- **2026-07-15** - **Roster: "Enrolled" filter + Family ID shown only when minted + UAT eager-FID cleanup (Vaibhav feedback).** **(A) Code (shipped `bb28f85`):** `/welcome/roster` gained an **Enrolled (Yes/No)** dropdown (`enrolled` = has ≥1 active program enrollment) wired through `matchesRosterFilters` + the report CSV route + export button; and the roster cards now render the new Family ID **only when a `publicFid` has been minted** — the meta line is built from parts (`FID {publicFid}` · `Legacy {id}` · location) instead of `displayFid()`, which fell back to the internal `CMT-` doc id and would leak it for lazy/unenrolled families. No schema/index change. **(B) Corrective UAT write — `strip:eager-publicfids` (new script, §10):** UAT was renumbered to the 5001+ band for EVERY family before the lazy model (§14 2026-07-14), so the roster showed a Family ID even on families that never enrolled. Stripped `publicFid` (→ null) from the **358** families with no active enrollment (231 never-enrolled + 127 whose only enrollment was cancelled backfill churn); `_test` fixtures skipped. Result: **513 families keep a FID (all with an active enrollment), 0 unenrolled families show one** — UAT now matches prod-lazy. A stripped family re-mints a fresh FID if it re-enrolls (`ensurePublicFid`). **Prod-cutover TODO: none** — prod is lazy from the start (never eagerly renumbered), so no family has an unearned FID to strip. No code/schema/index change for the data step.
- **2026-07-15** - **Cutover clarification: the teacher "Previous students" roster depends on the BV enrollment backfill (§6 step 8) — now a firm launch decision, not an optional eager step.** Surfaced by Vaibhav testing: after his family (`legacyFid 477`, Harshita Grade 2) was reset to pending, she vanished from Level 2's "Previous students". Root cause is structural, not a bug: `deriveRoster` builds BOTH the Enrolled and Previous-students lists **only from families with an active enrollment for the term's `pid`**. The lazy-`publicFid` cutover (§14 2026-07-14) skips the bulk enrollment backfill, so at prod launch **no legacy family has a carry-forward enrollment → teacher rosters, including "Previous students", start EMPTY**; returning kids appear only as families engage (kiosk / teacher-add / portal enroll). Resolution: **running `backfill:bv-enrollments --allow-prod` at cutover populates those rosters AND is FID-safe** — verified 2026-07-15 that it does NOT mint a `publicFid` (an enrolled family stays `publicFid:null` until the enrollment is *confirmed*), so it is fully consistent with lazy minting. Reframed §6 step 8 into an explicit ⭐ LAUNCH DECISION (run it unless you intend rosters to fill purely by engagement) and updated the §6 step 2 note so step 8 is decided on its own rather than swept into "skip the eager path". **No code change.** UAT test-data note: to reproduce the "Previous student" state for Harshita I ran `backfill:bv-enrollments --fid 477` (recreated the unconfirmed carry-forward for the 2 Rana kids; no `publicFid` minted) and deleted one stray `present` attendanceEvent (`brampton-level-2-…-2026-27` / `CMT-AI55HB3E-04` / 2026-07-05) that would otherwise confirm her into Enrolled — transient UAT test data, not a prod step.
- **2026-07-14** - **Shared E2E `_test` family fixture (`CMT-FSWEDU2X`) restored + made wipe-resilient.** During the roster E2E verification the shared seeded family was found **missing** from UAT (a prior `wipe:test-leaks` had removed it), which fails every spec that hardcodes it (`roster`, `prasad`, `rollover-set-grade`, `enroll-wording`). Root cause of the brittleness: `seed:e2e-family` created the family via `registerFamily`, whose fid is **random** (`generateFid()`), so a re-seed produced a *new* id that no longer matched the hardcoded specs. Fix: `registerFamily` gained an **optional test/seed-only `fid` seam** (`input.fid ?? generateFid()`; the register ROUTE never sets it — production always mints random), and `seed:e2e-family` now pins `fid: 'CMT-FSWEDU2X'` so a wiped fixture re-seeds to the same well-known id. Also corrected the seeded child's grade from the non-canonical `'Grade 4'` → `'4'` (the member forms only ever write canonical `CHILD_GRADE_OPTIONS` tokens; `'Grade 4'` was re-seeding the exact dirt the roster Grade filter now guards against). Re-seeded UAT 2026-07-14 (`seed:e2e-family`); all 5 `roster.spec.ts` tests green vs deployed `cmt-setu.vercel.app`. **Code-only + a `_test` re-seed — no schema/index/flag, no prod DB op; the `fid` seam is test-only (never the prod register path).**
- **2026-07-14** - **`/welcome/roster` filter polish + stale `schoolGrade` cleanup (Vaibhav roster feedback).** **(A) Filters → dropdowns:** the roster's Location/Program/Level/Grade/Payment filters were walls of chip buttons that "filled half the screen" on mobile — replaced with compact native `<select>` dropdowns in a responsive grid (`roster-browser.tsx`). **Code-only, no DB op.** **(B) Grade filter normalized + de-junked:** `deriveGradeOptions` now returns only canonical grade-ladder tokens (JK, SK, 1..12) actually present, ladder-ordered; `matchesRosterFilters` compares grade via `normalizeGrade` on both sides, so a legacy `"Grade 4"` collapses onto `"4"` and un-mappable blobs never render as a filter chip (`packages/shared-domain/src/setu/roster-report.ts` + tests). **(C) Corrective UAT write — `normalize:legacy-grades` (new script, §10):** 11 of 12 stale `members.schoolGrade` values repaired — 10 pre-level kids `"Pre L1 (Gr JK-SK)"`/`"Pre A (Gr JK-SK)"` → **`SK`** (re-derived authoritatively from the legacy `/roster` snapshot, legacy `grade=0`; those rows had lost `legacySid` in an old migration, matched instead by the stale blob == the row's `level`), and `"Grade 4"` (a test "Pending Child") → **`4`**. One test-family record `"J"` (`CMT-89KRYB15`) is genuinely ambiguous and was left untouched + reported (the normalized filter drops it from the chip list anyway). Result: distinct child grades are now JK/SK/1..12 + the single `"J"`. Idempotent; UAT-only (refuses non-UAT). **These grade blobs were produced by `legacy-parser` runs that predate its grade-mapping fix; current code maps `grade -1/0 → JK/SK` and never stores the blob**, and prod materializes families lazily via the current parser — so **prod-cutover TODO: none** unless a deliberate bulk `migrate-legacy-families` was ever run in prod (then re-run `normalize:legacy-grades --allow-prod`). No schema/index/flag change.
- **2026-07-14** - **Lazy `publicFid` minting adopted (Model Y2) - prod cutover no longer bulk-migrates or backfills/renumbers public ids.** The user-facing Family ID (`publicFid`, 5001+ band) is now minted at a family's FIRST enrollment via the single funnel `enrollFamily()` (idempotent get-or-mint: pre-read + pre-allocate OUTSIDE the txn, set INSIDE only if still null - never burns an id when one already exists, so re-enrollments / multi-program families keep their id), NOT at family creation. The three creation sites (`registerFamily`, `lazyMigrateLegacyFamily`, `upsertPendingFamilyChild`) now write the family doc WITHOUT a `publicFid`; the kiosk gained migrate-on-miss (`resolveKioskFamilyOrMigrate`) so a legacy family whose first touch is the door is lazy-migrated (record only, no id) then minted when the check-in auto-enrolls it; the family dashboard shows an "Assigned when you enroll" nudge when `publicFid` is null (never the internal `CMT-` id). `publicMid` timing is unchanged (still minted at member creation). **Rationale (Vaibhav, WhatsApp 2026-07-13/14):** ~300-500 active families vs thousands of legacy - don't carry forward stale families or hand new IDs to families that never enroll; "in Setu with an ID" ⟺ the family engaged. Lock (the existing `counters/familyPublicId` transaction) NOT a queue - new-family mints run ~100x under Firestore's ~1 write/sec single-doc ceiling at this scale, so a queue would solve a non-existent throughput problem. **NO schema change** (`families.publicFid` was already `z.string().nullable().optional()`), **no new Firestore index**, no data migration. Spec `docs/superpowers/specs/2026-07-14-lazy-publicfid-minting-design.md`, plan `docs/superpowers/plans/2026-07-14-lazy-publicfid-minting.md`. Mobile contract entry in `MOBILE_API_CHANGELOG.md` (`publicFid` now null until first enrollment). **UAT is left as-is** (already eagerly migrated + renumbered to 5001+; lazy paths are tested with a fresh non-migrated `_test` family). **PROD cutover impact:** the eager data pre-load steps in §6 - **step 2** (bulk `migrate-legacy-families`), **step 3** (legacy-sid backfill, now done inside `lazyMigrateLegacyFamily`), **steps 4 & 8** (BV enrollment backfills), **step 5** (`migrate:public-ids`), and the whole **`renumber:public-ids`** step - are NO LONGER the prod path. Families + enrollments + publicFids materialize lazily as families engage (OTP sign-in / kiosk check-in / teacher add / portal enroll). Steps 1 (calendar seed), 6 (grants), 7 (annual rollover) still apply. If a fully pre-loaded roster is ever wanted in prod for the welcome tools at launch, the bulk migrate MAY still be run deliberately - but `publicFid` is NOT backfilled (it mints at enrollment). **Prod-cutover TODO: none required beyond deploying the code** - the lazy model needs no prod DB op. Deployed-UAT verified 2026-07-14: a seeded not-yet-enrolled family shows `publicFid:null` on `GET /api/setu/dashboard` and the "Assigned when you enroll" nudge on `/family` (no numeric id, no `CMT-` leak on the card or sidebar); the mint transition (null -> 5001+ on first enroll, same id on a 2nd-program enroll, no burn) is covered by the real-UAT integration test `enrollments.e2e.test.ts`. New **UAT-only test fixture** `seed:e2e-pending-family` (`scripts/seed-e2e-pending-family.ts`) creates a gate-complete family with no publicFid + a password login for `e2e/setu/lazy-publicfid.spec.ts` (refuses non-UAT without `--allow-prod`; never a prod step). New **UAT-only dev utility** `reset:family-to-pending` (`scripts/reset-family-to-pending.ts`) resets real test families to the pending state - strips `publicFid`, deletes `enrollments` + `donations` - for repeated end-to-end testing (`--legacy-fid`/`--fid`/`--email`; dry-run default, `--apply`; refuses non-UAT). Used 2026-07-14 to reset the Matta (CMT-P672RGSS) + Rana (CMT-AI55HB3E) UAT families. **Never a prod step.**
- **2026-07-13** - **`backfill-bv-enrollments.ts` generalized to the live school year (+ already-enrolled skip-guard + prior-year cancel + gradeless report).** The BV enrollment backfill now targets the LIVE year via `getSchoolYearConfig().currentYear` (was hardcoded 2025-26), **skips** any family that already has an active current-year enrollment (protects rollover-promoted families whose grades were advanced), **cancels** a stale prior-year BV enrollment for the families it enrolls, and prints an enrolled-but-gradeless report. Idempotent; UAT-guarded (`--allow-prod` to bypass). **PURE ops-script change - no schema/index/collection/field/flag change.** **Ran on UAT 2026-07-13** (`pnpm --filter @cmt/portal backfill:bv-enrollments`): **21 families enrolled** (14 Brampton + 7 Scarborough; incl. `legacyFid 477` / Harshita), 9 stale prior-year enrollments cancelled, 9 gradeless children flagged (need a grade set to appear on a level), 0 errors; an immediate dry-run re-run enrolls 0 (idempotent). Note: only **21**, not the ~142 Setu-view estimate - the authoritative legacy roster shows the ~120 others are graduated/inactive (null `level`) and correctly stay off rosters (129 skipped as "no current kids"). Their kids now surface in the teacher "Previous students" list (or Enrolled if the family is already engaged - e.g. `legacyFid 477` has a stray attendance event so Harshita landed in Enrolled, per the issue-#23 rule). **FOLLOW-UP FIX (same day, from the deployed-UAT walkthrough):** the walkthrough caught that the script's grade re-assert reverted 4 members' grades to STALE legacy (2025-26) values - Harshita 2->1, which dropped her off Level 2's [2,3] band entirely. **Removed the grade re-assert ENTIRELY**: the backfill no longer writes `schoolGrade` (the legacy roster holds last-year grades; grades are owned by migration/rollover/admin; gradeless current kids are only flagged in the report). Restored the 4 reverted grades (families `477` Rana + `1435` Ashwin). Blast radius was exactly those 4 grades - `lazyMigrateLegacyFamily` is a zero-write no-op for already-migrated families, and the 21 enrollments + 9 prior-year cancels were correct. Re-verified Harshita now appears on Level 2. **Prod-cutover TODO:** re-run with `--allow-prod` after the prod launch year is activated + its BV offerings are seeded (see §6 step 8).
- **2026-07-12** - **Teacher attendance roster split into "Enrolled" vs "Previous students" (Vaibhav request).** The teacher attendance screen's main list (**Enrolled students**) now shows ONLY engagement-confirmed enrollments (the existing issue #23 `isEnrollmentConfirmed` rule); rollover carry-forwards that have not re-engaged (`enrolledVia:'promotion'`/`'welcome-team'`, no attendance/donation/legacy-paid) are moved into a secondary **Previous students** list reached from a "Previous students (N)" button. Marking a previous student present (new `POST /api/setu/teacher/attendance/confirm-previous`) writes one `present` attendance event that confirms the family's already-active enrollment, so the student + siblings surface in their levels' Enrolled lists on the next load. **Also a correctness fix:** the attendance stats and the unmarked->absent save-sweep now cover ONLY the confirmed roster, so ~100 not-yet-returned kids per level stop being stamped Absent every Sunday. **PURE code / read-model + presentation change - NO DB op: no schema change, no data migration, no rollover change, no new Firestore index, no new collection/field/flag.** The split is opt-in via `deriveRoster(..., { withConfirmation: true })`; the default preserves prior behavior, so `student-detail` (canTeacherSeeStudent) and the welcome-team `/welcome/levels/[levelId]` roster are unchanged. Verified by the full portal vitest suite (2654 green) + a deployed-UAT E2E `e2e/setu/teacher/previous-students.spec.ts`, GREEN on cmt-setu.vercel.app (the `seed:e2e-family` §6f fixture provisions the mix on a DEDICATED `_test` level `e2e-prev-level` - isolated from the binary-attendance `e2e-att-level` so the two specs never collide under the parallel runner: 2 confirmed family-initiated students + 3 previous promotion carry-forwards incl. one two-sibling family, all `_test:true`, UAT-only - **no prod fixture, no new index/collection**). Mobile contract entry in `MOBILE_API_CHANGELOG.md` (new confirm-previous endpoint + the roster split; SHA `6d994a8`). Spec `docs/superpowers/specs/2026-07-12-teacher-roster-enrolled-vs-previous-design.md`, plan `docs/superpowers/plans/2026-07-12-teacher-roster-enrolled-vs-previous.md`. **PROD cutover TODO: none beyond deploying the code** - derived semantics apply to existing prod data automatically (a teacher's roster shrinks to confirmed students, the rest move to Previous), matching the family dashboard's Registered-vs-Enrolled behavior already shipped 2026-07-02.
- **2026-07-12** - **`enrollments.location` collectionGroup index (fixes the /admin/locations remove-guard 500).** The Locations editor's remove-guard `countLocationReferences` runs `collectionGroup('enrollments').where('location','==').count()`; a collectionGroup single-field query needs an explicit `COLLECTION_GROUP` field-override, which was missing - so removing ANY centre (even an unused one) returned **500 FAILED_PRECONDITION** instead of 200/409. **Caught by the deployed-UAT E2E `locations.spec.ts`, NOT unit tests** (fake-firestore is index-blind - reinforces the "audit collectionGroup indexes on any query change" rule). **Fix:** added the `enrollments`/`location` `COLLECTION_GROUP ASC` field-override to `firestore.indexes.json` and **DEPLOYED to UAT** (`firebase deploy --only firestore:indexes --project chinmaya-setu-uat`, no `--force`). The top-level `families`/`offerings`/`levels` counts in the same guard are auto-indexed; only the collectionGroup one needed an entry. **PROD cutover TODO:** deploy this index override to 715b8 (`firebase deploy --only firestore:indexes --project chinmaya-setu-715b8`, never `--force`) BEFORE the Locations remove feature is used in prod - see §5.
- **2026-07-12** - **Locations became admin-managed config (Slice 1 of the locations + level-mgmt revamp).** The set of centre locations is no longer a hardcoded 4-value enum; it is now the admin-managed `app_config/locations` doc (`{ options: string[], updatedAt }`), **lazy-defaulting to `['Brampton', 'Scarborough']`** - no seed script, the doc auto-creates on the first admin Save at `/admin/locations`. Going-forward the two removed centres (Mississauga, Markham) are dropped from the default set; UAT verified read-only that **all 881 families are already Brampton (714) or Scarborough (167), zero Mississauga/Markham/blank**, so no read breaks. **Code:** new `apps/portal/src/lib/locations.ts` (`getLocationOptions`/`setLocationOptions`/`DEFAULT_LOCATIONS`); shared-domain location fields relaxed from `z.enum(LOCATIONS)` to `z.string().min(1)` across offering/enrollment/level/program/class-calendar/roster/family (`LOCATIONS` const reduced to the 2 defaults, `type Location = string`); membership now enforced at WRITE time (`POST /api/setu/register` + the 3 calendar routes reject a location not in `getLocationOptions()` with 400 `invalid-location`); new **public** `GET /api/setu/locations` (in BOTH `public-routes.ts` and `canAccessRoute`); new admin `/admin/locations` editor + `PUT /api/admin/locations` (admin-only, add/remove-unused/reorder) with a **referential-safety guard** (refuses removing a centre still referenced by any family/offering/level/enrollment, via `countLocationReferences` .count() aggregation - single-field equality, auto-indexed); all location pickers (register client via the public endpoint, admin program/offering/calendar dropdowns, welcome roster filter) now read the dynamic list. Family location still comes from the roster `center` verbatim (identity, no remap). **One new Firestore collectionGroup index required** — `enrollments (location ASC)` field-override, see the 2026-07-12 index entry below (the remove-guard's `collectionGroup('enrollments').where('location')` count is not auto-indexed; caught by the deployed-UAT E2E, not unit tests). No new collection / data migration (`app_config/locations` is a single doc; the field-type relaxations keep stored string values valid). Mobile contract entries in `MOBILE_API_CHANGELOG.md` (register `location` enum->string SHA `2753f40`; new `GET /api/setu/locations` SHA `2f84cf7`). **Standing pre-prod check:** before shipping to prod, run the read-only location distribution to confirm no family/offering/level/enrollment references a centre outside the config set (UAT clean 2026-07-12). **PROD cutover TODO:** none beyond deploying the code - the config doc lazy-defaults to Brampton + Scarborough in prod too; only add/remove centres via `/admin/locations`, and honour the referential guard (never orphan a referenced centre).
- **2026-07-12** - **publicFids moved to a uniform 5001+ band (supersedes yesterday's 1886-2413 re-number).** Per Vaibhav: every real legacy check-in id is below ~4456 (833 are < 2000; 34 in 2000-4999; the lone 99991 is junk), so starting publicFids at **5001** places them in an empty band ABOVE all legacy ids - collision-free AND visually distinct (5xxx reads as "the new ID"). **Code:** allocator `FAMILY_START` 1001 -> **5001** (`public-id-allocator.ts`; keeps the skip-legacy-id guard as insurance; runtime uses the persisted counter, so this is the fresh-DB base). Migration renamed `renumber-colliding-public-ids.ts` -> `renumber-public-ids.ts` and rewritten to re-number ALL families to 5001+ (fid-ordered = deterministic/idempotent fixpoint, skips legacy ids). **RAN on UAT 2026-07-12:** all **881** families re-numbered to **5001..5881**, `counters/familyPublicId.next` -> **5882**. Verified: all publicFids >= 5001, 0 legacy collisions, 0 duplicates, Matta -> 5636, re-run = no-op. Member ids (publicMid 50001..52689) confirmed a SEPARATE 5-digit namespace - no overlap with the 4-digit family band. publicFid still stored only on `families/{fid}.publicFid`. **NO schema / index / collection change.** This is the SECOND publicFid re-number in two days (families' new ids changed again); still safe (Setu unannounced - reset the "no pre-today id list" caveat to 2026-07-12). **PROD cutover TODO:** after `migrate:public-ids` on 715b8, run `renumber:public-ids --dry-run --allow-prod` then `--apply --allow-prod` BEFORE announcing new ids / enabling the kiosk in prod. Supersedes the 1886-2413 approach in the entry below.
- **2026-07-11** - **publicFid re-numbered to be disjoint from legacy ids (fixes the kiosk legacy-first + nudge collision).** ROOT ISSUE: the kiosk resolves LEGACY-first (families still use their legacy check-in id at the door), but ~60% of publicFids equalled some OTHER family's legacy id, so the "use your new Family ID" nudge would send those families to the WRONG family (e.g. Matta's publicFid 1514 WAS Garlapati's legacyFid). **Code:** (1) `resolveKioskFamily` flipped to legacy-first, publicFid-fallback (`resolve-kiosk-family.ts`); (2) the kiosk panel shows a "your new Family ID is X, please use it" nudge when the entered id differs from the resolved family's publicFid (`kiosk-check-in-panel.tsx`); (3) `allocateFamilyPublicId` (`public-id-allocator.ts`) now SKIPS any candidate that is an existing legacyFid, so NEW registrations never mint a colliding publicFid. **Data migration (RAN on UAT 2026-07-11):** new script `renumber:public-ids` (`scripts/renumber-colliding-public-ids.ts`, §10) re-assigned the **527** publicFids that equalled a legacy id to fresh numbers in the counter forward range (**1886..2413**, skipping legacy ids), advanced `counters/familyPublicId.next` 1886 -> **2414**, and left the 354 non-colliding families untouched. Verified read-only: 0 remaining publicFid/legacy collisions, 0 duplicate publicFids, Matta 1514 -> 2273. publicFid is stored ONLY on `families/{fid}.publicFid` (audited: NOT in searchKeys, not a doc id, not referenced elsewhere), so a field update sufficed. Idempotent (a re-run finds 0 colliding). **NO new Firestore index / collection / schema change** - only `publicFid` field VALUES + the counter changed. Setu is unannounced, so families had not memorized the old numbers. **PROD cutover TODO:** after the prod public-id backfill (`migrate:public-ids`), run `renumber:public-ids --dry-run --allow-prod` (preview) then `--apply --allow-prod` on 715b8 so prod publicFids are disjoint from legacy ids too - BEFORE announcing new ids or enabling the kiosk in prod. (Supersedes the fixture caveat in the entry below: with publicFids now disjoint from legacy ids, any family is a valid kiosk E2E fixture.)
- **2026-07-11** - **Kiosk slice: sign-in session fix + Setu lookup endpoint + owner-gated UAT tail EXECUTED (E2E GREEN).** Completes the portal kiosk new-ID lookup + auto-enroll slice (the two entries below cover the endpoint + role). **(A) Code - kiosk sign-in session fix (`build-session-claims.ts`):** `buildSessionClaimsForContact` now mints a session for the generic kiosk account. It gained `isKioskUser = allExistingRoles.has('kiosk')`; the register-redirect guard gained `&& !isKioskUser`; the family-less sevak branch gained `else if (isKioskUser) { role:'kiosk', redirectTo:'/check-in' }` (admin > welcome-team > kiosk precedence). Without this the kiosk account signed in (200) but got `redirectTo:/register` with NO `__session` cookie - the slice wired `kiosk` into ROLES/isKiosk/canAccessRoute/seed but NOT the sign-in session builder. **Found by the deployed-UAT walkthrough, not unit tests** (the seed→sign-in path is integration-only). +`build-session-claims-kiosk.test.ts` (4 cases incl. guard-intact + admin-wins). **(B) Code - Setu lookup endpoint (`GET /api/check-in/setu/lookup`):** returns the resolved family + children in the legacy `Family` shape (reuses `getFamilyByFid`; `type==='Child'`, `level=schoolGrade`, `Family.fid=publicFid`) so the two-step kiosk (lookup→show children→check in) works for the new id. `canAccessRoute` gate widened from the exact `/api/check-in/setu/check-in` rule to the prefix `startsWith('/api/check-in/setu/') → isKiosk||isAdmin` (covers lookup + check-in; still NOT public). No new index (single-family read). **(C) UAT ops DONE (was "PENDING owner action" in the entries below):** ran `pnpm --filter @cmt/portal seed:kiosk-account` → created the one generic kiosk auth account **`kiosk-tablet@chinmayatoronto.org`** (role `kiosk`, uid `85f395a5…`) in `chinmaya-setu-uat` (`KIOSK_ACCOUNT_EMAIL`/`KIOSK_ACCOUNT_PASSWORD` in local `.env.local`, password machine-generated). Set **`NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK=true` on the `cmt-setu` Vercel *Production* env** (the UAT deployment; via `vercel env rm/add`, rebuilt on push) - kiosk routes now live on cmt-setu.vercel.app but still require the `kiosk`/`admin` role. **(D) E2E GREEN (7/7)** - `e2e/setu/kiosk-auto-enroll.spec.ts` run vs deployed cmt-setu.vercel.app: unauth POST+GET → 401 (gate enforced); lookup → children; publicFid check-in → `enroll.created:true`; re-post → `created:false`; **legacyFid → SAME family**; enrollment doc active. **Fixture (env `KIOSK_FIXTURE_PUBLIC_FID`/`KIOSK_FIXTURE_LEGACY_FID`) MUST be a family whose legacyFid is NOT any OTHER family's publicFid** - `resolveKioskFamily` tries publicFid FIRST, so a legacyFid that equals someone's publicFid resolves the wrong family (documented publicFid-precedence limitation; the first fixture 1816/1658 collided - 1658 was another family's publicFid - switched to 1860/4456). The spec self-heals: `beforeAll`+`afterAll` delete any active BV enrollment on the fixture (index-free subcollection scan), so it mutates a real UAT family only transiently. Two stray BV enrollments leaked by the pre-fix E2E runs (on `CMT-CN09V8M4`, `CMT-ARJ0D2YN`) were cleaned. **NO new Firestore index / collection / migration.** **PROD cutover TODO (at kiosk cutover only):** (1) seed the kiosk account in prod 715b8 (`seed:kiosk-account` with `--allow-prod`-equivalent - the script hard-refuses non-UAT today, so relax the guard deliberately at cutover); (2) set `NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK=true` on the prod Vercel env; (3) the auth fix + lookup endpoint ship with the code. Until then the kiosk stays flag-off in prod.
- **2026-07-11** - **Authenticated Setu kiosk check-in endpoint (portal kiosk new-ID lookup + auto-enroll slice).** New route **`POST /api/check-in/setu/check-in`** - AUTHENTICATED, **not public**: `canAccessRoute` gates it with an explicit rule `pathname === '/api/check-in/setu/check-in' → isKiosk(claims) || isAdmin(claims)` (beside the other `/api/check-in/*` rules, before the default-deny; NOT added to `public-routes.ts`). Body `{ id, students: Record<string,boolean> }` → resolves the entered id (publicFid or legacy check-in id) to a Setu family via `resolveKioskFamily`, writes one `check_in_events` doc per student (`checkedInBy:'sevak'`, keyed `fid = legacyFid ?? publicFid ?? fid` so the existing B2 check_in_events dashboards keep resolving), then **best-effort** auto-enrolls into the current Bala Vihar offering via `autoEnrollBalaVihar` (`enrolledVia:'kiosk'`). The handler trusts the middleware gate and only re-checks `flags.checkInKiosk` (returns 404 when off). Auto-enroll is an ADDED step: an unexpected enroll throw is caught + logged and the response is still 200 with `enroll:{ enrolled:false, reason:'error' }` - a check-in already recorded is never failed by an enroll error. **NO new Firestore index / collection / migration / data backfill.** UAT-only; prod stays gated by `NEXT_PUBLIC_FEATURE_CHECK_IN_KIOSK` (unset in prod). No mobile contract entry (kiosk-only `/api/check-in/*` route, not a family `/api/setu/**` shape the mobile mirrors). **PENDING owner action:** deployed-UAT E2E for this route is deferred (needs the `seed:kiosk-account` run + `KIOSK_ACCOUNT_*` env, owner-gated as noted in the kiosk-role entry below).
- **2026-07-11** - **Dedicated least-privilege `kiosk` role + generic kiosk account (portal kiosk new-ID lookup + auto-enroll slice).** The portal door-tablet check-in endpoint is AUTHENTICATED, not public - a new least-privilege `kiosk` role authorizes it. **Code:** `'kiosk'` added to `ROLES` + new `isKiosk(claims)` helper (admin inherits it, same pattern as `isTeacher`/`isWelcomeTeam`) in `@cmt/shared-domain/auth/role`; `'kiosk'` added to the `Capability` union in `apps/portal/src/lib/auth/role-claims.ts` (so `addCapability`/customClaims accept it) and to `ROLE_REFERENCE`/`ROLE_REFERENCE_ORDER` (exhaustive `Record<Role,...>` - required for typecheck). Claim-survival verified: the kiosk session flows middleware `verifyPortalSessionCookie` -> plain cast (no Zod strip) -> `readSessionFromHeaders` (validates `role` against `ROLES`) -> `isKiosk`; the `SetuSessionClaimsSchema` discriminated union (no `kiosk` member) is NOT on the check-in path. **New env vars (local `.env.local`, UAT):** `KIOSK_ACCOUNT_EMAIL` + `KIOSK_ACCOUNT_PASSWORD` (min 8 chars, letter+digit) - NOT prod Vercel vars yet (kiosk stays cutover-gated). **New script:** `seed:kiosk-account` (§10) - UAT-only (hard-refuses non-UAT, no `--allow-prod` escape), idempotent, creates/updates the one generic kiosk auth account + writes the `kiosk` claim. **NO new Firestore index / collection / migration. No data backfill.** **PENDING owner action:** the actual UAT seed run (`pnpm --filter @cmt/portal seed:kiosk-account`) is deferred - it needs `KIOSK_ACCOUNT_EMAIL`/`KIOSK_ACCOUNT_PASSWORD` set in `.env.local` out-of-band first (owner's call, same gating as the E2E). The check-in route gating (`isKiosk || isAdmin`) ships in the next task.
- **2026-07-10** - **Family home address (required) - registration + profile-completion gate + editable card (Vaibhav request).** New REQUIRED family-level `families.familyAddress = { street, unit, city, province (2-letter CA code), postalCode } | null`. **Code:** `FamilyAddressSchema` + `CANADIAN_PROVINCES` (13, ON first) + `CANADIAN_POSTAL_RE` + `isFamilyAddressComplete` in `@cmt/shared-domain`; `familyAddress` on `FamilyDocSchema` (read-tolerant, added to the hand-mapped `get-family-by-fid`). Collected two ways: (1) NEW families - required section in the register wizard + `POST /api/setu/register` now requires `familyAddress` (`register-family` writes it conditionally); (2) EXISTING families - the profile-completion gate (`family/layout.tsx`) now also redirects a MANAGER to `/complete-profile` when `!isFamilyAddressComplete(family)`, and the `/complete-profile` form gained a manager-only Home-address section saved via `PATCH /api/setu/family` (widened to a partial `{ familyEmergencyContact?, familyAddress? }` merge). New `ProvinceSelect` + `FamilyAddressCard` (manager-editable, optimistic) on `/family/members`. The complete-profile early-return AND the disclaimers deferral both now account for the address so a complete-members-but-no-address manager never loops. **Data backfill: NONE** - no address exists anywhere (legacy roster has none); the field starts null and is collected going forward. **NO new Firestore index / collection / migration.** Mobile contract entry in `MOBILE_API_CHANGELOG.md`. **Prod TODO:** none beyond deploying the code - BUT note the visible cutover behavior: on first sign-in **every existing family whose manager has not entered an address (i.e. all ~880)** is hard-gated to `/complete-profile` until the manager adds a home address. This is intended (that is how we collect accurate addresses for all families) but communicate it before go-live (same shape as the 2026-06-22 profile-completion-gate cutover note).
- **2026-07-10** - **Emergency contact moved to the family level + friendly Family ID on My-family (#26b/#27/#23).** Per admin decision, emergency contact is now ONE optional FAMILY-level record (`families.familyEmergencyContact = { relation, phone, email } | null`), manager-editable on the My-family page, instead of the per-member `members[].emergencyContacts` tuple. **Code:** new `FamilyEmergencyContactSchema` + `FAMILY_RELATION_OPTIONS` (Mother/Father/Grandmother/Grandfather/Sibling/Other family member) + `familyEmergencyContact` on `FamilyDocSchema`; surfaced on GET /api/setu/family (added to the hand-mapped `get-family-by-fid`); new **manager-only PATCH /api/setu/family** (`canAccessRoute` made method-aware: GET any family role, PATCH `isSetuManager`); new `RelationSelect` + `FamilyEmergencyContactCard` (optimistic save, no reload) on `/family/members`; the per-member emergency section REMOVED from the add/edit member forms; member `emergencyContacts` schema slots loosened to nullable (deprecated, kept for backward compat + existing docs). Also the My-family page now shows the friendly `displayFid` (4-digit publicFid) instead of the raw `CMT-` fid, matching the home dashboard (#23 - the friendly number already existed; the page just was not using it; `migrate:public-ids --dry-run` confirmed all 879 UAT families already have a publicFid, 0 to assign). **Data backfill: NONE applied - not needed.** A read-only bulk sweep found 0 families with any usable per-member emergency contact except 1 test-junk record (`CMT-5YRFYCFO`, "testemergency"); real families never used the per-member EC form (Setu is not yet announced), so the new family-level field starts clean. **NO new Firestore index / collection / migration.** Mobile contract entry `MOBILE_API_CHANGELOG.md` (GET /api/setu/family gains `familyEmergencyContact`, new manager-only PATCH, per-member `emergencyContacts` deprecated). **Prod TODO:** none beyond deploying the code; if prod ever has real per-member EC data at cutover (unlikely - Setu unannounced), backfill `familyEmergencyContact` from each family's first usable member `emergencyContacts[0]` (bulk collectionGroup read, relation+phone required, idempotent, skip families that already have one).
- **2026-07-10** - **Disclaimers `(rev …)` leak - corrective UAT write + recurrence fix (test).** The `/disclaimers` (Acknowledgements) screen's first card body was rendering a leaked `... premises. (rev 1783124068896) (rev 1783174803822)` suffix. Root cause was NOT render code - the deployed-UAT E2E `e2e/setu/disclaimers.spec.ts` bumps section 0's body with ` (rev ${Date.now()})` and PUBLISHES it to the shared `app_config/disclaimers` doc, but its `afterAll` only restored acceptance state, never the body - so every run permanently appended another `(rev <ms>)`. **Fix (test, prevents recurrence):** the spec now snapshots the ORIGINAL `sections` (deep copy) before mutating and re-PUTs them in `afterAll` (best-effort try/catch) so it no longer pollutes the shared doc. **Fix (UAT data, corrective write):** a one-off throwaway script stripped `/\s*\(rev \d+\)/g` from every `app_config/disclaimers` section body (only `respect-responsibility` was affected - 2 rev suffixes removed; other 3 sections clean); read-back confirms all 4 sections `HAS_REV:false`, curly apostrophe + all other copy preserved. Script refused any target != `chinmaya-setu-uat`, dry-ran first, then `--apply`; deleted after use. **Also (copy):** header "Our family agreement" → **"Acknowledgements"**, and the word "agreement" removed from the `/disclaimers` metadata title, the admin editor description (`/admin/disclaimers`), the disclaimers `error.tsx` feature label, and the `/admin` dashboard card sub-text (no user-facing "agreement" wording remains). **NO schema / collection / field / index change** - only the `sections[].body` string values in the existing doc were rewritten. **Prod TODO:** none - `app_config/disclaimers` in prod (715b8) was never touched by this UAT-only E2E, so there is nothing to clean there; the test fix simply prevents future UAT pollution. If a prod doc ever shows a `(rev …)` suffix, run the same strip (dry-run first, UAT-guard removed only with explicit `--allow-prod`).
- **2026-07-08** — **Enrollment membership auto-sync (fixes the N=2 dashboard bug) + UAT retro-reconcile.** `enrolledMids` (denormalized on each `families/{fid}/enrollments/{eid}`) was frozen at enroll time — `enrollFamily`'s already-active branch is a strict no-op — so a child added AFTER the family enrolled never joined the active enrollment: the family dashboard's Bala Vihar section (which reads `enrolledMids`) silently omitted them, while the enroll page misleadingly listed them as "enrolling" (root-caused read-only against FID 1883 / `CMT-5YRFYCFO`: members had 2 children, `enrolledMids` had 1). **Fix (code):** new helper `features/setu/enrollment/sync-enrollment-members.ts` → `syncActiveEnrollmentMemberships(fid)` recomputes every ACTIVE enrollment's `enrolledMids` = the family's currently-eligible members (`memberEligibleForProgram`); it ADDS newly-eligible members and DROPS departed/ineligible ones, writes only when the set changed, and skips enrollments whose program doc is missing or not `active`. Wired (best-effort, before `revalidateTag(family-${fid})`) into `POST /api/setu/members` + `PATCH`/`DELETE /api/setu/members/[mid]`. **Fix (UAT data):** a one-off reconcile swept all **507** active-enrollment families — **46 enrollments were out of sync (every diff purely ADDITIVE — children left out; zero removals) and were reconciled**; an immediate re-run reports **0 out of sync**. **NO schema / collection / field / index change** — only `enrolledMids` array *values* were rewritten. Verified by `e2e/setu/enrollment-add-child.spec.ts` (add a 2nd child to the enrolled E2E family → dashboard BV section lists both), run vs deployed UAT after this deploys. Mobile: `MOBILE_API_CHANGELOG.md` (refetch enrollments/dashboard after a member mutation — no shape change). **Prod TODO: none at cutover** — `backfill-bv-enrollments` computes `enrolledMids` fresh (all eligible at backfill time) and the auto-sync maintains it on every later member add/edit/delete; if a prod drift is ever suspected, re-run the same read-only reconcile (bare `collectionGroup('enrollments')` scan → per-family eligible-vs-stored diff) and apply.
- **2026-07-07** — **2026-27 Brampton Bala Vihar class calendar seeded (the "Class Dates" tab deferred in the 2026-07-06 levels entry).** Transcribed CMT's *"CMT 2026-27 levels and classes"* sheet → "Class Dates" tab into `seed:bala-vihar-calendar` as `ENTRIES_BY_YEAR['2026-27']`, and **generalized the seeder** (`--year`/`--location Brampton\|Scarborough\|all`/`--admin-mtg`/`--dry-run`; the 2025-26 Brampton entries are preserved verbatim under `ENTRIES_BY_YEAR['2025-26']`, weekly schedule transcribed for Brampton only). Ran `--year 2026-27 --location Brampton` on UAT → **40 entries** (32 class Sundays Sep 13 2026 → Jun 13 2027 + 8 no-class: Thanksgiving Oct 11, CMT Diwali Nov 15, Winter Break Dec 27 + Jan 3, Family Day Feb 21, March Break Mar 21, Easter Mar 28, Victoria Day May 23) + Brampton `weeklySchedules`. Sep 13 = `first`, Jun 13 = `short`, the rest `regular`; `prasadNeeded` true on class days / false on no-class (prasad already filters `kind==='class'`, so harmless either way). **Owner decisions (2026-07-07):** location **Brampton only** (the sheet is one unified schedule with no location column; Scarborough/East dates come later — re-run `--location Scarborough` when Vaibhav sends them); the sheet's 9 **"Teacher's Admin Mtg"** annotations are **omitted** from the family calendar (internal ops note — re-run with `--admin-mtg` to surface them if ever wanted); Sat **Jun 19 Teacher's Retreat** excluded (not a class Sunday). Read-back verified read-only: all 40 schema-valid (`ClassCalendarEntryDocSchema`), 32 class + 8 no-class, Dec 27 + Jan 3 both no-class. **NO new index / collection / field / flag / migration** — `classCalendarEntries` + `weeklySchedules` already exist (slice 4b) and the `classCalendarEntries(programKey ASC, date ASC)` index is already deployed (see the 2026-06-23 entry). **Stray UAT test data removed (corrective write, owner-approved 2026-07-07):** deleted 2 phantom Brampton entries in the 2026-27 range that predated this seed — `bala-vihar-brampton-2026-09-15` (a **Tuesday**, left over from `/admin/calendar` editor testing, `createdBy` a real admin UID) and `bala-vihar-brampton-2026-09-06` (a prior `seed-script` artifact, before the official Sep 13 start), both of which would have rendered as bogus classes. Read-back confirms the 2026-27 Brampton range is now exactly the **40 seeded entries** (32 class + 8 no-class). **Prod TODO:** at cutover run `seed:bala-vihar-calendar --year 2026-27 --location Brampton --dry-run --allow-prod` → `--allow-prod` (§6 step 1); enter Scarborough's calendar once Vaibhav provides it; the 2 UAT strays are UAT-only (prod replays the committed defs, so nothing to clean there).
- **2026-07-06** — **2026-27 Bala Vihar levels finalized from CMT's official sheet (West + East).** Replaced the placeholder 2026-27 **Brampton** levels (the 2026-07-04 roll-forward from 2025-26) and populated **Scarborough's** (previously teacher-refs-only phantom) with the authoritative grade bands + curriculum from CMT's *"CMT 2026-27 levels and classes"* sheet (Vaibhav). Updated `seed:bala-vihar-levels` LOCATIONS to the 2026-27 defs (past years frozen — git history has prior) and ran `--year 2026-27` (both locations; **teacherRefs preserved**). **17 levels** upserted. **Brampton (9 managed):** Shishu Vihar, Pre-Level 1, Level 1–7 — West **Level 5 rebanded Gr 8–9 → Gr 8** and **Level 6 Gr 10 → Gr 9–10**; curriculum updates (L2 Bala Bhagavatam, L3 24 Gurus, L4 India/P.O Box God, L5 Yato Dharma, L6 Self Unfoldment + 7 Habits, L7 Essence of Gita for Youth (Verses), Pre-L1 Alphabet Safari). The sheet lists **NO West adult class**, so the existing Brampton **Parents** level is intentionally left untouched (owner default 2026-07-06). **Scarborough (8):** Shishu Vihar, Pre-Level A, Level A–E, Parents — bands unchanged from 2025-26, curriculum updated (A Bala Ramayana, B Balabhagavatam, C Symbolism in Hinduism, D Hindu Culture, E Bhagavat Gita), Parents = "Adult Class"; these were phantom and are now **real + enabled**. **Proposed-teacher NAMES in the sheet were NOT mapped to accounts** (a separate onboarding step); teacherRefs preserved. The sheet's multi-classroom rows per level collapse to one level with the union grade band (schema has no section concept). Verified read-only: Brampton L5 → 47 students (Gr 8), L6 → 71 (Gr 9–10, grade-9 kids moved from L5); Scarborough Level A → 33 (Gr 1–2), Level E → 60 (Gr 9–12). The sheet's **"Class Dates" (calendar) tab is NOT yet populated** — pending owner review. **PROD PARITY GUARANTEE:** the seeder's LOCATIONS defs (this transcription of the sheet) are the single committed source of truth — running `seed:bala-vihar-levels --year 2026-27 --allow-prod` on prod writes **byte-identical** data to UAT (no re-reading the xlsx, no manual entry). New **`--verify`** mode (read-only, safe on prod without `--allow-prod`) reconciles a target DB against those defs and exits 0 iff they match — ran on UAT 2026-07-06: all 17 levels ✔ (Brampton Parents correctly flagged as an intentional extra). **Prod TODO:** at cutover run `seed:bala-vihar-levels --year 2026-27 --dry-run --allow-prod` → `--allow-prod` → `--verify` (exit 0 = prod matches the sheet) after the 2026-27 offerings/periods exist and BEFORE teacher prefill; note the West L5/L6 reband moves grade-9 West kids between rosters. No new index / collection / schema.
- **2026-07-04** — **2026-27 Brampton Bala Vihar levels populated (corrective) + `seed:bala-vihar-levels` generalized.** The 2026-27 BV level docs were **phantom** (teacher-refs-only — no `levelName`/`gradeBand`/`enabled`), created as a side effect of the 2026-07-04 `prefillTeachers` run (`assignTeacher`'s set-with-merge writes `levels/{id}.teacherRefs`, which CREATES a stub doc when the level id has no real doc). Net effect: a teacher opening the current-year class saw **"No students"** — `deriveRoster`'s `memberMatchesLevel` filtered out every child because the level carried no `gradeBand` — while the 553 graded 2026-27 Brampton children had no real class to land on. (The 2026-06-07 `school-year:start` entry claims it "created 18 BV 2026-27 levels"; those are exactly the 18 that are now phantom — the real docs did not survive, root-cause TBD.) **Fix (code):** generalized `seed:bala-vihar-levels` (§10) with `--year YYYY-YY` (default 2025-26) / `--location` / `--delete-ids` / `--reset-teachers`, and made it **preserve** existing `teacherRefs` on re-run (it previously `.set({teacherRefs:[]})` — a latent wipe-on-reseed footgun that would have cleared teacher assignments on any 2025-26 re-seed). **Fix (UAT data):** ran `seed:bala-vihar-levels -- --year 2026-27 --location Brampton --delete-ids brampton-level-1-krishna-bv-brampton-2026-27`. Result: the 10 Brampton 2026-27 levels (Shishu Vihar, Pre-Level 1, Level 1–7, Parents) are now **real + enabled** with their grade bands, `teacherRefs` preserved from the phantoms (Level 1 = 3 teachers, others = 1); the stray manual duplicate `Level 1(krishna)` (0 teachers) was deleted so grade-1 kids match one clean Level 1. Verified read-only: `deriveRoster('brampton-level-1-bv-brampton-2026-27')` now lists **52 grade-1 students** (was 0). **Scarborough's 8 phantom 2026-27 levels are intentionally left as-is** (owner-scoped to Brampton; the East table/updated levels come from Vaibhav — re-run with `--location Scarborough`/edited defs then). **Known follow-ups (NOT done this change):** (a) `getMyLevels` has **no school-year filter**, so a teacher assigned to both years' Level 1 sees **two cards** (last year's empty + this year's populated) — needs a live-year filter (`features/setu/teacher/levels.ts`); (b) `school-year:start` should create **real** 2026-27 level docs at rollover (it evidently didn't stick) — root-cause TBD. **Prod TODO:** at each school-year rollover run `seed:bala-vihar-levels --year <newYear> --allow-prod` (dry-run first) to create the new year's **real** level docs **BEFORE** `prefillTeachers`/`copy-teachers`, so teacher-assignment merges land on real docs rather than minting phantoms. No new index / collection / schema change.
- **2026-07-04** — **Slice 3 — admin/teacher polish** (spec `docs/superpowers/specs/2026-07-03-slice3-admin-teacher-polish-design.md`; plan `docs/superpowers/plans/2026-07-03-slice3-admin-teacher-polish.md`; commits `3e72f4a..6e6a09a`). **Code-only — NO new Firestore index, NO new feature flag, NO schema/collection/migration.** **(A) Grade dropdowns from the canonical source:** every grade picker (level create/edit, child profile, guest-add) now ticks tokens from `GRADE_BAND_OPTIONS` / `CHILD_GRADE_OPTIONS` (derived from `@cmt/shared-domain` `GRADE_LADDER` — JK, SK, Grade 1…12; no free-text). The level modal's free-text **age field is removed**; the human age/grade label is derived via `levelGradeSummary(levelKind, gradeBand)`. Level-name uniqueness is now enforced (normalized) within `(location, period)` on BOTH create and rename (single-`pid` read → **no new index**) — a colliding rename returns **`409 level-conflict`**. **(B) Per-level teacher assignment:** new **`GET /api/admin/teachers/search?q=`** (admin + welcome-team; reuses the family `searchKeys` index — no new index) and **`POST`/`DELETE /api/admin/levels/[levelId]/teachers`** (admin + welcome-team) — both covered by the existing `/api/admin/*` catch-all. The `/admin/levels` table gained inline teacher **name-search + pills** (add/remove) and the standalone teacher-assignments tab was removed. Both write paths route through `assignTeacher`, which keeps the **two sources of truth in sync in one batch** — `teacherAssignments/{mid}.levelIds` AND the denormalized `levels/{id}.teacherRefs`. **(C) Binary attendance:** the Setu teacher marker + `POST /api/setu/teacher/attendance` (and `…/guests`) now WRITE only `present` | `absent` — `late` → `400`; reads stay tolerant (historical `late` events still returned, folded into Present). The welcome **reports** attendance summary drops the **Late** column (Late folds into Present). Legacy `/check-in/teacher` marker likewise offers Present/Absent only (data + the uninformed page unchanged). Mobile contract entry `MOBILE_API_CHANGELOG.md` `de017f6`. **(D) Rollover fix:** the school-year teacher carry-forward (`copy-teachers`) now writes BOTH `teacherAssignments` and `levels.teacherRefs` (previously only one), fixing an empty teacher list on the new year's twin levels. **E2E (write-only, run at the UAT gate after deploy):** `e2e/setu/admin/level-management.spec.ts` (grade-dropdown create + no age field + derived label + rename-conflict + inline teacher assign/pill/remove; self-cleans created levels via the admin SDK — there is no level DELETE API) and `e2e/setu/teacher/attendance-binary.spec.ts` (2 tap-to-present rows, no Late/Uninformed, mark two → save → persists on reload; legacy marker's Present/Absent-only is covered by its unit test). `seed:e2e-family` now also provisions a dedicated `_test` attendance fixture (level `e2e-att-level` + family `CMT-E2E-ATT` with 2 grade-matched children) — see §10. **UAT E2E gate (2026-07-04, deployed cmt-setu.vercel.app): GREEN** — `disclaimers` 2/2, `level-management` 3/3, `attendance-binary` passed (a bare `getByLabel(/age/i)` E2E selector false-matched the Kind select's "Shishu (age 1.5–4)" option → scoped to `role=textbox`, commit `e583072`). **OPS action — DONE 2026-07-04:** the UAT **2026-27** twin levels were teacher-EMPTY (copy-teachers never run for that rollover). The `copy-teachers` **route hardcodes `fromYear = currentYear`**, and UAT `currentYear` had already advanced to **2026-27**, so the route would copy 2026-27→2027-28 (a no-op). Instead ran `prefillTeachers(db, {fromYear:'2025-26', toYear:'2026-27', dryRun:false})` directly (18 twins filled, 0 skipped; sync verified — sample `brampton-level-1-…-2026-27.teacherRefs` = 3 teachers, each ref's `teacherAssignments.levelIds` includes the twin). **Route limitation to remember:** `copy-teachers` only works when run BEFORE advancing `currentYear`; to backfill an already-advanced year, run `prefillTeachers` with the explicit prior `fromYear`. **Prod TODO:** none beyond deploying the code (no DB op / index / migration); at each school-year rollover run `copy-teachers` **immediately after `start` (twin creation) and before/around `activate`** — while `currentYear` is still the old year — so the new year's levels carry their teachers.
- **2026-07-03** — **Slice 2 — Family disclaimers.**
  - **New flag `NEXT_PUBLIC_FEATURE_SETU_DISCLAIMERS`** (OFF by default). Gates the /family DisclaimerGate, the /disclaimers route, and the dashboard `disclaimersPending` field. Ships dark; flip ON in UAT for the E2E gate and at launch. Registered in `turbo.json` `env`.
  - **New `app_config/disclaimers` doc** (admin-editable at /admin/disclaimers). Created on the first admin publish; absent = seed DEFAULT (version 1). No seed write required to ship.
  - **New optional `families.disclaimersAccepted` field** `{ schoolYear, version, acceptedByMid, acceptedAt }`. Absence = must-accept; NO backfill/migration.
  - **No new Firestore composite index** (all single-doc reads).
  - **canAccessRoute**: `/api/setu/disclaimers` GET any-family / POST accept manager-only; `/disclaimers` page any-family; admin editor + API admin-only.
  - **Mobile**: additive `GET /api/setu/disclaimers`, `POST …/accept`, and `dashboard.disclaimersPending`. MOBILE_API_CHANGELOG updated.
  - **E2E**: `e2e/setu/disclaimers.spec.ts` (deployed UAT). The shared E2E family seed default is `--disclaimers accepted` so the fixture is NOT gated in other specs once the flag is on.
  - **Prod cutover**: setting `NEXT_PUBLIC_FEATURE_SETU_DISCLAIMERS=true` in prod gates ALL families on their next manager visit (absence = must-accept). Confirm the seed content / admin copy is finalized before flipping.
- **2026-07-03** — **Slice 1 — family dashboard simplification + enrollment triggers** (spec `docs/superpowers/specs/2026-07-06-slice1-dashboard-simplification-and-enrollment-triggers-design.md`; plan `docs/superpowers/plans/2026-07-06-slice1-dashboard-simplification-and-enrollment-triggers.md`; commits `00de86c..164c80a`). **Code-only — NO schema/collection/field change, NO new Firestore index, NO migration.** **(A) Enrollment triggers:** `isEnrollmentConfirmed` widened so `enrolledVia:'family-initiated'` (clicked Enroll, even $0) OR `'first-attendance'` (teacher auto-enroll) now read **Enrolled** on their own — amber **Registered** now survives ONLY for `'promotion'`/`'welcome-team'` records with zero engagement (builds on the #23 2026-07-02 entry; `enrolledVia` is also threaded through `reports/enrollment-report.ts` with a safe `'promotion'` fallback so the welcome report's confirmed/registered split stays consistent). Still DERIVED — retroactive, no data change. **(B) Dashboard:** `/family` rebuilt to 3 blocks (Family · Action Items · Bala Vihar) both layouts; the BV donation CTA now lives ONLY in the Bala Vihar section as **"Complete donation"** (owner decision 2026-07-03 — removed from Action Items so it's never double-prompted; `model.actionItems` is the now-empty Slice-2 disclaimers seam); removed the email/phone nudge, the confirm-nudge sentence, and the Seva/Prasad/other-program/calendar cards. **(C) NEW env flags (register in Vercel §9):** `NEXT_PUBLIC_FEATURE_SETU_SEVA` + `NEXT_PUBLIC_FEATURE_SETU_PRASAD` (both **OFF by default** — hide Seva/Prasad from families: dashboard + family left-nav + `/family/seva|prasad` route guards `redirect('/family')`; welcome-team/admin Seva+Prasad UNTOUCHED), plus optional `NEXT_PUBLIC_FAMILY_CALENDAR_URL` (when set, the family Calendar nav links out to that PDF in a new tab; else internal). All three added to `turbo.json` env. **(D)** per-member "missing info" completeness chip on `/family/members` (uses the existing `@cmt/shared-domain` required-fields matrix — no new logic). **(E) Additive `GET /api/setu/dashboard` fields:** `family.counts:{children,adults}`, `balaVihar.children:[{mid,firstName,levelName,teacherNames,attendance:{present,total}}]`, top-level `actionItems:[]` (empty in Slice 1); `balaVihar.bvState` semantics WIDEN per (A). Mobile contract entry `MOBILE_API_CHANGELOG.md` `4195d05`. New read helpers `getPerChildBalaViharAttendance` + `getBvTeacherNames` (the latter resolves only member-`mid` teachers via the **already-deployed** `members.mid` collectionGroup index — standalone `tid`-based teachers are skipped, fails soft to level-name-only; a fast-follow if tid teachers must show). `seed:e2e-family` gains a `--enrolled-via <family-initiated|promotion>` flag (default `'promotion'`, preserves the #23 ground state; §10) + writes a `levelSnapshots` entry; new deployed-UAT E2E `e2e/setu/dashboard-slice1.spec.ts`; the #23 `enrollment-state.spec.ts` realigned to the rebuilt copy. **Prod TODO:** none beyond deploying the code — leave `NEXT_PUBLIC_FEATURE_SETU_SEVA`/`_PRASAD` unset (families keep Seva/Prasad hidden), set `NEXT_PUBLIC_FAMILY_CALENDAR_URL` only once a yearly-PDF URL exists; note the visible cutover behavior: at deploy MORE not-yet-engaged families flip from Registered → **Enrolled** than under #23 alone (any family that clicked Enroll or had a kid auto-enrolled), which is the intended product behavior.
- **2026-07-02** — **Enrollment engagement criteria — derived "Registered" vs "Enrolled" (issue #23)** (spec `docs/superpowers/specs/2026-07-02-enrollment-engagement-criteria-design.md`; commits `ed3c0fb..1767a11`). A family now reads **Enrolled** for Bala Vihar only when ENGAGED that school year — attended ≥1 BV class in the enrollment's offering window OR any completed donation for that enrollment's eid OR (legacy offerings) legacy-roster paid; an active-but-unengaged enrollment (incl. every rollover-promotion/backfill record) shows an amber **Registered** pill + confirm nudge + donate CTA (mobile dashboard gains its first donate CTA). Implemented as **DERIVED state** (`isEnrollmentConfirmed`, one shared predicate): **no schema/enum change, no new collection or field, no Firestore index change, no migration — retroactive by construction**. `isEnrolled` keeps doc-exists semantics everywhere; the new three-state `bvState` carries the meaning (dashboard model/page, `GET /api/setu/dashboard` → `balaVihar.bvState` — additive, mobile contract entry `MOBILE_API_CHANGELOG.md` `2e87f19`). Welcome surfaces: roster rows gain `bvEngagement` chip (full teacher+door signal); the enrollment report gains `confirmed`/`registered` split (BULK derivation — deliberately omits door-only engagement to avoid per-family fan-out, so a door-only-engaged family can read Registered in the report but Confirmed on the roster; documented in `deriveBvConfirmedFids`, revisit at kiosk cutover). `seed:e2e-family` gains the `-- --confirm-bv` mode (§10) + deployed-UAT E2E `e2e/setu/enrollment-state.spec.ts`. **Prod TODO: none beyond deploying the code** — derived semantics apply to existing prod data automatically; expect every not-yet-engaged family (incl. all rollover-promoted ones) to flip from "Enrolled" to "Registered" at deploy, which is the intended product behavior (communicate alongside the year-rollover messaging).
- **2026-06-25** — **FID/MID renumber — public ids on families/members (issue #4).** New stored fields **`families.publicFid`** (4-digit user-facing Family ID, e.g. `1042`) and **`members.publicMid`** (5-digit Member ID, e.g. `50001`), both **additive + nullable** (legacy `fid`/`mid` stay the join keys, unchanged). New **`counters` collection** holds the monotonic allocators — `counters/familyPublicId` (`{next}`, starts **1001**) and `counters/memberPublicId` (`{next}`, starts **50001**); both self-create on first allocation (no seed). New member creation (`POST /api/setu/members`) and family registration allocate a `publicMid`/`publicFid` going forward. One new **collectionGroup field-override index `members.publicMid` (COLLECTION_GROUP ASC)** added to `firestore.indexes.json` and **deployed to UAT** (`firebase deploy --only firestore:indexes --project chinmaya-setu-uat`, no `--force`). New backfill script **`migrate:public-ids`** (`pnpm --filter @cmt/portal migrate:public-ids`; `scripts/assign-public-ids.ts`) assigns a `publicFid` to every existing family + a `publicMid` to every existing member from the same counters, **idempotent** (skips docs that already have one); UAT-only by default — refuses unless `PORTAL_FIREBASE_PROJECT_ID=chinmaya-setu-uat`, `--allow-prod` to override; supports `--dry-run`, `--limit N`, `--fid X`, `--csv-out path`. **RAN on UAT 2026-06-25** — assigned **877 families** (`publicFid` 1001→1882) + **2668 members** (`publicMid` 50001→52680), 0 skipped on the first pass; idempotent re-runs assign nothing. (Fixed first: the allocator no longer `import 'server-only'`, which had made the backfill — and every seed via `registerFamily`/`lazyMigrateLegacyFamily` — crash under tsx with "Cannot find module 'server-only'".) The `e2e/setu/public-ids.spec.ts` deployed-UAT spec is now green (5/5): dashboard API exposes `publicFid`/`publicMid`, the family chrome shows `FID 1042`, member detail shows `Member ID 50002`, and roster search by `1042` finds the family. These public ids are now also surfaced in the mobile-facing API: `GET /api/setu/dashboard` (`family.publicFid` + each `members[].publicMid`), `GET /api/setu/family` (`family.publicFid` + `members[].publicMid`), `GET /api/setu/members/[mid]/profile` (`profile.publicMid`), and `GET /api/setu/family/search` (`hits[].publicFid`) — all additive/nullable; mobile contract entries in `MOBILE_API_CHANGELOG.md` (this date + `921bb37`). **Prod TODO (cutover order):** deploy the `members.publicMid` index to 715b8 (no `--force` — §5) and let it build → deploy the code → after family migration, run **`migrate:public-ids --allow-prod`** against 715b8 (`--dry-run` + `--csv-out` first to preview the allocation) to assign public ids to all existing families/members; idempotent re-runs are safe.
- **2026-06-24** — **Academic school-year context Phase 2 (year switcher + Past/Live/Preparing surfaces).** Admin/welcome chrome gains a **`SchoolYearSwitcher`** (`#sy-switch` select) whose selectable set is `listKnownSchoolYears` = BV offering `termLabel`s (+ the live year). Choosing a non-live year sets `?year=…` (omitted for live); **Preparing** years (`year > live`) show a "not live yet" strip, **Past** years (`year < live`) render **read-only** (mutate controls disabled, e.g. the Level-management "+ New level" button). New admin-only year-copy routes `POST /api/admin/school-year/copy-{prasad,seva,teachers}` (all covered by the `/api/admin/*` catch-all). Seva gains an additive **`'draft'`** opportunity status (families' browse list is `status:'open'`, so drafts are invisible until opened — no migration; pre-existing docs stay `open`/`closed`). `GET /api/setu/dashboard` now returns a top-level **`schoolYear`** (the live year). `GET /api/setu/calendar` is now **live-year-scoped** — `getPublishedCalendar` filters to the live year's full `[start, end]` window, so preparing-year (cloned) Sundays are excluded until the year is Activated. Past-year writes are blocked by **`assertWritableYear`** (`POST /api/admin/calendar` returns **`409 past-year`** for a date in a past school year). **NO new Firestore indexes** — every year filter is **in-memory** (the calendar query is the same single `(location, date)` / `(location, programKey, date)` shape already indexed; year scoping is a post-fetch `.filter`). **NON-DESTRUCTIVE** — Phase 2 does **not** flip the live year or run Activate (that's Phase 1's `POST /api/admin/school-year/activate`). New **UAT-only** fixture `seed:year-switcher-fixture` (`pnpm --filter @cmt/portal seed:year-switcher-fixture`; refuses unless `PORTAL_FIREBASE_PROJECT_ID=chinmaya-setu-uat`, no `--allow-prod`): idempotently seeds a Past `bv-brampton-2024-25` + Preparing `bv-brampton-2026-27` BV offering (+ `donationPeriods` mirror), one BV level per year, a `sevaYear:'2024-25'` seva opportunity, and an enabled `2026-09-06` (preparing-year Sunday) calendar entry — all `_test:true`, created-only-if-absent so a re-run never clobbers real data. Non-destructive deployed-UAT E2E `e2e/setu/admin/year-switcher.spec.ts` (≥3 selectable years; select Preparing → `?year=2026-27` + not-live strip; Past → read-only strip + disabled create control + `409` past-year write; `/api/setu/calendar` excludes the preparing Sunday; `/api/setu/dashboard.schoolYear === '2025-26'`). **Prod TODO:** none beyond deploying the code — **no DB op / index / migration** for Phase 2; the fixture is UAT-only and must never run against `715b8`. Mobile contract: `GET /api/setu/dashboard` gains `schoolYear` and `GET /api/setu/calendar` is now live-year-scoped — record in `MOBILE_API_CHANGELOG.md`.
- **2026-06-23** — **Academic school-year context Phase 1 (Year center).** New admin-only routes `POST /api/admin/school-year/{activate,copy-calendar}` (both covered by the `/api/admin/*` catch-all); `GET /api/admin/school-year` now also returns a **`readiness`** block (`{toYear, promotionRan, offerings, levels, calendar, teachers, prasad, seva}`) computed alongside the existing `{config, nextYear}`. **Activate** flips `app_config/school_year.currentYear` forward **AND** aligns seva's active year (`app_config/seva_requirement.currentSevaYear` ← next year) in the same call — **gated** by the promotion having run (returns **`409 promotion-not-run`** unless an active `bv-*-{nextYear}` enrollment exists), so it can't strand families on an empty next year. **New Firestore index** `classCalendarEntries(programKey ASC, date ASC)` (collection-scope) added to `firestore.indexes.json` and **deployed to UAT** (no `--force`) — backs `copy-calendar` reading the prior year's calendar. New **live-year badge** in the admin/family/teacher chrome (`SchoolYearBadge`, cacheTag `'school-year'`, busted via `revalidateTag('school-year')` on Activate). **No schema/collection/field change, no migration, no new env var/flag** — `app_config/school_year` + `seva_requirement` already exist. Non-destructive deployed-UAT E2E `e2e/setu/admin/year-center.spec.ts` (asserts the surface + Activate gate, never flips the live year — the flip happy-path is the activate route unit tests). **Prod TODO (cutover order):** deploy the `classCalendarEntries(programKey, date)` index to `715b8` (`firebase deploy --only firestore:indexes --project chinmaya-setu-715b8`, **NO `--force`** — §5) and let it finish building, **then** deploy the code; at rollover an admin runs Start → Promote → Activate from `/admin/school-year` (Activate also moves the seva year, so confirm the seva offerings/requirement for the new year first).
- **2026-06-23** — **Seva `(sevaYear, date)` composite index** added to `firestore.indexes.json` and **deployed to UAT** (no `--force`). Fixes a runtime `500 FAILED_PRECONDITION` on **`/family/seva`** (and any caller of `getFamilySevaView`) that surfaced the moment an admin set the active seva year (`app_config/seva_requirement.currentSevaYear` = `2026-27`): the family seva view runs `listOpportunities({ sevaYear })` — `where('sevaYear','==').orderBy('date')` with **no status filter** — which needs `seva_opportunities(sevaYear ASC, date ASC)`; the pre-existing `(sevaYear, status, date)` index does NOT cover it (the middle `status` field can't be skipped). While `currentSevaYear` was `null` the view returned early and ran no query, so the gap was latent. **Code-only otherwise — no schema/collection/field change, no migration.** **Prod TODO:** at seva go-live (or cutover), deploy this index to `715b8` (`firebase deploy --only firestore:indexes --project chinmaya-setu-715b8`, **NO `--force`** — §5) and let it finish building **before** an admin sets `currentSevaYear` in prod, else `/family/seva` 500s for every family until the build completes.
- **2026-06-22** — **Offering date-overlap guard + `teacher-managed` payment source + donation-period donate flow** (merge `096463e`, from `codex/separate-worktree-394a`). **Schema:** `@cmt/shared-domain` `PAYMENT_SOURCES` gains **`teacher-managed`** (offerings whose donation the teacher collects off-portal) — additive, no migration. **Behavior change (admins):** creating/editing an ENABLED offering whose (programKey, location) date window overlaps an existing enabled one is now **rejected `409 offering-date-overlap`** on `POST/PATCH /api/admin/offerings` (was a soft `overlapWarning`); open-ended (null endDate) and location-less offerings are now included in the check. **No new Firestore index** — the existing `offerings:[enabled,location,programKey,startDate]` composite already covers the equality query. **`POST /api/setu/donations/checkout`** returns `422 payment-source-teacher-managed` for teacher-managed enrollments (before any Stripe call); dashboard/donate/enroll hide the in-portal Give button for them. The Bala Vihar donation **acknowledgements** gate shipped but is **DORMANT** (wired off at `app/family/donate/page.tsx` with placeholder copy) — provide final disclaimer text + flip the one-line flag to enable. Mobile contract entry: `MOBILE_API_CHANGELOG.md` `096463e`. **Prod TODO:** none — code-only (no DB op/index/migration); when setting up prod offerings, note overlapping enabled offerings are now hard-rejected.
- **2026-06-22** — **Profile-completion gate + required member-field matrix** (spec `docs/superpowers/specs/2026-06-22-profile-completion-gate-and-registration-fields-design.md`; commit `120c885`). A post-sign-in gate (`app/family/layout.tsx`) now **hard-redirects any family with incomplete required member info to `/family/complete-profile`** before the dashboard (a manager completes the whole family; a plain member only their own record). Enforced everywhere via the single shared matrix `@cmt/shared-domain/setu/member-required-fields`: ALL members need gender (**`Male|Female`** on write — `PreferNotToSay` stays ONLY on the read enum + the 3 internal sentinel-minting paths) + foodAllergies (a "No known allergies" choice writes the `'None'` sentinel); adults need email+phone+≥1 volunteeringSkill; children need schoolGrade+birthMonthYear (`birthMonth` 1-12 derived server-side). Required-ness is enforced at the WRITE routes (`POST/PATCH /api/setu/members`, `POST /api/setu/register`) + the forms — **never** by tightening the read-validated `MemberDocSchema`. New 400 codes `foodAllergies-/contact-/grade-/birthmonth-required` (+ existing `skills-required`). Same-**family** contact reuse now **shares** the contactKey instead of overwriting it (fixed a mis-seating bug: completing a migrated spouse with the manager's own email had re-pointed the manager's sign-in to the spouse). **No schema/index/collection change, no new env var/flag, no migration** — pure read-validation + UI + one new middleware request header (`x-portal-pathname`, so the gate's server component can read the path). Seeds/fixtures (`seed-test-accounts`, `seed-e2e-family`, `seed-join-request-family`, e2e fixtures) updated to write gate-complete members so existing E2E specs aren't redirected; new `seed:profile-completion-family` (UAT-only; `pnpm --filter @cmt/portal seed:profile-completion-family`) provisions a deliberately-incomplete manager for the gate E2E. Mobile contract entry: `MOBILE_API_CHANGELOG.md` `120c885`. **Prod TODO:** none beyond deploying the code — BUT note the visible cutover behavior change: on first sign-in **every already-migrated family (~800) with missing required fields** (most migrated adults lack foodAllergies/skills; some children lack birthMonthYear) is **hard-gated to `/family/complete-profile`** until a manager completes the family. This is intended (owner decision: applies to everyone, no flag), but communicate it before go-live, or pre-seed `foodAllergies:'None'` + derive birthMonth via a backfill to soften the first-sign-in friction.
- **2026-06-22** — **Family-lookup manager/member classification + gated co-manager join-request** (spec `docs/superpowers/specs/2026-06-22-family-lookup-manager-member-join-request-design.md`). **New member field `members.portalAccess`** (`'active'|'pending'`, optional; absent ⇒ active). A non-manager **roster-migrated** adult is `'pending'` = **gated**: blocked from family access on *every* sign-in path (`verify-code`, `password-sign-in`, `magic/[token]` each return a `pendingApproval` signal and mint **no** session) until a manager approves. `lazy-migrate` now writes `'pending'` on non-primary adults; ran new **`backfill:portal-access`** (§10) on UAT → **711 migrated non-manager adults gated** (878 managers / 1063 children / 13 registered-family members skipped; idempotent re-run = 0). **Scoped to migrated families only** (`families.legacyFid` present) **+ adults only** — registration-added members and the test personas keep access. **New collection `families/{fid}/joinRequests/{token}`** + **one collectionGroup index `joinRequests` (`token` ASC)** added to `firestore.indexes.json` and **deployed to UAT** (no `--force`). API: `POST /api/setu/family-lookup` gains **`matchAction: 'sign-in'|'request-to-join'`**; new `POST /api/setu/join-request/send` (open, IP-rate-limited, anti-enum `{ok:true}`, notifies all managers email+SMS), `GET /api/setu/join-request[/[token]]` + `POST .../approve` + `POST .../decline` (manager-only). `approve` promotes the matched member to **co-manager** (atomic txn, `manager:true` + `family.managers` arrayUnion + `portalAccess:'active'`, invite/accept contactKey theft-check). UI: register 3rd branch, `/family` approve panel, `/join-request/[token]` page, sign-in pending state. Mobile contract entry: `MOBILE_API_CHANGELOG.md` `0225cca`. **Prod TODO (cutover order):** deploy the `joinRequests(token)` index to 715b8 (no `--force`) → after family migration, run **`backfill:portal-access --allow-prod`** against 715b8 to gate roster-migrated non-manager adults — **dry-run first** and confirm the scope, this is a behavior change that locks those members out of portal access until their manager approves them. Register-new **step 2** redesign still pending owner spec.
- **2026-06-15** — **Security review fixes (5 findings).** (1) **Registration is now OTP-gated** — `POST /api/setu/register` requires a one-time `registrationGrant` proving the manager's email was just OTP-verified (it previously minted a `family-manager` session for any unverified email = account squatting). **New collection `registrationGrants/{token}`** (`{contactHash, createdAt, expiresAt}`; 20-min TTL; single-use, deleted on consume) — created lazily at runtime, **no index, no seed**. `verify-code` issues the grant on the email-no-family path; the `/register/family` wizard gained an email-code step; the mobile spec was updated (the RN app must thread the grant). (2) Magic-link sign-in URLs build from a trusted canonical base (`NEXT_PUBLIC_PORTAL_BASE_URL` / host allowlist) instead of `x-forwarded-host` (host-header poisoning). (3) Public `family-lookup` returns only `{found, matchedType, matchedValue}` — no family PII. (4) CSV exports neutralize spreadsheet formula injection. (5) `/docs` markdown output is sanitized (new dep `sanitize-html`). Items 2–5 are **code-only** (no DB change). **Prod TODO:** none beyond deploying the code — `registrationGrants` self-creates and needs no index/`--force`; expired grants are deleted on the next consume (optionally add a Firestore TTL policy on `expiresAt` later to sweep abandoned ones).
- **2026-06-10** — **Role-persona test accounts seeded (UAT) + Staff→Sevak rename.** New `seed:test-accounts` (§10) created 8 password sign-in personas (`setu-test-*@chinmayatoronto.org`, shared `TEST_ACCOUNTS_PASSWORD` env — gitignored): Brampton parent family (manager + second-adult family-member + 2 children, active `bv-brampton-2025-26` enrollment **with `pid`**), Scarborough parent family (2 children, `bv-scarborough-2025-26`), 3 parent-teacher families with the **first `teacherAssignments` docs in UAT** (Brampton Level 1; Scarborough Level A; "universal" = all enabled levels), plus standalone auth-claim **welcome-team** and **admin** users. All family docs `_test:true` (the integration suite's cleanup sweep deletes them — re-run the seed after `test:integration`). Docs: `docs/runbooks/test-accounts.md`. New E2E `e2e/setu/test-accounts.spec.ts` (8 persona tests). Also renamed the user-facing word **Staff → Sevak** portal-wide: UI strings, `listStaff()`→`listSevaks()`, `StaffRow`→`SevakRow`, `StaffManager`→`SevakManager`, and the `GET /api/admin/users` response key **`{ staff }`→`{ sevaks }`** (safe now — no mobile/external API consumers yet; the `SevakRow.source` enum value `'staff'` is unchanged, it means "auth-claim grant path"). **Prod TODO:** none — test accounts are UAT-only by design; the rename is code-only.
- **2026-06-11** — **Prasad propose→confirm revision** (spec `docs/superpowers/specs/2026-06-10-prasad-propose-confirm-design.md`; admin-team feedback). Publish now writes `status:'proposed'`; families confirm in place or pick any open Sunday (`POST /api/setu/prasad/confirm`, manager-only via the existing prefix rule); admin assigns stragglers per-row (`assign:true` on the PATCH, transactional) or in bulk (`POST /api/admin/prasad/assign-remaining`, per-doc `lastUpdateTime`-preconditioned — conflicts are skipped and reported). New doc fields `confirmedAt`/`confirmedBy('family'|'admin')`/`proposalNotifiedAt`; statuses now `proposed|assigned|cancelled` ('assigned' still = committed, so pre-revision docs need **no migration**). Publish fires a one-time confirm-request email+SMS per family (chunked ×10; publish route pins `maxDuration=300`) and the daily cron nudges unconfirmed proposals at 7d/2d — both gated by `PRASAD_REMINDER_CRON_ENABLED` + allowlists; the admin screen surfaces disabled/failed sends after publish. **No new indexes** (cron reuses `(status,date)`; bulk assign is equality-only). Seed: proposed fixture on the Scarborough test family (`seed:test-accounts`, plain-set so re-seed resets a confirmed doc back to proposed). New E2E `e2e/setu/prasad-propose.spec.ts` (5 tests). **Operational ritual change:** publish proposals → families confirm over ~2 weeks → admin clicks "Assign all unconfirmed (N)" per location before the season starts. **Prod TODO:** unchanged from the prasad-module entry below.
- **2026-06-10** — **Prasad module shipped** (spec `docs/superpowers/specs/2026-06-10-prasad-module-design.md`). One prasad Sunday per family per school year — auto-assigned by the youngest child's birthday month (engine in `@cmt/shared-domain/setu/prasad-engine`), self-serve family moves (7-day lock, cap-checked transaction), 7d/2d email+SMS reminders, `/admin/prasad` preview→publish, `/family/prasad` + dashboard card, `/welcome/prasad` day-of list. **New collections:** `prasadAssignments` (doc id `{pid}-{fid}`), `prasadConfig/{pid}`. **Two new composite indexes** added to `firestore.indexes.json` and **deployed to UAT**: `prasadAssignments(pid,date)` + `prasadAssignments(status,date)`. **New member field** `members.birthMonth` (1–12); ran `backfill:birth-months` on UAT — **906 children updated** from legacy `dob_m` (snapshot-fed, idempotent re-run = 0). (906 > the spec's "728 current students with `dob_m`" because the backfill matches EVERY migrated child with a `legacySid` — including no-longer-registered kids — not just currently-registered ones; 1,039 were eligible.) **Calendar field** `classCalendarEntries.prasadNeeded` (default true; admin toggle on `/admin/calendar`). **New cron** `/api/cron/send-prasad-reminders` (daily 14:00 UTC, in `vercel.ts`) gated by new env `PRASAD_REMINDER_CRON_ENABLED` (unset = disabled; set "true" in Vercel to go live) + existing `CRON_SECRET`. E2E `e2e/setu/admin/prasad.spec.ts` (10 tests) green vs deployed UAT; seed fixture extended (prasadConfig + one assignment for `CMT-FSWEDU2X`). **Prod TODO (cutover order):** deploy both indexes to 715b8 (no `--force`) → run `backfill:birth-months --allow-prod` after family migration → enter the Scarborough class calendar via `/admin/calendar` → admin publishes **proposals** per location from `/admin/prasad`, then (after the confirm window) bulk-assigns unconfirmed families before the first prasad Sunday → set `PRASAD_REMINDER_CRON_ENABLED=true`. NOTE (superseded 2026-06-22): prasad periods are now resolved from the app-managed current school year (`app_config/school_year`, set on `/admin/school-year`) — there is no longer a `CURRENT_PRASAD_PIDS` constant to bump at rollover (it was renamed to `FALLBACK_PRASAD_PERIODS`, used only when offerings/config are absent). See §7.
- **2026-06-10** — **RTDB download-cost optimization** (code-only; no DB schema/index/write change). Prod billing showed ~$14.84 of RTDB **download** charges (the legacy layout has no `.indexOn`, so kiosk lookups, the roster migration strip, crons, and migration scripts each downloaded the full `/roster`; the per-family legacy parser re-downloaded it per family — ~867× per script run). Three fixes: (1) `readRtdb()` (`@cmt/firebase-shared/admin/rtdb`) now has a **15-min in-process TTL cache** (live mode) — per-family re-reads and warm-lambda page loads download once; (2) **snapshot mode** — `RTDB_SNAPSHOT_DIR=.rtdb-snapshot` serves every `readRtdb()` from a local JSON snapshot captured once via new `snapshot:rtdb` (§10) and **never falls back to network** (missing file throws); snapshot dir is **gitignored (real family PII — never commit)**; (3) `/welcome/roster` migration strip is now **on-demand** (button) instead of fetch-on-mount. Local dev/scripts/tests now read RTDB **zero times**; Vercel runtime (no snapshot) uses the TTL cache. **Prod TODO:** none — at cutover the portal stops depending on RTDB entirely (kiosk cutover retires it; this optimization is interim cost control).
- **2026-06-09** — Re-ran **`backfill:bv-enrollments`** (full, UAT) to restore a representative teacher roster (UAT had drifted down to 6 active Brampton 2025-26 enrollments). Read prod 715b8 RTDB `/roster` **read-only** (`listAllFamilies()`), lazy-migrated each legacy family, and wrote **active** `bv-brampton-2025-26` / `bv-scarborough-2025-26` enrollments carrying `pid` (the field `deriveRoster` queries on). Result: **512 families enrolled** (357 Brampton / 155 Scarborough), 127 stale enrollments cancelled, 767 `schoolGrade` re-asserts, **0 errors**. Brampton **Level 1** now lists **53** students (was 1); other Brampton levels 39–99 each. Idempotent (deterministic `eid=${fid}-${oid}`, `merge:true`); enrolls only current school-age children (not adults; shishu needs `birthMonthYear` the grade-rows lack → Parents/Shishu stay 0). Verified end-to-end against deployed UAT (`cmt-setu.vercel.app`): password sign-in → `/teacher/levels/brampton-level-1-bv-brampton-2025-26/attendance` renders 53 `att-row`s, no access-denied. Added read-only diagnostic `scripts/inspect-brampton-level.ts` (`pnpm --filter @cmt/portal inspect:brampton-level`, §10). **No schema/index change** — this is a data backfill, not a migration. **Prod TODO:** during cutover, after family migration + level/offering seed, run `backfill:bv-enrollments --allow-prod` against 715b8 (no index needed) to populate teacher rosters; `--dry-run` is NOT a pure preview (it still lazy-migrates families), so dry-run only on a target you intend to migrate.
- **2026-06-09** — Rollover **admin "Set grade"**. New admin-only endpoint `POST /api/admin/school-year/set-grade {fid,mid,schoolGrade}` (covered by the `/api/admin/*` catch-all) that sets one Bala Vihar child's `schoolGrade` (grade restricted to the canonical `GRADE_LADDER`). Surfaced two ways: an inline "Set grade" control on each *need-attention* row of the rollover preview (refreshes the dry-run after save), and an admin-only grade editor on the welcome member detail page (`/welcome/family/[fid]/members/[mid]` — read-only for non-admin welcome-team). Resolves the "no grade set" rollover blocker without a CLI. **No schema change** (`schoolGrade` already exists), **no new index**. UAT writes are normal feature writes (not a migration) — prod replay is code-only.
- **2026-06-09** — Admin-revamp **Phase 4 (Reports hub)**. New `/welcome/reports` hub (welcome-team + admin) with four cards: enrollment headcounts, attendance summary, donations summary (**admin-only**), legacy check-in CSV. New read-only API `GET /api/welcome/reports/{enrollment,attendance,donations}` (`?format=json|csv`); `donations` gated to `isAdmin` at BOTH `canAccessRoute` and the handler. **No new Firestore indexes** — aggregations use bulk reads (unfiltered `collectionGroup('enrollments')`, top-level `attendanceEvents` single-field `date` range, top-level `donations`). The legacy `check-ins`/`guests` CSVs reuse the existing `POST /api/check-in/admin/reports/[kind]` (admin-only); the legacy `/check-in/admin/reports` page now `redirect('/welcome/reports')`. **No DB writes** → prod replay is code-only (no migration/index/seed step for this phase). **Prod TODO:** none beyond deploying the code.
- **2026-06-09** — Admin-revamp **Phase 3 (Roster)**. Added two composite indexes to `firestore.indexes.json` and **deployed to UAT** (`firebase deploy --only firestore:indexes --project chinmaya-setu-uat`, no `--force`): (1) collectionGroup `enrollments(programKey ASC, status ASC)` — backs the Roster **program filter** (`collectionGroup('enrollments').where('programKey').where('status','active')` in `features/setu/roster/list-families.ts`); (2) collection `families(location ASC, name ASC)` — backs the **location-filtered** ordered browse (`where('location').orderBy('name')`). Without these, the filtered roster paths throw `FAILED_PRECONDITION` in UAT/prod (unit tests use a fake that ignores indexes, so they stay green — caught only at runtime). New read-only APIs `GET /api/welcome/families` (browse/filter/CSV) + `GET /api/welcome/families/migration-status` (welcome-team + admin). The migration-status endpoint reads the legacy 715b8 RTDB `/roster` **read-only** (via `listAllFamilies()`; `masterRtdb()` exposes no write helpers) and diffs against Setu `families.legacyFid` — never writes 715b8. **Prod TODO:** deploy both indexes to 715b8 (no `--force`) during cutover, before first use of `/welcome/roster` filters.
- **2026-06-08** — Added collectionGroup **field-override** index `members.mid` (COLLECTION_GROUP ASC) to `firestore.indexes.json`; **deployed to UAT** (`firebase deploy --only firestore:indexes --project chinmaya-setu-uat`, no `--force`). Required by the new admin **Users & Roles** screen's `listSevaks()` (named `listStaff()` until the 2026-06-10 Staff→Sevak rename) (`collectionGroup('members').where('mid','==',ref)`) — without it the screen + `GET /api/admin/users` return 500 (`FAILED_PRECONDITION`). The existing teacher reads (`teacher/student-detail.ts`, `teacher/guests.ts`) use the same query and also depend on it. Caught by the new Playwright admin E2E (`e2e/setu/admin/*`). Also extended `scripts/seed-e2e-family.ts` to grant the single E2E test user (`E2E_FAMILY_EMAIL`) **admin** via `roleAssignments/{mid}`, so one UAT user drives family + admin browser tests. **Prod TODO:** deploy the `members.mid` index to 715b8 (no `--force`) during cutover, before first use of `/admin/users`.
- **2026-06-07** — Added collectionGroup index `enrollments(oid ASC, status ASC)` to `firestore.indexes.json`; **deployed to UAT** (`firebase deploy --only firestore:indexes --project chinmaya-setu-uat`, no `--force`). New scripts `school-year:start` / `school-year:promote` (§7, §10). Ran `school-year:start` (created 18 BV 2026-27 levels, idempotent) then `school-year:promote` **committed** the 2025-26→2026-27 rollover in UAT: 512 families, 769 promoted, 23 graduated; 24 Shishu (Pre-K) intentionally left out (handled at registration). Idempotent re-run verified (0 to promote). Enrollment schema gained `levelSnapshots` + `pid` + `'promotion'` enrolledVia; `enrollFamily` now writes `pid:oid`. **Prod TODO:** deploy the new index to 715b8 (no `--force`), then run the same Step 1 + promote sequence during cutover.

---

*Maintenance: when you add a new ops script, collection, index, env var, or feature flag, update §3, §5, §9, §10, and add a §14 Change-log entry in the same PR.*
